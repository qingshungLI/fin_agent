# AURORA 研究控制台

控制台从本地研究产物读取真实状态；默认展示二维工作视图，三维地图和统计图按需加载。暂停、继续、停止通过后端控制接口提交，回执不代表研究已在当前指令处中断，最终状态以新快照为准。

## 启动

在仓库根目录执行：

```powershell
npm ci
npm run build
powershell -ExecutionPolicy Bypass -File scripts/start-control-panel.ps1
```

启动脚本使用 `.venv-research` 和已构建的 `dist/`。默认服务地址为 `http://127.0.0.1:8001`。已有端口监听时脚本不会替换服务；前端构建后刷新页面即可读取新静态资源，修改 Python 路由才需要重启对应服务。

开发模式使用 `npm run dev`，浏览器访问 `http://127.0.0.1:5173`，API 代理到 8001。服务用于本机工作空间；本轮没有增加多用户登录、权限隔离或远程部署功能，企业上线仍需补齐这些能力。

## 当前页面

| 页面 | 用途 |
| --- | --- |
| 运行总览 | 尝试额度、测量数、子代数、LLM 请求；当前任务、下一步与控制入口 |
| 机制地图 | 二维机制 × 形式坐标；可切换三维、复位视角、查看选中坐标的真实结果 |
| 结构证据 | 搜索结构、主要 IC、持有期曲线、冻结表达式、父子谱系与检验原始记录 |
| 组合门控 | 各组合规则的成分、暴露、成本、缺失收益和执行状态 |
| 研究日志 | 事件记录与冻结配置；底部提供研究报告下载 |
| Research Skill | 下载协议与查看 SDK 数据契约 |
| 自有数据接入 | 上传数据、定义格子、运行 SDK 研究及导出结果 |
| 新建研究任务 | 保留兼容工作台入口，用于配置和提交引擎任务 |

展示模式隐藏侧栏，点击“退出展示”恢复。窄屏页面支持横向导航，主体不产生页面级横向溢出。

## 状态与证据边界

- 请求串行轮询，完成后等待 5 秒；切换批次会取消旧请求，防止过期结果覆盖新批次。
- 服务错误显示告警；已有快照可以保留，但明确提示可能过期。无批次时显示入口，不生成演示数据。
- 地图坐标与证据列表分别管理选择，空坐标不会显示其他坐标的最新结果。
- 缺失统计显示“—”，零结构显示 0；fast 和正式确认分开显示。
- “继续”仅在暂停、失败、中断或停止状态启用；失败批次能否恢复仍取决于后端冻结身份与资源检查。
- 旧批次状态不等于最新服务运行状态；控制台不修改冻结记录来使失败显示为成功。

## 验证与截图

```powershell
npm run build
# 需本机后端及 Vite 开发服务；真实批次用例需要已有验收产物
npx playwright test --workers=2
```

浏览器验收包含真实三维选取、结构证据与报告下载、移动端、组合、SDK 数据上传运行导出，以及模拟控制回执与过期请求取消。会提交真实引擎研究的用例默认跳过，需显式设置 `RUN_LIVE_RESEARCH=1`；它不属于本轮已通过的测试。

截图位于 `artifacts/validation/`。`console-overview.png` 为真实批次界面；名称包含 `fixture` 的图片仅是测试数据，不作为阶段研究成果。
## 控制接口的恢复边界

控制请求使用每批次操作系统锁串行处理；控制文件与 PID 记录采用原子替换。重复 resume/restart 遇到仍在运行的已登记进程时只提交请求，不再启动第二个进程。已完成批次或非法请求返回明确拒绝。

重新创建恢复进程前检查代码哈希与 Python/依赖环境；不一致时返回 409，并在前端展示具体原因。数据、模型、产物哈希仍由引擎完整验证。外部 CLI 启动且没有 PID 记录的 RUNNING/PAUSED 批次只接受恢复请求，不凭空认定进程已死；需在原启动终端核对其实际状态。

本轮修改了 `dashboard_server.py`，已有后端进程必须重启后才会启用上述控制保护。重启展示服务与停止研究进程是两件事；不要结束研究进程来刷新 UI。