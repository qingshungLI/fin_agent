# 研究控制台与评委展示

控制台直接读取本地真实研究产物，不生成演示收益或假子代。新增展示代码在 `dashboard_server.py` 与 `src/`，不改变 `engine/` 的冻结代码指纹。

## 启动

项目已安装 Three.js 和 React Three Fiber，构建产物在 `dist/`。在项目目录运行：

```powershell
npm run build
powershell -ExecutionPolicy Bypass -File scripts/start-control-panel.ps1
```

浏览器打开 `http://127.0.0.1:8001`，Python 服务同时提供页面和 API。仅监听本机，不自动开放公网。

开发时运行 `npm run dev`，浏览器打开 `http://127.0.0.1:5173`。Vite 将 `/api` 代理到 8001。原运行设置页保留在 `/#workbench`，可通过侧栏“运行设置”进入。

## 展示顺序

1. **运行总览**：选择当前批次，看真实搜索尝试、已测结构、LLM 逻辑请求和演化子代数量。预算消耗不是全流程完成率。
2. **三维机制地图**：10 个机制 × 7 种形式，对应 70 个可选择节点。拖动旋转、滚轮缩放、点击查看，右下角复位。颜色和高度表示研究状态，不表示收益。真实父子及 donor 关系存在时显示对应三维连线。
3. **二维地图**：按机制和形式逐格审查；与三维选择联动，并提供键盘可用的坐标选择器。WebGL 不可用时保留完整二维视图。
4. **研究中的假设**：展示冻结机制、表达式和主周期。正在提案时明确标记，不将未生成的假设伪装为研究结果。
5. **自动演化闭环与 LLM 角色**：解释提案、操作化、逻辑审查、盲下注、结果核对和归纳的分工。角色共用同一模型，不等于多模型独立投票；HTTP 内部重试和缓存命中不单独累计。
6. **结构证据**：点击已测结构，查看原始冻结机制、三个表达式、周期 IC、必要断言和晋级阻塞，下载研究报告。
7. **研究日志**：查看选取的真实模型/统计事件，以及数据质量、1000 bootstrap / 500 placebo / 500 棵树 / 20 切分等冻结配置。
8. **展示模式**：右上角进入精简侧栏的评委展示视图；桌面与手机均可查看。

## 状态来源与边界

- 页面每 5 秒轮询一次，每次请求结束后再安排下一次；断线时保留最后快照并显示错误。
- 当前计数来自批次检查点。新测、继承、拒绝和子代分别计数，继承不当作新的独立测量。
- 校准报告只有代码和依赖环境与批次一致时才显示。B/H 状态来自审计访问事实；不可验证时不会标为通过。
- 如果进程已停而检查点还显示 RUNNING，已登记进程的批次会显示中断。原检查点为 FAILED 而恢复进程正在运行时，显示检查点恢复中。
- 日志仅投影模型角色、研究阶段和统计进度，不公开密钥、完整模型请求或任意服务器路径。下载接口只接受报告白名单。
- IAAFT 无效时仍显示 untested；没有真实子代时显示 0。不将这些情况包装成 PASS。
- 三维使用按需渲染和最高 1.5 倍像素比；模块独立懒加载，切换二维会释放三维场景。坐标标签使用本地画布，无外部字体或模型依赖。

## 验证与截图

```powershell
.\.venv-research\Scripts\python.exe -m pytest tests/test_dashboard.py -q
npx playwright test tests/browser/control.spec.ts --workers=1
```

浏览器验收覆盖真实三维节点点击、二维 70 格联动、证据详情、报告下载、展示模式、移动端无横向溢出和 API 断线。真实服务需预先启动，测试不会额外启动收费研究。

截图：`artifacts/validation/control-desktop.png`、`control-mobile.png`、`control-evidence.png`。