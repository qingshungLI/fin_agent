# 北京大学金融AI智能体创新大赛 - 项目提交材料

## 项目名称
**AutoAlpha Harness - 证伪驱动的自动化因子研究系统**

## 项目摘要（200字）

AutoAlpha Harness是一个用证伪压力驱动的自动化因子研究系统。它不追求"找到更多因子"，而是对每个结论都明确其可信度、成立条件和失效边界。

核心创新：
1. **机制地图**：将无限的表达式空间压缩为10×7=70个可枚举的格子
2. **三态判定**：区分"证据充分"、"证据相悖"和"数据不足"
3. **信息防火墙**：提案器不能看到收益数据，保证机制先于观测
4. **e值证据累积**：允许边跑边看而I类错误仍受控

适用于量化投资研究、风险管理、智能投顾等金融AI场景。

## 项目公开介绍

### 一、问题背景

传统因子挖掘方法（遗传规划、LLM生成）的共同失败模式：生成大量候选 → 回测 → 挑最好的。问题在于**搜索本身制造假阳性**。

假设有10000个完全无用的随机因子，在零假设下最大IC约为：
$$\text{max IC} \approx 3.9 \times \sigma_{IC} \approx 3.9 \times 0.014 \approx 0.055$$

这会被认为是不错的因子，但它完全来自噪声。

### 二、核心创新点

#### 创新1：想法空间的机制地图（10×7矩阵）

不在无限表达式空间搜索，而是事前枚举一张**机制×形式**矩阵：

**行（机制族）**：
- M1 注意力冲击
- M2 流动性提供
- M3 处置效应与锚定
- M4 信息缓慢扩散
- M5 交易约束（A股特有）
- M6 被动资金与再平衡
- M7 参与者结构
- M8 风险补偿与估值
- M9 不确定性解决
- M10 隔夜信息聚合

**列（表现形式）**：
- F1 水平 / F2 变化 / F3 时序相对 / F4 截面相对
- F5 背离 / F6 条件化 / F7 事件化

70格中约52格可行。三个后果：
1. "想法空间有多大"成为可报告的数字
2. 空白格是研究计划
3. 标签成为统计模型的设计矩阵

#### 创新2：档案的双层结构（标签+断言）

每份档案分为两部分：

**标签**（受控词表）：
```yaml
agent: liquidity_provider
friction: inventory_risk
mispricing: temporary_impact
correction: inventory_unwind
form: F3_ts_relative
```

**断言**（可证伪命题）：
```
P1 剂量曲线单调向下
P2 峰值horizon ∈ [1,5]
P3 效应符号为负
P4 在流动性差的股票上效应更强  ← 旁证
P5 在竞价价差宽的股票上效应更强 ← 另一旁证
```

**硬要求**：至少2条旁证，且至少1条不指向因子本身。

#### 创新3：信息防火墙的工程实现

防止"看到数据后写机制"的四重措施：
1. 确认段独立目录 + 审计锁
2. 内容哈希链（修改历史档案会导致后续全部失效）
3. 白名单构造上下文（提案器只能看变量名）
4. 盲下注时间戳早于测量

这不是提示词约束，是**代码结构**强制。

#### 创新4：e值证据累积

用赌本度量证据：
- e值 = 似然比 = 下注后的财富增长
- 赌本 W = ∏eᵢ
- Ville不等式：P(∃n: Wₙ ≥ 1/α) ≤ α

**关键性质**：对所有时刻同时成立，允许**边跑边看**。

盲下注就是下注：LLM给概率q，零假设下断言成立概率p₀：
$$e = q/p_0 \text{ (成立)}, \quad e = (1-q)/(1-p_0) \text{ (不成立)}$$

#### 创新5：三态判定

- **PASS**：证据充分
- **FAIL**：与断言矛盾（有信息的失败）
- **UNDECIDABLE**：数据不足（没信息的失败）

把后两者混在一起，会把"没有证据"当成"有反面证据"。

### 三、技术架构

```
数据层 → 注册层 → 想法空间 → 测量层 → 判别层（四把刀）
                                  ↓
            证据层（e值） → 发现层 → 演化算子
                                  ↓
            一致性检查（符号图） → 记忆层（层次贝叶斯）
                                  ↓
                    规律账本 + 组合层 → 可交易因子
```

**四把刀**：
1. 安慰剂（效应是假的？）
2. 断言（原因不对？）
3. 增量（早就有了？）
4. 正交矩（在确认段成立？）

### 四、应用场景

#### 场景1：量化投资研究
- 自动探索因子空间
- 明确每个发现的可信度
- 防止过拟合和假阳性

#### 场景2：风险管理
- 识别失效条件
- 提供监控指标
- 市场状态切换检测

#### 场景3：智能投顾
- 可解释的投资建议
- 机制驱动而非黑箱
- 适应性组合构建

### 五、数据与实现

**数据**：
- A股日频量价（2016-2026，2448交易日，5562只股票）
- 包含成交笔数、五档盘口、PIT行业等稀缺字段

**技术栈**：
- 后端：Python 3.12 + NumPy/Pandas/PyArrow
- 统计：SciPy + scikit-learn
- 前端：TypeScript + React（展示）

**关键指标**：
- 57项工程测试通过
- 9个真实DeepSeek事前提案
- 3000次非线性交互检验模拟
- 800组必要断言校准

### 六、创新价值总结

#### 技术创新（30%）
1. 机制地图将无限空间降维为有限集合
2. e值允许合法的顺序检验
3. 信息防火墙的工程实现

#### 落地价值（40%）
1. 量化机构可直接部署
2. 规律账本可跨项目复用
3. 防止假阳性节省研发成本

#### 商业潜力（30%）
1. To B：卖给量化私募、资管
2. To C：智能投顾后端引擎
3. SaaS：因子研究云平台

## 核心代码说明

**主要模块**：
- `engine/data.py`：数据验证、PIT行业、复权
- `engine/pipeline.py`：研究主流程
- `engine/blades.py`：四把刀判别
- `engine/llm.py`：五个LLM岗位
- `engine/memory.py`：层次贝叶斯记忆
- `run_competition.py`：竞赛运行脚本

**关键设计**：
- 所有操作通过显式输入输出连接
- 确认段/封存段有独立审计
- 档案哈希链防篡改

## 演示视频脚本

1. **开场（30秒）**：问题背景 - 传统方法的假阳性
2. **核心展示（2分钟）**：
   - 机制地图可视化
   - 一个结构的完整流程
   - 四把刀判别
   - 盲下注与e值
3. **结果展示（1分钟）**：
   - 9个提案的评估结果
   - 规律账本示例
   - 测量面板展示
4. **总结（30秒）**：商业价值与应用场景

## 团队信息

（请补充团队成员信息）

## 项目封面

（建议使用机制地图的可视化图，16:9格式）
