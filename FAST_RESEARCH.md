# Fast 研究配置

2026-09-08 增加 `--fast`，用于优先验证 DeepSeek 提案、自动演化、组合及回测的工程闭环。完整配置保留，fast 不取得正式确认资格。

| 项目 | 完整配置 | fast |
|---|---:|---:|
| A 段日期 / 股票 | 全量 | 全量 |
| 网格起点 / 演化预算 | 70 / 140 | 70 / 140 |
| bootstrap | 1000 | 100 |
| 安慰剂 | 三类各最多 500 次 | 横截面打乱 19 次，仅诊断 |
| 时间移位 / IAAFT | 开启 | 延后 |
| 条件发现 | 全候选筛查、500 树 × 20 切分 | 事前候选表前 4 项筛查、探索排序前 2 项、30 树 × 2 切分 |
| 周期性贝叶斯采样 | 开启 | 延后 |
| 子结构入口 | 完整安慰剂通过且满足功效 | 允许未完整检验的探索候选进入，仍限制深度 2 |
| 正式 PASS / B/H | 满足独立确认条件才可进入 | 禁止 |

低重复次数得到的诊断 p 值不作为正式通过依据。安慰剂总状态保持 `untested`，被跳过的项目明确记录。低功效不阻止 fast 提出新假设，但基本有效日期数量、数据质量隔离、时间先后关系、冻结与审计仍保留。新子结构使用新 ID、继承来源和冻结规则，不把父结构的未知结论当成机制法则。

在项目根目录运行：

```powershell
.venv-research/Scripts/python.exe -u run_engine.py --fast --full-grid --run-id fast-new --industry-policy quarantine --industry-source exact_intervals --auction-policy quarantine --workers 3
```

早期 fast 批次为 `fastcycle-v2-20260908`，日志在同名 `.stdout.log`。第一次 fast 启动误选了本地不存在的日频行业数据源，已保留失败记录，第二次使用原完整研究的 `exact_intervals` 数据源。没有更改原始数据。

原完整批次 `fullcycle-20260908` 已暂停（已提交 12 个结构）；其引擎源码、CLI 和检查点在 `artifacts/full-engine-before-fast-20260908/` 保存。原批次证据保留。恢复原完整检查点必须使用与其冻结身份一致的源码和环境；不能用新 fast 引擎冒充原版本恢复。

`scripts/watch_fast_compositions.py` 已接入固定 4 / 8 / 16 个已完成后处理结构的节点，顺序生成组合与启动回放，单结构研究不等待它。批次按完成顺序取前 N 个结构，不按收益择优；分别使用前 4、8、16 个成分，避免相同成分重复计算。每次失败均保留报告。竞价数据导致的真实成交阻塞仍须独立解决，fast 不绕过它。

控制台显示 FAST 提示，仍可切回旧批次查看原结果。配置/诊断/演化入口与正式资格隔离测试已通过；本次全套 Python 为 98 passed，6 项浏览器测试通过，生产前端构建成功。

## 多进程条件发现（2026-09-08）

`--fast --workers 3` 现在把两个独立时间切分交给 Windows spawn 子进程。
`workers` 是并发上限：fast 默认只有两个切分，所以最多同时运行两个发现进程；
可用内存不足时按保守预算降为一个进程。`--workers 1` 保留直接串行路径，便于复核。
每个子进程只读取临时 Parquet 中对应日期的长表，不复制整个市场面板；
树训练和 BLAS 限为单线程，避免嵌套并发。所有输入、隔离带、候选、切点、
树数和种子保持原定义，结果按切分序号汇总，工作进程失败会使本次研究明确报错。

森林结果的 `split_diagnostics` 记录真实 PID、切分序号和计算秒数。
日志会输出 `discovery multiprocessing: N processes`；查看该值即可确认实际并发数。
内存预算是调度估计，不是操作系统硬上限；临时文件所在磁盘需要容纳完整案例长表。

复核旧结构的全量发现阶段（不调用 LLM、不改写旧证据）：

```powershell
.venv-research/Scripts/python.exe -u scripts/benchmark_discovery_parallel.py --source-run fastcycle-rsi-v2-20260908 --structure S-fastcycle-rsi-v2-20260908-001 --workers 2 --output artifacts/discovery-multiprocess-benchmark.json
```

脚本先核验源结构和面板缓存哈希，再重做相同候选筛查和发现，输出耗时、逐折 p 值、
候选一致性和实际 PID。与历史耗时比较会受到机器负载影响，不能据此保证固定加速倍数。
源码升级后使用新批次冻结身份；旧批次源码已归档至
`artifacts/engine-before-multiprocess-20260908/`，不能直接用新源码恢复旧检查点。


本次实测使用 `S-fastcycle-rsi-v2-20260908-001` 的全量冻结信号：
1,458 个交易日、5,562 只股票。候选筛查加重复发现从原来的 **454.923 秒**
降至 **238.610 秒**，历史同机比较为 **1.907 倍**；加载缓存的时间不计入发现阶段。
两折 p 值均逐值一致（0.2504376670250518、0.4422843542224059），候选频率也一致。
两个真实工作进程分别耗时约 213 秒，报告为
`artifacts/discovery-multiprocess-benchmark.json`。这不是端到端固定加速保证。

当前新批次为 `fastcycle-mp-v2-20260908`（`--fast --full-grid --workers 2`），
保留 70 个起点、140 个演化预算及 DeepSeek 调用，日志为同名 `.stdout.log`。
监督器和 4/8/16 结构组合监听在检查点建立后启动。前一批次
`fastcycle-rsi-v2-20260908` 已在完成两个结构后暂停，旧结果与源代码快照均保留。


完整集成时发现首版 `fastcycle-mp-20260908` 因主进程占用内存，被保守预算降为
一个进程；该批次在首个结构尚未完成时暂停，快照保存在
`artifacts/engine-multiprocess-before-memory-20260908/`。
第二版释放 CSR 拼接前的副本和每折设计矩阵，并对单次使用的设计原地缩放、关闭
Ridge 的冗余输入复制。复制式/原地式残差一致性和输入不变性测试通过。
现按每个训练行约 1600 字节估算工作进程峰值，并预留 2 GiB 给主进程及系统；
这是针对当前三个数值控制变量的保守调度预算，并非硬内存限制。
完整 Python 测试为 108 项通过。上述 238.610 秒报告对应内存优化前的独立复算，
新批次端到端与阶段耗时以它实际落盘的 result.json 为准。

第二版完整框架已实际进入双进程发现：日志记录可用内存 10.9 GiB，
PID 4732 / 28076 分别处理 2,033,632 / 2,693,214 行训练样本。
首结构基础测量为 44.6 秒、安慰剂诊断约 4.2 秒；记录此状态时发现仍在计算，
因此尚未给出第二版首结构端到端加速倍数。运行源码哈希核对无变化，监督器与组合监听正常。