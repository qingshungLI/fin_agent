# RSI Alpha Research Harness

本项目的产品叙事已统一为 **Recursive Self-Improvement Alpha Research Harness**。RSI 指递归自我改进；金融技术指标只是可选 alpha seed。研究代理在冻结 evaluator、有限预算和审计账本内提出、编译、测量、批评、变异并记录下一代研究假设。详细协议见 [RSI_ALPHA_HARNESS.md](RSI_ALPHA_HARNESS.md)。

# 企业评委视角的产品化验收

当前系统已经分为两条路径：

1. **比赛研究控制台**：固定 A 段、DeepSeek 角色、自动演化、安慰剂、组合 gate 和真实回放，服务于本次比赛的可审计研究。
2. **自有数据研究工作室**：企业用户上传自己的日频面板，自定义研究格子，选择 fast/balanced 档位，启动隔离任务，查看训练/验证结果、成本压力、市场暴露基准和候选相似度，导出因子及冻结证据。

## 评委最关心的四个问题

### 企业能不能直接使用

可以使用 `#studio` 进入工作室，或通过本机 API/SDK：

- 数据输入只支持 CSV/Parquet，先校验日期、证券、开收盘、重复主键、缺失和无限值。
- 服务端不接受用户路径和 Python 代码；上传文件放入独立 `artifacts/studio/datasets/`，研究目录必须新建。
- 格子由用户在前端编辑，也可以构造 `CellSpec`/`ExperimentSpec` 通过 Python 调用。
- 使用受限 DSL，不能执行任意 Python；表达式会在冻结前验证字段、窗口、量纲和未来字段防火墙。
- SDK 以 wheel 形式构建并做了项目外安装冒烟运行，便于企业接入自己的调度系统。

### 因子质量怎么判断

结果页不把正收益直接标成优秀。每个候选同时展示：

- 训练段 IC 与验证段 IC；验证段再拆为三个连续子区间。
- 描述性块重采样区间，并明确 fast/balanced 不是正式确认。
- 固定单边成本与双倍成本压力下的净收益。
- 同暴露等权可用证券基准，排除把市场普涨误当成选股能力。
- 训练期与已登记候选的信号相似度，提示重复因子和增量价值风险。
- 覆盖率、最大回撤、交易权重转移、持仓收益缺失和完整净值状态。

只有 IC 区间排除零、子区间方向一致、基准超额和双倍成本均有支持时，才显示“待独立复验”；否则是“需继续研究”。这仍然不是正式 PASS，正式确认还需要独立市场区间、成本口径、数据质量和多重搜索处理。

### 速度如何解释

系统提供两种档位：

- `fast`：100 次块重采样，优先得到候选、演化和成本结果。
- `balanced`：500 次块重采样，区间更稳定。

比赛全量 fast 实跑中，前三个结构分别约 546.7、665.0、548.7 秒；第一结构的横截面安慰剂从完整配置约 101 秒降到约 4 秒，主要剩余耗时转移到条件发现。企业工作室只做受限 DSL、训练选择和一代可复现演化，266,513 行、1,000 日、269 只证券、7 个候选的真实行情验收约 3.83 秒。两个数字属于不同工作负载，不能混为一项性能承诺。

### AI 能不能使用

可以，但 AI 只负责提出机制格子和编排任务；SDK 不自动把企业数据发送给模型。项目提供：

- `research_sdk` Python API。
- `python -m research_sdk` 命令行。
- `/api/studio/datasets`、`/api/studio/experiments` 和白名单产物下载接口。
- `skills/factor-research/SKILL.md`，规定数据契约、调用顺序、结果解释和禁止事项。

DeepSeek 仍然用于比赛研究中的 proposer、operationalizer、reviewer、bettor、reconciler 和 inducer 角色；企业工作室默认不调用 LLM，避免用户误以为数据会自动外传。企业需要 AI 提案时，可由外部编排器先生成 JSON，再提交冻结的 `ExperimentSpec`。

## 现场演示顺序

1. 进入 `#studio`，上传一份 CSV，展示字段映射和错误拒绝。
2. 选择数据资产，添加/删除格子，修改机制、DSL 表达式和预测周期。
3. 选择 fast，开启一代演化，冻结并启动任务。
4. 展示候选表中的父结构、训练/验证 IC、同暴露超额、双倍成本和质量状态。
5. 点开候选，展示验证净值、三个验证子段、相似信号和导出因子。
6. 进入 AI/开发者页，展示 SDK、CLI 和 HTTP 接口。
7. 回到比赛控制台，展示 DeepSeek、自动演化、多结构 gate 和正式资格仍受审计约束。

## 还需要持续建设的企业能力

- 加入企业自定义停牌、交易成本和行业映射适配器，并把数据授权和 PIT 证明做成导入证书。
- 对企业面板增加任务队列、取消/重试、资源配额、租户隔离和对象存储。
- 增加滚动样本外、不同市场/行业的复制实验；把当前轻量 SDK 的探索状态与正式确认管线连接起来。
- 用真实交易适配器完成整手、涨跌停、停牌、公司行为和库存漂移回放。
- 将 SDK 发布到私有包仓库，并提供 Docker/CLI 部署说明。