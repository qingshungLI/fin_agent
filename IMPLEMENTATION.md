# AutoAlpha 实现与验收台账

以 `PSEUDOCODE.md` v5 的模块契约及其后置修订为准；比赛材料见 `demand.md`。

## 口径裁决

- M5.6/M5.7、M6.1/M6.2 与 README 的修订优先于早期流程图：A 段不能授予 PASS，正式确认使用冻结批次与 Holm；e 轨迹仅供实验研究。
- 表达式已经 `neg` 时，方向断言必须作用于原始信号或明示的标准化方向，禁止二次翻转。结构同时保存原始信号与交易方向。
- 样本量来自每日 IC 的时间块 bootstrap，不套用名义行数；不同窗口的有效样本量不能在未验证独立性时直接相加。
- 相位随机化仅保留功率谱，不能声称同时保留边际分布；采用 IAAFT 并报告谱误差及缺失处理。
- 估计 p0 的上界只对命中分支保守，失败分支需要下界。未测试断言不应通过事后重归一化权重扩大证据。
- PIT 精确行业表实际为三级代码。一级映射必须有历史有效映射依据；无法验证时明确使用精确三级行业，不以当前一级映射冒充历史真值。
- 数据隔离分为应用访问协议和操作系统权限；本地同账户工具不能声称抵御具有文件写权限的攻击者。哈希链需外部锚点才可抵御整链重写。
- 复权验证比较除权样本与官方校准收益/参考昨收，不把正常市场波动误报为复权错误。

## 交付模块

| 契约 | 内容 | 状态 |
| --- | --- | --- |
| M1 | 数据质量、PIT、交易日对齐、A/B/H 隔离、功效 | 部分实现；真实行业重叠阻断，未完成独立目录隔离 |
| M2–M3 | 注册、量纲 DSL、切点、70 格地图、断言冻结 | 部分实现；缺非线性混淆验收、词表合并和完整维度分派 |
| M4–M6 | 测量、四刀、Holm、实验性证据与校准 | 核心函数已有；全批次确认与 Monte Carlo 校准未验收 |
| M7 | 块交叉拟合、均值/方差分流、honest 森林、演化 | 函数已有；尚未接入完整主循环并做统计模拟验收 |
| M8–M9 | 符号图、层次记忆、可辨识性及探针 | 图基础测试通过；层次模型未实际采样验收 |
| M10 | 路由、持仓路径、成交边界、成本情景 | 基础现金账本通过测试；公司行为严格阻断，RQAlpha 接入未完成 |
| M11–M13 | 岗位防火墙、规律账本、持久任务编排 | 账本和单结构 CLI 已有；LLM 岗位、持久编排未完成 |
| 产品 | Python API、TS 工作台、报告与参赛材料 | API/工作台可打开；交互任务提交、完整报告与材料未完成 |
| 验收 | 单元测试、统计模拟、真实数据运行、浏览器验收 | 7 项基础测试通过，前端构建通过，桌面/移动截图完成；统计模拟未完成 |

本表随实现和实际验收结果更新；未通过验收不记为已完成。

## 当前运行

工作台：`http://127.0.0.1:5173`；API：`http://127.0.0.1:8000/docs`。

```powershell
python -m uvicorn engine.api:app --host 127.0.0.1 --port 8000
npm run dev
python -m pytest -q
npm run build
python run_engine.py --data-root data --output-root artifacts --start 2018-01-01 --end 2018-12-31 --max-symbols 120 --engineering
```

真实数据运行退出非零：`002163.XSHE` 精确行业有效区间重叠。
先前另外确认 `000789.XSHE` 的 2014–2021 年同时存在 `850612.INDX` 和 `857111.INDX`。
错误证据保存在 `artifacts/failure.json`。未修改源数据、未静默剔除证券，当前没有正式研究结论。

截图：`artifacts/workbench-desktop.png`、`artifacts/workbench-mobile.png`。
基础测试不能替代伪代码要求的统计模拟和真实全流程验收。
