# AutoAlpha 实现与验收台账

依据 README 与 PSEUDOCODE v5 的后置修订：A 只探索，B 为一次性冻结批次 Holm 确认，H 为后续封存检验。实验性 e 轨迹不授予 PASS。工程验收、统计有效性、真实因子资格分别记录。

## 当前实现

| 模块 | 已接通 | 仍然限制 |
|---|---|---|
| M1 数据 | LFS 数据完整性核验、PIT 校验、严格/隔离模式、内容寻址缓存、A/B/H 日期边界 | 原始行业重叠及竞价时间异常未获供应商修订；无操作系统级隔离 |
| M2–M3 登记 | 固定坐标标签、受控 DSL、三种同向表达式、独立旁证、冻结切点、历史截断与尺度检查 | 完整非线性混淆验收、全七形式专属生成器未完成 |
| M4–M6 判别 | 测量、有限时间块不确定性、三类安慰剂并行、断言、增量、冻结批次确认接口、一次性消费记录 | 综合统计认证未完成；实验财富缺逐断言 p0 校准，明确不出虚构轨迹 |
| M7 发现 | 安慰剂通过后均值/方差分流、可选 honest 森林与重复切分、演化建议 | 演化仅记录建议，未自动入队生成下一代；全股票在线行业批次已触发发现路径，完整结果以批次产物为准 |
| M8–M9 记忆 | 一致性视图、可选四链层次采样、先验重叠/收敛闸门、后验缺口 | 跨结构测量协方差和完整恢复模拟未完成；无可用档案时返回不可辨识 |
| M10 执行 | 本地 RQAlpha Mod、A-only DuckDB、实际分红与送转股执行测试、缺失状态严格报错 | 无 B-PASS 结构，不启动正式策略组合；当前收益报告是固定成本研究组，不是可成交净值 |
| M11–M13 编排 | DeepSeek 真实调用、岗位字段白名单、响应缓存、有限重试、盲押、确定性对齐、审计冻结、断点恢复、单写者锁 | 五次盲押来自同一模型，不是五个独立模型；Platt 数据不足；归纳岗位尚未自动生成新规律 |
| 工作台 | 研究列表/测量、5 秒刷新、服务器任务提交与日志、桌面/移动布局 | 未制作比赛提交包，未提交比赛 |

## 验收证据

所有证据在服务器 artifacts/validation 或对应批次目录；本地不保存项目副本、缓存或测试输出。

- 27 项单元/契约测试通过，前端构建通过；桌面/移动浏览器、真实任务提交、服务异常恢复 3 类验收通过。测试涵盖时间边界、行业隔离、DSL、审计篡改、单写者、盲押隔离、安慰剂多进程确定性、现金路径、任务接口和缓存损坏。
- 行业均值同输入数值等价，1000×600 基准 0.998 秒降至 0.108 秒，约 9.26 倍。
- 600 股票 × 1458 个 A 段交易日，四个基线结构完成完整检验规模；每个结构约 57–59 秒，均未通过全部安慰剂。
- 相关 AR(1) 八候选 Holm 零假设模拟初版错误率 8.5%，据此停止旧批次并修正标准误/有限块推断；独立新种子 200 次模拟错误率 2%，ICM 零假设拒绝率 4%。只覆盖这些合成情景，不是完整统计认证。
- 强交互模拟中，500 棵 honest 树均有效，真实预设调节变量入选频率 100%，ICM p=0.000999；仅验证强信号路径，不代表完整发现恢复认证。
- 分红实测：2018-07-12，1000 股现金增加 136 元，与每 10 股 1.36 元一致。
- 送转股实测：2017-02-17，100 股变 150 股。历史缺失竞价报价保留 NaN，缺失停牌/ST 状态不填 False。
- 贝叶斯初次采样出现 12 次发散，改用零和参数化和更保守步长后，四链 1000 次保留采样的模拟场景为零发散、R̂=1.00、最低 ESS=932；仅能解释为该场景运行与收敛验证。
- DeepSeek 生成的竞价不平衡结构完整完成冻结、盲押、测量与判别：IC=0.00502，安慰剂未通过。工作台任务完成并落盘，缓存命中；重复运行同一结构不计为新候选。

## 数据与因子边界

600 股票探索面板中，原本合资格股票日期的 12.274% 因行业未知/冲突被明确隔离。例：002163.XSHE 的同一时间段存在 850611 与 857121 两个代码；不能凭结果选择其中一个。09:24–09:26 窗口外的竞价字段也被屏蔽。

现有基线没有合格因子。正 IC 不等于安慰剂通过、扣费可盈利或独立确认通过。B/H 仍未用于搜索，正式确认和组合保持阻断。

## 后续必要工作

1. 获取行业历史分类版本/修订规则及竞价字段口径，恢复严格数据模式。
2. 补全七形式生成、自动演化、归纳与探针闭环，并完成非线性零假设、功效、层次恢复模拟。
3. 在固定预算下完成全股票 A 段候选验证；达标后才冻结 B 批次，再执行 H 与正式策略回测。
4. 独立确认和执行验证完成后，再整理比赛材料。

运行命令和服务入口见 SERVER_RUNBOOK.md。本台账明确保留未完成项，不宣称 PSEUDOCODE 全部验收完成。

## 最终代码复核批次

verified-runtime-01 完成 4 个 DeepSeek 结构，600 股票 × 1458 个 A 段交易日，
完整计算规模、8 个安慰剂工作进程，总耗时 320.57 秒；行情缓存读取验证 5.95 秒。
4 个结构 IC 依次为 0.00502、0.01032、-0.01366、-0.04380，全部安慰剂未通过，
正式候选数为 0。相同身份的完成态恢复成功，未重复测量、未新增模型请求。
最终摘要在 artifacts/validation/final-summary.json，源码快照仅保存在服务器同目录。

模型通过语法/登记与模型复核，仍不等于经济机制已被证实；例如换手强度本身并不
直接提供价格冲击方向，模型描述的数值窗口也仍需要更强的结构化约束。
这也是不能把本批次候选当成可参赛因子的原因之一。

## 行业来源补充核对

用户确认供应商为 RQData、rqdatac==3.5.6.1、source=sws、level=3。
服务器确认读到 431 文件、11,371 区间、5,517 股票，当前使用精确表和半开区间。
A 段 43 股票/67 对代码重叠可由诊断性新旧代码裁剪消除，但月度快照对照仍有
26,433 条无精确区间及 1,835 条唯一代码不一致。尚未将诊断规则用于生产，
不能仅通过去重或切换日裁剪宣布恢复严格模式。
完整反例和哈希在 artifacts/validation/sws-source-audit/README.md。

## RQData 在线来源复核与替换候选

已使用用户授权在服务器认证，客户端固定 3.5.6.1。
五个日期、八只反例股票与十个行业代码的在线核对已完成。
当前接口仍存在部分历史区间缺口，若干旧代码直接返回空；不能仅覆盖原区间文件。
已新增逐交易日直接行业源，完整保存 A 段 1458 日期、5,479,320 行，原始 Parquet 未改动。
全股票 5562 × 1458 面板实测：交易池内行业缺口 0，行业严格校验通过。
仍有 116,674 条竞价时间异常被明确隔离，故尚未宣称全部数据/正式因子通过。
新增来源、无前填后填、重复键拒绝及来源身份缓存隔离验证后，28 项测试通过。

## 全股票运行性能复核

2026-09-07：30 项测试通过。每日 Pearson IC 的 5562 股票实测约提速 3.54 倍，
秩 IC 约提速 1.34 倍；最大数值差异 1.36e-15。
稀疏行业控制变量交叉拟合与原稠密实现对照，残差最大差异 2.52e-10；
模拟基准约提速 2.74 倍。500 棵浅森林全部保留，缓存相同树结构的叶子统计后
模拟基准约提速 7.98 倍，变量频率和叶子估计通过等价检查。
这不是全链路提速倍数，且只覆盖所列数值对照情景。
更换稀疏求解器后的 200 组统计模拟：Holm 错误率 2%、ICM 拒绝率 4%，
完整统计认证状态仍保持未通过。

旧 rqdata-full-A-01 已主动中止并保留失败状态及审计；
新 rqdata-full-A-02 使用优化实现、全部 5562 股票、1000 次 bootstrap、
500 次安慰剂、500 棵树和 20 次重复切分；日志位于 artifacts/jobs/。
结构耗时字段现包含发现检验，并分别记录发现耗时。
