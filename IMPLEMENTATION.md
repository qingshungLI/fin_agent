# AutoAlpha 实现与验收台账

研究、独立确认、可执行策略与比赛提交分开验收。A 段只用于探索；B 为一次性冻结批次 Holm 确认，H 保持封存。工程测试或正 IC 均不授予正式 PASS。

## 当前能力

| 模块 | 已接通 | 验收边界 |
|---|---|---|
| 数据 | 原始 LFS 完整性、严格行业连接、逐日 RQData 行业源、独立竞价策略、内容寻址缓存、固定 A/B/H 日期边界 | 当前供应商对历史有效日的响应不是当年发布版本存档；竞价源仍有时间异常 |
| 事前登记 | 七种形式各自的构造约束、固定语义标签、受控 DSL、三个同向表达式、独立旁证、冻结切点、尺度与历史截断检查 | 模型逻辑审核不能证明经济机制；不可用字段和事件必须拒绝 |
| 判别 | 有限时间块不确定性、三类安慰剂、必要断言、增量、冻结批次联合假设 Holm、一次性 B/H 消费记录 | 完整统计确认闸门仍未通过；事件/条件的连续安慰剂样本不足时保留未测试 |
| 发现与演化 | 非线性 nuisance 交叉拟合、500 棵 honest 树、重复切分、按证据入队、深度与请求预算、全新 ID/冻结/盲押 | 发现结果不是因果证明；未知结果不会被当成反向证据 |
| 研究记忆 | 季度档案、相同信号去重、共同日期块测量协方差、四链分阶段模型、收敛与先验重叠诊断、下一探针 | 季度之间的测量相关性未估计；阶段 B/C 恢复验证只覆盖预设模拟；不授予正式 PASS |
| 规律与模型 | 明确区分 A 内支持、证伪和未决，带证据引用的归纳建议、盲押可靠性报告 | 规律均未确认；缺少可独立校准的逐断言 p0，不生成虚构财富轨迹 |
| 交易 | RQAlpha 本地数据源、真实分红/送转账本、A 内研究目标到 T+1 开盘执行、成交失败诊断 | 无 B-PASS 结构；研究目标回放不等于正式组合，部分成交即作废 |
| 工作台 | 研究任务、全股票选项、日线/竞价范围、条件发现与记忆开关、测量和审计展示 | 未制作或提交比赛包 |

## 已完成验证

原始数据、模型响应、日志和验证产物不纳入版本控制。

- 53 项工程测试通过，前端构建通过。新增覆盖七形式、历史不变性、拒绝提案恢复、联合协方差、条件旁证使用未加门父信号、必要断言的多重校正。
- 9 个真实 DeepSeek 事前提案通过审核，覆盖全部七种形式；同一组提案通过全股票 A 特征上的尺度、历史截断和方向登记。该验证未加载收益标签。
- 非线性交互检验完成每场景 600 次、总计 3,000 次模拟。线性、二次、平滑非线性及异方差零交互场景的拒绝率为 6.33%、5.67%、5.17%、5.67%；各自单侧 95% 上界低于预设 9% 容差。强交互检出率 100%。这不是任意数据生成过程的认证。
- 800 组八候选批次模拟检验“主效应真、一个必要断言假”：符号、剂量形状、峰值、旁证的错误通过率分别为 2.5%、3.5%、0%、2.5%。另 200 组相关 AR(1) 主检验批次错误率为 2%，200 次线性场景 ICM 拒绝率为 3%。这些报告保留对应推断源码哈希。
- 500 棵森林的单线程/8 工作线程输出完全相同；强交互模拟中变量频率为 100%，ICM p=0.000999。测试只覆盖该已知强交互。
- 极小 Amihud 序列曾触发滚动方差累积误差。改为局部缩放后逐窗口计算，保留原尺度门槛；真实全股票输入复核通过。
- 固定协方差的预白化与原多元正态似然在数值上等价；采样规模不变。阶段 B 的三组固定联合协方差模拟均恢复全部 7 个已知族/形式效应，R̂=1.00、最低 ESS=1,080、零发散。第一组同规模采样由 583 秒降至 425 秒；只代表该场景。
- 真实分红现金增加 136 元、100 股送转为 150 股的执行适配器验证已完成。新的 T+1 目标适配器受控测试成交成功；真实基线投影遭遇六只股票的竞价成交量限制，已作废并保留未成交明细。

## 已有市场证据与剩余闸门

九份事前冻结的真实模型提案已完成 A 段评估，覆盖 5,562 股票和 1,458 个交易日。五份未通过安慰剂，两份条件/事件提案因完整连续子面板不足而未测试；另外两份通过安慰剂，但被必要断言否定。安慰剂使用预先固定的最多 128 只完整证券连续子面板，不能替代全股票总体检验。

这组固定提案评估缺少盲下注和自动演化，因此仅作为研究证据。正式合格因子仍为 0，B/H 未读取，没有比赛提交。

交互阶段 C 原有设计重复编码了主效应与组合效应，导致 24 列只有 19 秩。改为观测组合上的正交交互对比后，不再重复主效应，也不会填补不存在的组合。缺失组合、断开的析因设计和行重排测试通过；两个固定种子的平衡交互模拟均恢复全部预设非零效应，R̂=1.00、最低 ESS 分别为 581 和 839，均无发散。结果只覆盖所列合成场景，不能证明任意市场设计可识别。

模型默认统一使用 Flash，生成和审核保留思考模式。批次恢复身份包含不含凭据的模型设置；密钥轮换不改变研究身份，模型变更则要求新批次，避免混用生成条件。已有运行使用其原冻结版本，不在运行中替换引擎。

下一步须完成真实模型的整个研究循环并补齐统计确认资格。只有完整达标的候选才能进入冻结 B 批次，再做 H 与可执行策略验证。不可识别、安慰剂样本不足和无合格候选均保留为结果，不降低标准。

原始数据、模型响应、部署说明及执行日志不纳入代码仓库。
