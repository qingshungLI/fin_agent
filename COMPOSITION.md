# 多结构组合研究

组合层位于 `composition/`。它读取已提交、哈希校验通过的源结构，在读取组合收益前冻结成分、规则和代码版本，不占用源研究锁、不修改源检查点。正在运行的单结构研究继续由原引擎和 DeepSeek 推进。

当前实现三种固定规则：

| 规则 | 组合行为 |
|---|---|
| parallel | 原覆盖区域内，按各结构冻结周期展开持仓路径，等资本合并 |
| conflict_cash | 在上述路径上，对出现相反方向的证券关闭 gate |
| consensus | 至少两个不同定义的结构同向且无反向票时参与 |

相同冻结定义且信号一致的结构合并为一票。不同定义间的相关性、覆盖交集、方向冲突写入诊断矩阵，不根据事后收益优化权重。缺失覆盖不是反对票；当前 gate 关闭时不继续保留旧持仓路径。组合随后执行多头投影、最多 50 只、单股不超过 5%、至少 5% 现金约束。关闭 gate 释放的风险预算可以留在现金。

## 可复现入口

在项目根目录使用研究环境，批次 ID 必须是新的，避免覆盖冻结结果：

```powershell
.venv-research/Scripts/python.exe -u -m composition.runner --source-run fullcycle-20260908 --batch-id multi-next --max-sources 6 --execute
```

`max-sources` 按完成顺序选择已提交结构，不按 IC/收益择优。源研究尚未完成不影响组合快照计算。三种规则使用同一批成分和完整 A 段可用信号日期；末尾两个实际交易日保留给最后信号执行和清仓。当前研究日期共有 1458 天，组合信号为 1456 天。

执行适配器修复后可重放旧的冻结目标，历史失败报告保留：

```powershell
.venv-research/Scripts/python.exe -u -m composition.replay --batch-id multi6-20260908 --execution-id multi6-execution-next
```

组合目录包含 `frozen.json`、`artifact-hashes.json`、`report.json`；每套规则包含目标持仓、原始有符号路径、门控计数、结构贡献、关系诊断、研究收益与净额合并成本对比。重放结果位于 `artifacts/composition-executions/`。控制台“组合门控”页按源批次匹配最新回放，旧成功状态不会覆盖新失败或等待状态。

## 2026-09-08 实际运行结果

批次 `multi6-20260908` 使用最先完成的 6 个源结构，已生成三套完整组合研究产物。以下为调整开盘价加固定权重转移成本的研究估计，不是实际成交净值，亦不是年化收益。

| 规则 | 平均多头暴露 | 日均成本代理 | A 段累计净收益估计 |
|---|---:|---:|---:|
| parallel | 24.69% | 3.714 bp | -44.86% |
| conflict_cash | 3.33% | 0.618 bp | -6.64% |
| consensus | 3.33% | 0.618 bp | -6.64% |

门控结果的风险暴露明显较低，不能将亏损减少解释为已经获得超额收益。这里的成本使用目标权重转移代理，真实执行另行处理整手、成交量、停牌、涨跌停、费率与公司行为。

RQAlpha 第二次完整回放启动成功，但三套目标均在 2016-07-04 的 601515.XSHG 被原始竞价数据阻塞：竞价 open=0、volume=0、last=11.30（等于昨收），日线 open=11.17。不能用昨收或事后日线开盘价静默替换竞价成交。错误保存在 `multi6-execution-v2/report.json`，组合研究产物仍然有效保存，但不能声称实际交易回放通过。

组合规则尚未取得独立统计确认，所有产物 `formal=false`。源结构的探索性结果不能代替组合资格；B/H 不由本入口读取。

## 验证

Python 全套测试：94 passed。控制台浏览器测试：5 passed（含真实组合 API、三种规则、冻结下载、最新失败展示、移动端无溢出）。`npm run build` 成功。截图：`artifacts/validation/control-compositions.png`。