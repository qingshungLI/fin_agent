## 数据
主数据：Tushare 新闻（2018 起，已入库）+ 互动易/公告（巨潮公开）
量价：你自己的行情数据
框架不动：cell.py 里换掉文本层的 Cell，gate 层的量价部分（pre_return / vol / turnover / 涨跌停 / 发布时段）一行都不用改——这恰好是卖点：harness 与数据解耦。

顺便这给了你一个绝佳的叙事：同一套 harness，我们跑过 A 股文本事件面板和 Binance 永续 5m 面板两个完全异构的市场。这证明它是研究方法而不是一个因子脚本。评委见过一百个因子挖掘器，没见过跑两个市场的。


## 定位
"让 AI 的因子挖掘变得可信"——一个带预算约束、可证伪、可审计的自动研究智能体。

XGBoost 让数据选结构，GA 让 fitness 选结构，我们让逻辑选结构、数据只有否决权。
α 预算：把"能做多少次检验"变成一个像科研经费一样的可计算余额，跑完就停。这是对多重检验问题的工程化回答。
三态判定：PASS / FAIL / UNDECIDABLE。系统会明确告诉你"你的数据不足以判定这个假设"——这是别人的系统永远不会说的话，而它恰恰是最诚实的输出。

逻辑选择结构，数据只有否决权，没有提名权
## demo杀手锏

Demo 的杀手锏（一定要做这个对照实验）

同一份数据、同一个 holdout，并排跑两条线：

	遗传规划基线	AutoAlpha Harness
产出	100 个因子，样本内 IC 排行榜漂亮	3 个结构（1 PASS / 1 FAIL / 1 UNDECIDABLE）
holdout	崩掉 / 归零	PASS 那个还活着
附加产出	无	law.md 若干条被证伪的机制



有什么更好的架构吗比如当前我们使用的是因果森林做进化 相当于是优化异质性这个指标

GP中的复杂重组机制其实比较类似我们当前的框架 但是我们比GP高的就是有解释性 ——其中异质性这个指标就可以称之为是可解释性 也就是说能够防止过拟合，所以我们前面组合的技术需要学习一下GP 不然就没办法凸显 可解释性的优势

需要有很好的优化目标：除了预测精度之外 我们还需要稳定性 HACt  ：也就是一个比较完整的多目标适应度 
> 这里我们需要改善一下当前的优化方法以及因子的评价体系 我觉得这是一个创新点 比如对不同的结果做归因之后 做一些regime的识别 反正就是 要让之前计算过的例子参与到后面的进程中来，或者说当前使用训练集和测试集来做验证的方法并不合理 ，我们应该重新定义这样的优化问题。能够找到为什么这个因子不好？以及系统性的误差在哪里的时候，或许我们就成功了。
可以从其他学科中寻找灵感 ，比如llm等等
比如说当前经常使用训练集和验证机 测试集中间还要做purge embargo这样的操作

GP 在金融数据中非常容易“成功得不真实”。因为它会搜索海量公式、窗口和非线性组合；即使所有原始变量都没有预测能力，也可能在样本内挖出 IC 很好看的表达式。

以及在因子中 最后选择好因子很多都是设置一些硬门槛 但是得到每一个因子都是消耗了一定的算力的 所以我们每一个因子都需要得到一定的结论 bias在我们后面的研究中 以及已经挖出的逻辑后面可以做进化以及不要重复的因子 这中间大模型起到了可解释性的作用



别人做的是"让 AI 更快地挖因子"。我们做的是"让 AI 的因子挖掘变得可信"——一个带预算约束、可证伪、可审计的自动研究智能体。
autoresearch-harness'


异质性不是可解释性，而且异质性目标本身是过拟合倾向最强的那一类目标
真正在你框架里阻止过拟合的，从来不是异质性目标，是这四样：
	切点冻结（搜索空间是离散且事前的）
	causal_statement 前置（假设空间是事前的）
	honest split + depth ≤ 2（估计与选择分离）
	depth=2 只由失败画像触发（提案来自逻辑，不来自数据）

所以你不是"比 GP 多了可解释性"，你是用可解释性换掉了 GP 的表达能力，但换得太多了——换掉的不只是它的选择机制（该换），还有它的表示与重组机制（不该换）。这正是你自己那句"借表示，别借选择"没有兑现的部分。


你说"训练集测试集这套方法并不合理"——对，而且理由比"purge/embargo 麻烦"更根本：样本内外切分是对 out-of-sample 的一次模拟，而模拟是可以被反复重跑的。 你切五个种子、看五次结果、挑一个，切分就已经失效了。purge/embargo 防的是数据泄漏，防不了研究者跨实验的泄漏。

### 新的数据替代方案
替代方案是 prequential（预测序贯）评估 / MDL：

结构卡冻结时，必须发出一个带不确定性的前向预测——不是"IC>0"，是下一期 IC 的完整预测分布
时间推进，用严格适当评分规则（log score / CRPS）给这个预测打分
研究纲领的质量 = 累计对数得分，即"你的假设体系对未来数据的压缩能力"




### 检测维度
正确的做法是让每个维度对应一种独立的失效方式：

维度	独立性来源	与效应量的相关
净效应（net 口径）	—	—
形式稳健性（分层 vs 连续、窗口 ±50%）	与效应量近正交	低
regime 一致性	时间维度	中
集中度（单名/单月）	截面维度	低
n_eff / 可判定性	统计预算	独立
对现有结构库的新颖度	与效应量正交	负相关最好
推导长度（从预注册根节点起的产生式数）	复杂度先验	独立

最后两项是新的，也是最重要的。推导长度是文法版的奥卡姆剃刀，直接对应 MDL 的模型编码长度——它让"简单结构优先"从审美变成记分项。新颖度接下来单说。


### LLM
提案器	在文法上生成候选推导	高召回、低精度正好——选择由冻结协议做，不由它做	提案时不给它看任何收益数据
推导检查器	判断子陈述能否从父陈述 + 产生式推出	这是蕴含判断，LLM 的强项	换一个模型做对抗复核，两个不一致→人工
跨域类比检索	读 law.md，指出"A 族里失败的这条机制，和 B 族里活着的这条同构"	层次模型做不到这件事——它只认你事先定义的族；LLM 能提出新的分族方式	提出的新分族必须走完整套检验才算数


公式层(从方向到具体的alpha)以及表述层(非常可解释性的一些表达式和表述做成像GAN一样的网络 二者之间互相博弈 然后同时每一个网络都维护一个贝叶斯 同时表述层不一定要用自然语言 可以用一些结构化的语言 
最后这些alpha可以用KAN组合样条函数的形式组合起来 类似于可以有不同的regime 基于regime去做对应的alpha预测这样子 的东西 相当于两个内部循环网络互相对抗融合 (也可以用alphafold之类的single pre和pair_pre那样的东西 ) 再结合一些SGTP以及你说的prequential（预测序贯）评估 / MDL：




## 接受这个批评

计分那套是仪表盘，不是产品。你要的是**规律**和**因子**，我给了你一套怎么给规律打分的机器。本末倒置了。这一轮只谈架构和产出。

---

## 一、你说的三个输出，其实是同一个东西的三个视图

这是我认为真正让架构变美的那一步：

| 你要的输出 | 它在那个对象上是什么 |
|---|---|
| **能遍历的 idea 空间** | 地图上的**全部格子**（含未探索的空白） |
| **好因子** | 地图上**变绿的格子** |
| **law.md** | 地图**被上色的过程** |

所以不需要三套机制，只需要**一张会长大的地图**。

架构从此只有三个部件、一个闭环：

```
        ┌─────────────────────────────────────┐
        │        机制地图 (MAP)                │
        │  可枚举的全部 idea，事前存在，不碰数据 │
        └──────────────┬──────────────────────┘
                       │ 指定下一个探测点
                       ▼
        ┌─────────────────────────────────────┐
        │        检验探针 (PROBE)              │
        │  十项向量，全系统唯一碰 y 的部件      │
        └──────────────┬──────────────────────┘
                       │ 返回三态 + 失败画像
                       ▼
        ┌─────────────────────────────────────┐
        │        规律账本 (LAW)                │
        │  law.md，记数据的 know-how           │
        └──────────────┬──────────────────────┘
                       │ 重新给地图上色、开新格
                       └──────────► 回到 MAP
```

**三个产物，一条环。** 这个图放路演第一页，评委三秒看懂，而且它是真的，不是画给人看的。

---

## 二、地图：idea 空间是一张矩阵，可以真的枚举完

关键设计：**idea 的坐标是二维的——「为什么会有 alpha」× 「它以什么形式出现在数据里」。**

行是机制族（回答 why），列是表现形式（回答 how）：

|  | F1 水平 | F2 变化 | F3 时序相对 | F4 截面相对 | F5 文内相对 | F6 条件化 | F7 事件化 |
|---|---|---|---|---|---|---|---|
| **M1 信息传递** 含增量信息→价格调整 | | | | | | | |
| **M2 注意力** 制造关注→涌入→反转 | | | | | | | |
| **M3 一致预期** 多家同向=已 price in | | | | | | | |
| **M4 信源可信度** 声誉调节强度 | | | | | | | |
| **M5 状态依赖** 前期量价决定吸收方式 | — | — | — | — | — | | |
| **M6 时机路径** 发布时点决定进价速度 | | | | | | | |
| **M7 文内相对** 分析师自己的排序 | — | | | | | | |
| **M8 修正** 预期变化本身 | — | | | | | | |

**56 格，其中约 40 格有意义。** 这就是"所有组合出来的 idea"的全集——不是几万个表达式，是四十个格子。

几个性质，每一个都在解决你之前问过的问题：

- **枚举是免费的**。地图在跑任何数据之前就画完了，不碰 y，不花预算。"我们的 idea 空间有多大"从此是一个可以报出来的数。
- **空白格本身是产出**。M8×F7（sentiment 首次由负转正的事件研究）现在是空的——这个空白直接告诉你下一步做什么。
- **灰格也是产出**。M5 只在 F6/F7 有意义，因为状态依赖变量**本身不是信号，只能当调节变量**。这个判断是机制推出来的，不是试出来的。
- **深度 2 = 格子之间的连线**，而且只由失败画像触发，所以是线性增长不是组合爆炸。

**每个格子里放什么**：一条 `causal_statement` + 至少三个算子化表达式（收敛效度）+ 一个可证伪的形状预测。就这三样，不要再多。

---

## 三、账本：law.md 记的是数据的 know-how，不是因子的成绩

这是我之前写歪的地方——我的模板在记"哪个因子失败了"，你要的是"这个数据是什么脾气"。按**从数据到市场到执行**排四类：

**① 度量层：这个变量本身是什么东西**
> sentiment 有 68% 的观测堆在 [0.6, 0.8]，分位切在中间三层几乎没有区分度；novelty 与 quality 相关 0.71，本质是同一个量的两种归一化。

**② 传导层：信息怎么进价格**
> 盘后发布的研报效应在 T+1 开盘一次性释放；盘中发布的效应分散在 T+0 尾盘到 T+2，峰值在 T+1。**发布时段不是过滤器，是不同的传导路径。**

**③ 条件层：什么时候成立、什么时候反号**
> 覆盖突增的反转效应只存在于小市值；在沪深300成分里符号相反。2023H2 全库效应集体减弱——不是十个因子各自失效，是一个共同的时间效应。

**④ 边界层：成本、容量、拥挤**
> M2 族的效应集中在 T+1，双边成本吃掉约 60%；成本容忍度 14bp，超过就归零。

**这四类才是一个 PM 会真的付钱买的东西。** 它跟因子是解耦的——因子会被套利掉，"盘后研报在开盘一次性释放"不会。

每条 law 只要五个字段：

```yaml
law_id: L-0023
type: 传导层
claim: "盘后研报的效应在 T+1 开盘一次性释放，盘中发布的分散在 T+0~T+2"
evidence: [S-0007 的 CAR 分层, S-0011 的时段分组 IC]   # 哪些格子支持它
scope: "2021-2026 全样本；小市值上更强，300 成分内未验证"
reopen: "需要在 300 成分内单独跑一次才能声称全市场成立"
```

**`evidence` 指向格子、`scope` 说清边界、`reopen` 说清什么时候要重查。** 不需要更多字段。

---

## 四、层次贝叶斯的正确位置：它是账本→地图的那条边

我之前把它包装成一个评分器，那是错的。它的自然位置是**闭环上的反馈边**——把账本里一堆单点结果，变成"地图上哪个区域还值得挖"：

- 某一行（机制族）的多个格子都指向同一方向 → **这一行是真的**，展开它的深度 2
- 某一行的格子彼此矛盾 → 族划分错了，**这一行要拆**
- 所有行在同一段时间一起变弱 → 那不是因子问题，是**②③类 law**，直接进账本

它不给任何东西打分，它只回答一个问题：**下一个探针放哪。** 这样它就不再是额外的机器，而是这条闭环上本来就该有的那一步。

---

## 五、Demo 就是那张地图在你眼前被填满

路演的主视觉不要放 IC 曲线，放这张矩阵，然后**按时间顺序播放它被上色的过程**：

- 绿 = PASS（有结构卡）
- 红 = FAIL（有 law 条目）
- 灰 = UNDECIDABLE（n_eff 不足，可重开）
- 空白 = 还没测
- 格子之间的连线 = 深度 2 的进化路径

旁边一个滚动的 law.md。三十秒之内，评委会自己得出三个结论：**这个团队知道自己在找什么、知道自己找过哪里、也知道自己还没找哪里。** 其他所有队伍给的是一个 top-50 排行榜，说不出第二和第三条。

再配上你唯一独有的那句：**同一张地图我们在 A 股和 Binance 永续上各填过一遍。**

---

## 六、两天做什么

1. **把地图画出来**（两小时，最高优先）。八行七列，每格写一句 `causal_statement`，能填的填、该灰的灰。这一步不写代码、不碰数据，但它决定后面所有事。
2. **把已有格子填进去**（一小时）。你现在跑过的因子，各归其位。你大概率会发现它们挤在两三行里——**那个空白分布本身就是第一条 law**。
3. **挑三个格子跑通探针**，刻意覆盖三种颜色。建议 M8×F2（修正幅度，零参数、机制最强）、M2×F4（覆盖突增，大概率红）、M7×F5（文内相对，大概率灰但很新颖）。
4. **law.md 按四类重写**，把你现有的所有结论归类。

要我现在把这张 8×7 地图逐格写出来吗——每格一句机制陈述、三个候选表达式、一个可证伪的形状预测。写完你直接就能开跑，也直接就是路演那一页。

