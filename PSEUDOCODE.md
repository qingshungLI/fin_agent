# AutoAlpha Harness —— 实现规格 v5

> 2026-09-08 实现更新：原灰格已通过派生代理构造接入 70 格搜索；实际字段、时间对齐与研究解释范围见 README 开头的实现说明。原 F.3 灰格表保留为初始设计依据，不再作为运行时禁用列表。完整网格默认保留 70 格覆盖额度并增加 140 次演化额度；自动演化、条件发现与贝叶斯记忆同时开启。测量先提交、归纳可恢复，跨批次继承不计为独立重复。

> **这份文档的目标**：一个没有参与过设计讨论、也不熟悉量化研究的工程师，读完这份文件能够独立开始写代码，并且知道每一步为什么要这样做、做错了会怎样、以及怎么验证自己做对了。
>
> **阅读顺序**：第 0 部分（背景与概念）→ 第 1 部分（数据契约）→ 第 2 部分（模块规格）。
> 附录 F 是想法空间的完整定义，附录 H 是术语速查。
>
> **每个函数的描述格式**：
> `目的` 这个函数解决什么问题 ｜ `背景` 为什么需要它 ｜ `输入/输出` 接口
> `算法` 逐步骤 ｜ `数学` 必要的推导 ｜ `难点/解法` 已知的坑
> `验收` 怎么验证实现正确 ｜ `调用关系` 谁调它、它调谁

---

# 第 0 部分 · 背景与核心概念

## 0.0 数据范围（先确认这个）

| 项 | 值 |
|---|---|
| 数据位置 | `D:\finance_agent\data`，920 个 parquet 文件，约 1.51 GiB |
| 品种 | A 股，**日频量价** |
| 时间 | 2016-07-01 至 2026-07-30，共 2448 个交易日 |
| 截面 | 5562 只股票（含已退市），日均约 4300 只在池 |
| **没有** | 文本 / 研报 / 分钟频 / 财务报表科目 / 融资融券 / 龙虎榜 / 北向资金 |
| **稀缺资源** | `num_trades` 成交笔数、`open_auction` 五档盘口、行业归属精确有效期 |

稀缺资源为什么重要，见附录 F.7。

## 0.1 这个系统要解决什么问题

### 什么是因子

量化投资里，**因子**是一个函数：输入某个时刻某只股票的公开信息，输出一个分数。分数高代表未来收益可能高。找有效因子的过程叫**因子挖掘**。

衡量因子好坏的标准指标是 **IC**（Information Coefficient）：每天在所有股票的截面上，算「因子值」与「未来收益」的相关系数，再对天求平均。IC = 0.05 通常被认为不错。

### 现有方法的共同失败模式

自动化因子挖掘（遗传规划、LLM 生成表达式）都遵循同一个流程：生成大量候选公式 → 在历史数据上回测 → 挑出表现最好的。

问题在于：**搜索本身会制造假阳性。**

假设你有 10000 个完全没有预测能力的随机公式。在 5% 的显著性水平下，纯粹靠运气也会有大约 500 个"显著"。你挑出最好的那个，它的历史 IC 会很漂亮，但那个漂亮完全来自搜索，不来自市场。

更麻烦的是，报告出来的 t 值分母里**从来没有算进"我搜了 10000 次"这件事**。所以这类系统在论文里的数字总是很好看，上线就失效。

### 本系统换一个目标

> 不追求"找到更多因子"，而追求 **"对每一个结论，都知道它有多可信、在什么条件下成立、以及为什么"**。

主要产出因此不是因子排行榜，而是三样：

1. **规律账本 `law.md`** —— 关于这个数据集本身的知识。例如"某个效应在小市值上存在、在沪深300成分股内符号相反"。这类知识不会因为别人知道而失效。
2. **一批被明确证伪的机制假设** —— 知道什么不成立，与知道什么成立同样有价值，而且它是下一轮研究的输入。
3. **少量经过完整检验的可交易结构** —— 通常只有 1–3 个。

## 0.2 核心概念（按依赖顺序，后面的依赖前面的）

### 面板 panel
研究用的长表，一行 = 一个决策时刻 = `(股票, 交易日)`。列包括各种输入变量与未来收益。本项目的面板约 1049 万行。

### horizon (h)
预测的未来天数。`h=5` 表示预测未来 5 个交易日的累计收益。本项目用 `h ∈ {1,3,5,10}`。

### Cell（变量）
一个已注册的输入变量，例如 `ts_z(turnover_today, 60)`（换手率相对自身过去 60 天的标准分）。**系统只允许使用注册过的变量**，注册时要过五项检查（M2.1）。

### 机制地图 map
一张 M×F 矩阵：
- **行 M = 机制族**，回答"为什么会有超额收益"。本项目 10 行。
- **列 F = 表现形式**，回答"这个机制在日频量价数据里长什么样"。本项目 7 列。

**每个格子是一个待研究的想法**，70 格中约 52 格可行。完整定义见附录 F。

这样设计的意义：把"想法"从一个无限的表达式空间变成有限集合。于是"我们的想法空间有多大""已经探索了多少"都成为可以报出的数字，而未探索的空白格本身就是研究计划。

### 标签 labels
五个受控词表字段，描述一个机制：

| 槽位 | 含义 | 例子 |
|---|---|---|
| `agent` | 谁的行为产生了这个效应 | `retail`（散户）、`liquidity_provider`（流动性提供者） |
| `friction` | 他受什么约束，导致错价不能立刻消除 | `attention_bandwidth`、`inventory_risk` |
| `mispricing` | 产生什么形式的偏离 | `overreaction`、`underreaction` |
| `correction` | 谁在什么尺度上纠正它 | `attention_decay`、`inventory_unwind` |
| `form` | 在数据里以什么形式出现 | `F3_ts_relative` |

**标签的作用是构成统计模型的设计矩阵**（M9），让不同研究之间共享信息。它们必须来自受控词表，不能自由造词——否则每个取值只出现一次，模型学不到任何东西。

### 断言 assertion（P1..Pn）
编号的**可判真假命题**，写在跑数之前，冻结后不可修改。例如：

```
P1  剂量曲线单调（效应强度随信号强度单调变化）
P2  峰值 horizon 落在 [1,5] 天
P3  效应符号为负
P4  在流动性差的股票上效应更强          ← 旁证，不指向因子本身
P5  在竞价价差宽的股票上效应更强        ← 另一个独立旁证
```

**断言是被检验的对象。** 机制的价值不在于它读起来合理，而在于它能推出可能为假的预测。

标签与断言的分工：

| | 标签 labels | 断言 assertions |
|---|---|---|
| 作用 | 构成统计模型的设计矩阵 | 被检验的对象 |
| 取值空间 | **受控词表**，五个固定槽位 | **开放**，想加新预测就加一条 Pn |
| 可变性 | 会被词表归一化重写 | **冻结后永不可变** |
| 谁消费 | 层次模型、关系矩阵 | 四把刀、赌本、演化、交易参数 |

### 结构 structure
一份完整档案，是系统操作的**基本单位**：

```
坐标（地图上的位置）+ 标签 + 断言 P1..Pn + 三个具体表达式 + 血缘 + 冻结哈希
```

"三个表达式"是指同一个机制的三种不同写法（**收敛效度**）。如果三个都指向同一结论，那是机制；如果只有一个成立，那是表达式过拟合。

### n_eff（有效样本量）
面板有 1049 万行，但它们**远不是独立的**：
- 同一天不同股票的收益高度相关（同行业、同风格一起涨跌）
- `h=5` 的标签覆盖未来 5 天，连续 5 天的 IC 几乎是同一个信息看了五遍

真实的独立样本量大约 **2500–5000**，比名义值小三个数量级。这个数决定了什么算显著，因此是全系统最重要的单个数字。详见 M1.7。

### 三态判定
| 判定 | 含义 | 后续动作 |
|---|---|---|
| **PASS** | 证据充分 | 生成结构卡，交给交易层 |
| **FAIL** | 与断言矛盾 | **有信息的失败**，写进账本，触发逻辑修正 |
| **UNDECIDABLE** | 数据不足以判定 | **没信息的失败**，不修改任何逻辑，记下重开条件 |

第三态是本系统与所有同类系统的关键差别。把 FAIL 和 UNDECIDABLE 混在一起，你会把"没有证据"当成"有反面证据"，然后去改一个本来没问题的逻辑。

### e 值 / 赌本
用赌博语言度量证据积累。核心保证是**任何时刻停下来，I 类错误都受控**——所以你可以边跑边看，而这正是研究的真实工作方式。完整讲解见 M6。

### 探索/确认分样本
把数据切成两段：前段用来**搜索**候选调节变量，后段用来**检验**。搜索制造的假阳性由"丢掉前段"来支付，因此后段的检验可以用标准临界值。见 M7.1。

## 0.3 一次完整迭代（先读这个，再读函数）

```
①  选一个还没研究过的格子
    · 冷启动（前 4–8 轮）：按实验设计原则选，目标是让统计模型可辨识
    · 之后：综合空白格、先验预测、共享效应来选
      ↓
②  写下档案并冻结
    · 五个标签（从受控词表里选）
    · 若干条可证伪的断言 P1..Pn（至少 1 条形状 + 2 条旁证）
    · 三个具体表达式（同一机制的三种写法）
    · 算内容哈希（含上一份档案的哈希，形成链）、打时间戳
    · 此后断言永不可修改；要改就开新 ID
      ↓
③  盲下注
    · 一个"看不到任何数据"的 LLM 实例，对每条断言给一个概率
    · 落盘，时间戳必须早于任何测量
    · 这些概率既是可解释性的度量，也直接是第 ⑦ 步的下注比例
      ↓
④  判定力闸门
    · 算这个结构自己的 n_eff（受它的覆盖区域限制）
    · 不够 → 直接记 UNDECIDABLE，不花任何检验额度，跳到 ⑧
      ↓
⑤  测量（探索段 A）
    · IC/分组收益曲线、月度曲线、分层、贡献集中度、HAC t、
      方向命中率、事件研究、成本敏感性
    · 输出逐观测的影响函数分解 ψᵢ ——「每个观测把 IC 往哪边拉了多少」
    · 这一层不做任何判断，只负责看清楚
      ↓
⑥  判别：四把刀
    刀① 安慰剂  —— 这个效应是不是假的？（伪影/前视/巧合）
    刀② 断言    —— 效应是真的，但原因是不是你说的那个？
    刀③ 增量    —— 这个结构是不是早就被已有的覆盖了？
    刀④ 正交矩  —— 仅对搜索出来的结构，在确认段 B 上检验
      ↓
⑦  结算赌本
    · 每条断言：把第 ③ 步的下注概率与第 ⑥ 步的结果对账，算似然比
    · 赌本 W = 各条 e 值的合并
    · W ≥ 20 → PASS ； W ≤ 0.05 → FAIL ； 中间 → UNDECIDABLE
      ↓
⑧  写账本、更新记忆
    · law.md（四类规律）
    · 层次模型后验（跨研究的记忆载体）
    · 地图着色（用后验给未研究的格子上色）
    · 词表归一化
      ↓
⑨  发现下一步
    a. 先分流：失效点是「均值偏了」还是「只是噪声大」？——这一步不做会毁掉整个发现层
    b. 在探索段 A 上用因果森林找候选调节变量（只拿变量名）
    c. 【缝】在这里写子结构的机制与断言，此时确认段 B 尚未被读取
    d. 在确认段 B 上裁决：主效应 + 形状 + 旁证，三者同时检验
      ↓
⑩  一致性检查（符号图）
    · 新老结构能不能拼起来？
    · 拼不起来的地方 = 存在一个还没找到的调节变量 → 免费的新提案
      ↓
    回到 ①
```

## 0.4 四条贯穿全局的纪律

### 纪律一 · 顺序决定性质
同一句机制解释，写在看到收益数据**之前**是科学，写在**之后**是编故事。两者形式上一模一样，性质完全不同。

所以：机制必须冻结并哈希，之后只能被检验、不能被修改。要改就开新 ID。

### 纪律二 · 信息防火墙
负责"提出假设"的 LLM 岗位**绝对不能看到任何收益读数**。

具体地：`llm_proposer` 只能拿到候选变量的**名字**，不能拿到它的效应大小和方向。原因是——如果告诉它"这个变量在高区效应更强"，它写出来的断言必然是"效应在高区更强"，这条断言只是复述了观测，**没有任何证伪力**。只给名字，它必须从机制推出方向，断言才有可能是错的。而能不能错，是它有没有信息的唯一标准。

### 纪律三 · 免费的与花钱的分开
不接触未来收益（y）的操作是**免费的**，可以放开做：列想法、写断言、算覆盖重叠、构造效度检验。
接触 y 的操作**消耗证据额度**。

系统必须在代码层面能区分这两类。完整划分见附录 B。

### 纪律四 · 研究层与交易层单向
研究层给：条件区域、方向、有效 horizon、失效监控量、成本容忍度。
交易层做：入场触发、出场规则、仓位杠杆、拆单。

**研究层不给价位，交易层不改逻辑。** 这是防止交易层的调参回流污染研究层的唯一屏障。

## 0.5 为什么防火墙必须由结构强制，而不是靠提示词

2026 年一项跨八个科学领域、两万五千余次 AI agent 运行的评估发现：**证据在 68% 的轨迹中被忽略，由反驳驱动的信念修正只发生在 26% 的情况下**，而且基础模型本身（而非 agent 框架）解释了 41.4% 的行为方差。

结论很直接：**不能指望 LLM 在被证伪时自己修正信念。**

所以本系统里，"不许偷看"不是一条提示词，而是四样工程措施：

| 措施 | 防什么 |
|---|---|
| 确认段独立目录 + `audit_read()` 审计锁 | 在写机制之前偷看确认样本 |
| 内容哈希链 | 事后修改历史档案 |
| 白名单构造上下文 | 提案器意外看到收益读数 |
| 盲下注时间戳早于测量 | 事后调整下注概率 |


---

# 第 1 部分 · 数据类型与常量

## 1.1 类型定义

```
TYPE Label:                       # 受控词表中的一个取值
    slot   : {agent|friction|mispricing|correction|form}
    value  : str                  # 例："inventory_risk"
    n_used : int                  # 被多少份档案用过
    # n_used 决定统计模型里的收缩强度：只用过一次的取值，
    # 后验会被强烈拉向全局均值，等于没提供信息。
    # 于是"随口造新词"的代价是自动的，不需要人来管。

TYPE Assertion:                   # 一条可判真假的预测
    id       : "P1".."Pn"
    subject  : str                # 被检验的可观测量。**必须是面板里的实际列名
                                  #   或一个已定义的派生量**，不能是自由文本，
                                  #   否则无法自动判定
    relation : {= | > | < | ∈ | monotone_up | monotone_down |
                saturating | U | reversal}
    value    : any
    prior_p  : float              # LLM 盲下注给的概率（已校准、已收缩）
    p0       : float              # 零假设下这条断言碰巧成立的概率
                                  #   （从安慰剂分布估出，绝不拍 0.5）
    weight   : float              # 合并 e 值时的权重，**冻结时写死**
    verdict  : {hold|violated|untested}    # 跑数后填

TYPE Cell:                        # 一个已注册的输入变量
    id             : str
    expression     : str          # 受限 DSL 表达式
    dimension      : {signed_continuous | unsigned_magnitude | bounded_01 |
                      count | categorical | binary | binned_distance}
    scale_exponent : int          # 价格×k 时本变量应该 ×k^scale_exponent
    causal_role    : {signal | moderator | mediator | selector | confounder}
    confounders    : list[str]    # 声明的混淆变量，注册时要检查已剥离
    data_deps      : list[str]

TYPE Structure:                   # 系统操作的基本单位
    id          : "S-0007"
    coord       : (M_row, F_col)
    labels      : {slot: Label}
    assertions  : list[Assertion]
    operational : list[Cell]      # 恰好 3 个
    lineage     : {parent: str, operator: str, origin: str}
                                  # origin 记录来源：manual / forest(freq=14/20) / transplant
    coverage    : str             # 覆盖区域的谓词表达式，决定 n_eff
    frozen_at   : timestamp
    hash        : str             # sha256(本档案内容 + 上一份档案的 hash)

TYPE Panel:                       # 测量层输出
    curves      : dict            # 见 M4.1
    psi         : array           # 逐观测影响函数分解（跨日聚合后）
    n_eff       : dict            # {horizon: n_eff}
    cost_tol_bp : float           # 成本容忍度

TYPE Blades:
    placebo            : {pass|fail, quantile}
    assertion_verdicts : {P1: verdict, ...}
    increment          : {pass|fail, residual_ic}
    icm                : {p_value, e_value, selection_freq} | None

TYPE Verdict: {PASS | FAIL | UNDECIDABLE}

TYPE Law:
    kind            : {度量层|传导层|条件层|边界层}
    claim           : str
    scope_predicate : str         # **必须可求值**，否则组合层用不了
    scope_negative  : str         # 可选：明确不成立的区域
    confirmed       : bool        # 见 M12.1
    evidence        : list[str]   # 指向 "S-0007.P4" 这样的具体证据
    reopen          : str         # 什么条件下应该重查
```

## 1.2 常量

```
# ── horizon ──
HORIZONS            = [1, 3, 5, 10]        # 首版只检验这四个交易日周期
DEFERRED_HORIZON_MAX = 10                   # 峰值超过此值的地图格首版暂缓

# ── 切分 ──
HOLDOUT_START       = 2025-08-01           # 封存段起点
EXPLORE_END         = 2022-06-30           # 探索段终点
CONFIRM_START       = 2022-07-22           # 确认段起点（中间 15 天 purge）
SPLIT_BLOCK_DAYS    = 5                    # 训练内部块切分的最小单位
PURGE_DAYS          = 10                   # = max(HORIZONS)
EMBARGO_DAYS        = 5

# ── bootstrap ──
BOOT_BLOCK_DAYS     = max(10, 2 × horizon) # ★ 必须 ≥ horizon，否则方差被低估
N_BOOT              = 1000

# ── 测量 ──
LAYER_N             = 5                    # 分层数（上限，实际由可分辨率决定）
WINDOW_FRAC         = 0.01                 # 滑窗宽度占信号值域的比例
TOPK_LIST           = [5, 10, 20]          # 贡献集中度的 top k%
CAR_WINDOW          = (-10, +20)           # 事件研究窗口

# ── 判别 ──
N_PLACEBO           = 500
E_THRESHOLD_PASS    = 20                   # W ≥ 20 判 PASS（= 1/0.05）
E_THRESHOLD_FAIL    = 0.05                 # W ≤ 0.05 判 FAIL

# ── 档案 ──
N_OPERATIONALIZE    = 3                    # 每份档案必须的表达式数
SLOTS               = [agent, friction, mispricing, correction, form]
VACUITY_BAND        = [0.20, 0.80]         # 断言先验概率的合法区间
N_BLIND_BETTORS     = 5                    # 空洞性检验用的独立实例数

# ── 发现 ──
EXPLORE_FRAC        = 0.65                 # 探索段占研究池的比例
N_SPLIT_REPEAT      = 20                   # 重复切分次数 B
FOREST_N_TREES      = 500
FOREST_MAX_DEPTH    = 2
QUANTILE_CUTS       = 5                    # ★ 每个候选变量只在 5 个分位点上搜索

# ── 冷启动 ──
N_SEED_GRIDS        = 4                    # 2×2 析因设计
```

## 1.3 一个完整档案的实例（贯穿全文的例子）

后面很多地方会引用这份档案，先放在这里。它对应地图上的 **M2-F3** 格。

```yaml
# research/S-0001/structure.yaml
id: S-0001
coord: [M2_流动性提供, F3_时序相对]

labels:
  agent:      liquidity_provider     # 谁：承接订单失衡的一方
  friction:   inventory_risk         # 约束：他持有存货有风险，因此要求补偿
  mispricing: temporary_impact       # 偏离形式：临时性冲击，不是信息
  correction: inventory_unwind       # 纠正：存货出清后价格回来
  form:       F3_ts_relative

# 机制陈述（人类可读，不进统计模型）：
#   短期订单失衡需要有人承接。承接者持有存货有风险，因此要求补偿。
#   这个补偿表现为价格的临时偏离，在存货出清后回归 —— 即短期反转。

operational:                          # 三个表达式 = 同一机制的三种写法
  E1: neg(ts_z(ret_5d, 60))
  E2: neg(xs_rank(ret_5d))
  E3: neg(sub(ret_5d, industry_mean(ret_5d)))

assertions:
  - id: P1
    subject:  dose_shape
    relation: monotone_down           # 涨得越多，后续跌得越多
    prior_p:  0.62
    p0:       0.35
    weight:   0.20
  - id: P2
    subject:  peak_horizon
    relation: "∈"
    value:    [1, 5]                  # 存货出清是快的
    prior_p:  0.70
    p0:       0.30
    weight:   0.20
  - id: P3
    subject:  effect_sign
    relation: "="
    value:    -1
    prior_p:  0.75
    p0:       0.50
    weight:   0.20
  - id: P4                            # ★ 旁证：不指向因子本身
    subject:  ic_ratio(amihud_high, amihud_low)
    relation: ">"
    value:    1.0                     # 流动性差的地方补偿更高
    prior_p:  0.60
    p0:       0.40
    weight:   0.20
  - id: P5                            # ★ 另一个独立旁证
    subject:  ic_ratio(auction_spread_wide, auction_spread_narrow)
    relation: ">"
    value:    1.0
    prior_p:  0.55
    p0:       0.42
    weight:   0.20

coverage: "in_pool AND NOT is_st"     # 全 panel 覆盖
lineage:
  parent:   null
  operator: seed
  origin:   manual
frozen_at: 2026-09-06T10:00:00Z
hash:      sha256:0000...             # 第一份，前驱哈希为全零
```

**注意 P4 和 P5**：它们说的不是"因子和收益的关系"，而是"如果我的机制是对的，那么在**别的地方**也应该看到某个现象"。这是刀②能够出鞘的前提。只写 P1–P3 的档案等于没有描述任何机制。


---

# 第 2 部分 · 模块规格

## 模块总览

| 模块 | 文件 | 职责 | 接触 y？ |
|---|---|---|---|
| M1 数据层 | `data.py` | 原始 parquet → 干净面板 + n_eff | 构建标签，是 y 的来源 |
| M2 变量注册 | `cell.py` | 输入变量的唯一事实源 | 否 |
| M3 想法空间与档案 | `cell.py` | 地图、断言生成、档案冻结 | 否 |
| M4 测量层 | `compute.py` | 完整刻画一个结构的表现 | **是** |
| M5 判别层 | `compute.py` | 四把刀 | **是** |
| M6 证据层 | `compute.py` | e 值与赌本 | **是** |
| M7 发现层 | `compute.py` | 下一步研究什么 | **是** |
| M8 一致性检查 | `compute.py` | 符号图平衡 | 否（输入已付费） |
| M9 记忆层 | `compute.py` | 层次模型 | **是**（输入已付费） |
| M10 组合层 | `compute.py` | 结构 → 可交易因子 | 否 |
| M11 LLM 岗位 | `test.py` | 五个岗位，按可见信息划分 | 分岗位 |
| M12 账本 | `test.py` | law.md 的读写与对账 | 否 |
| M13 主循环 | `test.py` | 编排 | — |

---

## M1 数据层（`data.py`）

> **职责**：把 920 个 parquet 变成一张干净的、无未来信息泄漏的日频面板，并算出真实的有效样本量。
> **这一层出错，后面全部无效**，所以每个函数都有强制断言。

### M1.0 数据源清单

| 目录 | 行数 | 用途 | 注意 |
|---|---|---|---|
| `daily_bar/` | 10.49M | **主表骨架** | OHLC、prev_close、limit_up/down、volume、total_turnover、**num_trades** |
| `open_auction/` | 10.34M | 09:25 集合竞价快照 | 含五档买卖盘口 `a1..a5`/`b1..b5` 及委托量。`datetime` 是 09:24–09:25 的浮动值，需截断成 date |
| `turnover/` | 10.49M | 换手率 | **单位是百分数**，0.4512 表示 0.4512% |
| `valuation/` | 10.49M | 市值、PE/PB/PS/PCF、EV | PE/PCF 可为负 |
| `st_flag/`、`suspension/` | 10.88M | 可交易性 | 比 daily_bar 多一天 |
| `industry_sws_2021_exact_changes/` | 11.4K | **PIT 行业的唯一正确来源** | `cancel_date=2200-12-31` 是哨兵值 |
| `industry_*`（快照类） | 519K | 月末快照 | 仅作交叉校验，不作主用 |
| `index_components_*` | — | 月末成分快照 | → 月末成分状态观察事件；不能推断准确纳入/剔除生效日 |
| `index_weights_*` | — | 月末权重快照 | `weight` 是小数，0.00409 = 0.409% |
| `index_daily_bar/` | 9.8K | 四个宽基指数 | 市场状态变量 |
| `dividend/` | 33.8K | 现金分红 | **含 2026-08 的未来事件**，必须用公告日控制可得性 |
| `split/`、`adj_factor/` | 4.6K / 34.4K | 送转与复权因子 | 复权方向必须先验证 |
| `yield_curve.parquet` | 2520 | 收益率曲线 | 宏观状态调节变量 |
| `instruments.parquet` | 5562 | 证券基本信息 | 上市日、板块、ST 类型 |
| `trading_calendar.parquet` | 2448 | 交易日历 | 所有位移运算的基准 |
| `return_calibration/` | 1.05M | 官方校准收益 | **只覆盖约十分之一**，不是完整面板，仅用于校验 |

### M1.0a `validate_data_sources(data_root) -> ValidationReport`

**目的**：在构建面板之前独立验证数据的结构、单位、时间边界和关键业务关系；任何高严重度检查失败都必须阻止后续研究。

**设计**：这是可插拔的数据质量组件，不读取未来收益作为研究证据，也不修改原始数据。每项检查返回 `pass / warning / fail`、样本数量、失败示例和检查版本；报告写入 `data_validation.json`。主流程只依赖报告，不依赖某个具体供应商实现。

**首版强制检查**：

1. 所有主键唯一，日期可解析且落在交易日历；关联表连接前后行数不膨胀。
2. OHLC、成交量、成交额、盘口价格满足非负性和基本不等式；成交额与成交量×价格的量级差异必须进入报告。
3. `turnover.today` 的单位按百分数处理，并与成交额、流通规模做量级交叉检查；无法确认的周/月字段不进入主因子。
4. `open_auction` 每只股票每日最多一条，时间落在集合竞价窗口；盘口价差、买卖量为零或缺失的比例单独报告。
5. `adj_factor` 与 `split` / `dividend` 事件关联；抽取高送转样本验证复权后除权日收益接近零，失败则中止。
6. `industry_sws_2021_exact_changes` 区间不重叠；空档率和重复匹配率分别报告，重复匹配直接失败。
7. `st_flag`、`suspension`、`valuation` 与 `daily_bar` 的日期边界差异符合预期；状态表多出的日期不得进入主行情骨架。
8. `return_calibration` 只作为抽样校验，不得被误当成完整收益面板。

**边界**：检查无法证明供应商语义时只输出 `warning` 并禁用相关字段；不得用默认值把未知口径变成零。

---

### M1.1 `build_panel(data_root, universe_spec) -> DataFrame`

**目的**：构建 `(order_book_id, date)` 粒度的日频研究面板。

**算法**：
1. 调用 `validate_data_sources`；存在 `fail` 时立即停止。
2. 以 `trading_calendar` × `instruments` 生成骨架，按 `listed_date` / `de_listed_date` 裁剪。
3. **内连** `daily_bar`（不是左连，见难点 1）。
4. 依次左连 `turnover`、`valuation`、`st_flag`、`suspension`，键 `(order_book_id, date)`。
5. 左连 `open_auction`：先 `date = datetime.dt.date`。
6. 调用 `build_universe_pit` 生成可交易性列。
7. 调用 `attach_industry_pit` 生成 PIT 行业。
8. 调用 `build_labels` 生成未来收益列。
9. 调用 `assert_no_lookahead`，不过则整个函数失败。

**不需要 `aggregate_stock_day`**：日频量价天生就是一行一个 `(股票,日期)`，没有多事件聚合问题。这是相对文本面板的一个简化。

**难点 1 —— 状态表比行情表多一天**：`st_flag`、`suspension`、`valuation` 到 2026-07-30，`daily_bar` 只到 2026-07-29。
**解法**：第 2 步用内连接，以 `daily_bar` 的日期为准。左连会在最后一天产生只有状态没有价格的行。

**难点 2 —— join 使行数膨胀**：如果任何一张表的 `(order_book_id, date)` 不唯一，join 后行数会变多，而且很难发现。
**解法**：每次 join 前对右表做 `assert df.duplicated(['order_book_id','date']).sum() == 0`；join 后断言行数不变。

**验收**：
- 面板行数 == `daily_bar` 在 universe 内的行数
- 每次 join 前后行数一致

**调用**：`validate_data_sources`；`build_universe_pit`, `attach_industry_pit`, `build_labels`, `assert_no_lookahead`
**被调用**：`main`（仅一次）

---

### M1.2 `build_universe_pit(instruments, st_flag, suspension, daily_bar) -> DataFrame`

**目的**：生成逐日的可研究/可交易标记。这是防止幸存者偏差与不可成交样本污染的地方。

**背景**：A 股有大量结构性的不可交易情形（停牌、ST、一字板、次新股）。如果不处理，你会得到"IC 很高但实际买不进"的因子。这是 A 股回测最常见的一类假阳性。

**输出列**（**每条单独一列，不要合并成一个布尔值**——后面它们既做过滤器又做调节变量）：

| 列 | 规则 | 为什么 |
|---|---|---|
| `listed_ok` | `date ≥ listed_date + 120 交易日` | 次新股价格行为完全不同（炒新、限售解禁），混进来会污染所有因子 |
| `not_delisted` | `de_listed_date` 为空 或 `date < de_listed_date` | 防幸存者偏差 |
| `not_st` | `is_st == False` | ST 股涨跌幅限制 5%，是另一个机制 |
| `not_suspended` | `is_suspended == False` | 停牌日无法交易 |
| `tradable_t1` | 仅作为 T+1 执行检查，不回溯修改 T 日候选池 | T 日股票池必须由当日已知信息确定 |
| `not_limit_locked` | 仅作为 T+1 执行检查，不回溯修改 T 日候选池 | 无法成交必须暴露为执行错误 |
| `in_pool` | `listed_ok AND not_delisted AND not_st AND not_suspended` | 主研究池；默认排除 ST |

**难点 —— 涨跌停是 A 股最大的假 IC 来源**：很多"高 IC 因子"的收益全部来自次日一字涨停，而一字涨停是买不进的。
**解法**：T 日候选池不使用次日字段。回测在 T+1 开盘执行时检查停牌和涨跌停；买入或到期卖出任一订单无法成交，主回测必须抛出明确异常并停止，不能删除该样本或静默持仓。

**验收**：随机抽 20 只已退市股票，确认它们在退市前的日期里 `not_delisted == True`；抽 20 个已知的一字板日期，确认 `not_limit_locked == False`。

---

### M1.3 `attach_industry_pit(panel, exact_changes) -> DataFrame`

**目的**：给每个 `(股票, 日期)` 打上**当时真实有效**的申万一级行业。

**背景**：行业中性化是本系统的主 IC 口径（见 M1.4），所以行业归属必须准确。用月末快照会在两次快照之间造成最多一个月的错配——某只股票 7 月 10 日调整了行业，但你用 6 月 30 日的快照，那 7 月 10–31 日的中性化就是错的。

**算法**：
1. 读 `industry_sws_2021_exact_changes/`，得到 `(order_book_id, industry_code, start_date, cancel_date)`。
2. 区间连接：`start_date ≤ date < cancel_date`。
3. `cancel_date == 2200-12-31` 表示当前仍有效（哨兵值，不是真实业务日期）。
4. 断言：每个 `(股票, 日期)` 恰好匹配到一个行业。

**难点**：同一只股票在不同时期属于不同行业，区间可能有重叠或空隙（数据质量问题）。
**解法**：断言匹配数 == 1。匹配数 == 0（空隙）时记为 `industry = UNKNOWN` 并单独统计比例，比例 > 1% 要去查数据；匹配数 > 1（重叠）直接抛异常。

**验收**：随机抽 50 只股票，画出它们的行业归属时间线，人工检查是否有跳变异常。

---

### M1.4 `build_labels(daily_bar, adj_factor, universe, industry_pit) -> DataFrame`

**目的**：生成四种口径的未来收益，每种 × 四个 horizon。

#### 第一步：复权

`daily_bar` 里的价格是未复权的。直接算跨日收益会在除权日产生巨大的假收益（10 送 10 会显示成 −50%）。

```
adj_close(i,t) = close(i,t) × cum_factor(i,t)
adj_open(i,t)  = open(i,t)  × cum_factor(i,t)
```
`cum_factor` 由 `adj_factor` 表的 `ex_cum_factor` 按 `ex_date` 前向填充得到。

**难点 —— 复权方向搞反是极高严重度的错误**，而且症状不明显（只在少数日期出错）。
**解法（必做的验证）**：从 `split` 表里挑一只 `split_coefficient_to / split_coefficient_from == 2` 的股票（10 送 10），检查复权之后除权日的收益是否接近 0（|ret| < 5%）。**这个验证不做，后面全部无效。**

#### 第二步：定义收益的起止点（T+1 建仓）

**难点 —— 忽略 T+1 会让整个系统的收益系统性虚高。**

信号在 T 日收盘后算出，最早只能在 T+1 开盘建仓。所以：

```
r_h(i, t) = adj_open(i, t+1+h) / adj_open(i, t+1) − 1
```

**注意 horizon 是从建仓点 t+1 开始数的**，不是从信号日 t 开始。用收盘到收盘（`adj_close(t+h)/adj_close(t)`）会把 T→T+1 那一段隔夜跳空也算进收益，而那一段你根本吃不到。

#### 第三步：四种口径

**关键：基准必须是同一个 h 天窗口上的截面均值，不是当日的 1 日收益。**

```
market_excess_h(i,t)  = r_h(i,t) − mean_{j ∈ P_t} r_h(j,t)
industry_resid_h(i,t) = r_h(i,t) − mean_{j ∈ I_t(i)} r_h(j,t)
net_h(i,t)            = r_h(i,t) − cost_bp_fixed / 10000
```

其中：
- `P_t` = **t 日**的在池股票集合
- `I_t(i)` = **t 日**股票 i 所属行业的成员集合
- **股票池和行业归属都在 t 日固定**，不能用 t+h 时的——否则用了未来的行业变更和退市信息
- 窗口内停牌/退市的股票 `r_h` 为 NaN，从均值里剔除，但**不改变其他股票的基准定义**

| 口径 | 主要用途 |
|---|---|
| `raw_h` | 参考，以及与 `market_excess` 的一致性验证 |
| `market_excess_h` | 多空组合收益（TB）的口径 |
| **`industry_resid_h`** | **IC 计算主用** |
| `net_h` | 成本容忍度的计算 |

**难点 —— `market_excess` 对截面 IC 完全没有影响。**

相关系数对平移不变，而"减去当日全样本等权均值"就是一次平移：

```
corr(f, r − c) = corr(f, r)   对任意常数 c
```

所以 **IC(raw) ≡ IC(market_excess)**，两者数值完全相同。用 `market_excess` 的理由是多空组合收益的口径，不是 IC。

真正会改变 IC 的是 `industry_resid`——它是**分组去均值**（每个行业减自己的均值），不是全局平移，因此移除了行业间的变异。IC 通常会变小，这才是行业中性化的实质。

**主口径用 `industry_resid_h` 的两个理由**：
1. 剥离了行业暴露，剩下的是真正的选股能力
2. **它降低了 ρ̄，从而提高 n_eff**（见 M1.7）——行业效应是横截面相关的主要来源

报告里要同时给 `raw` 与 `industry_resid` 两套 IC，**差额本身就是行业暴露的度量**。

#### 第四步：成本 —— 只用固定成本，不用冲击成本

**背景**：原本的成本模型含 `η·σ·√(Q/ADV)` 这一项，其中 `Q` 是下单量。但**下单量是组合层的决策，研究阶段根本还不存在**。在研究层引入它等于凭空造了一个可调旋钮，而且它会诱使你去调。

```
cost_bp_fixed = 佣金 + 印花税（卖出） + 事前冻结的滑点情景
```

研究层不再用“净 IC 穿零”定义成本容忍度。统一扣除固定成本不会改变截面 IC；成本容忍度必须在回测层的参考持仓规则下定义：

```
cost_tolerance_bp = 使参考策略平均净收益降到 0 的成本水平
```

实现：冻结参考持仓、换手和费用规则后，横轴扫描成本情景，纵轴记录净收益、Sharpe 和换手；收益穿零点才是容忍度。IC、TB 与成本敏感性分开报告。

冲击成本、容量、拆单全部推给回测系统，那是策略层的事。

**验收 1**：成本为零时，参考策略净收益必须等于毛收益；固定成本不得改变 IC。
**验收 2**：对任意因子，`IC(raw_h)` 与 `IC(market_excess_h)` 必须在浮点误差内相等。不等说明两处的 universe 不一致（常见 bug）。
**验收 3**：复权验证（第一步的难点）。

---

### M1.5 `split_data(panel) -> {holdout, explore, confirm}`

**目的**：定义全项目的数据切分。**两层切分，用途完全不同，绝不能混。**

#### 第一层：封存

```
封存段 H：2025-08-01 ~ 2026-07-30    ≈ 240 交易日（约 10%）
研究池  ：2016-07-01 ~ 2025-07-31    ≈ 2208 交易日
```

封存段放在**独立目录**，读取必须经 `audit_read()` 包装并写日志。**全项目只读一次**——在最终报告之前，对已经 PASS 的那 1–3 个结构做最后确认。

#### 第二层：研究池内的探索/确认（`discover` 专用）

```
探索段 A：2016-07-01 ~ 2022-06-30    ≈ 1440 交易日（65%）
  purge ：2022-07-01 ~ 2022-07-21    ≈ 15 交易日（挖掉，谁都不用）
确认段 B：2022-07-22 ~ 2025-07-31    ≈ 753 交易日（35%）
```

purge 长度 = `PURGE_DAYS(10) + EMBARGO_DAYS(5)`，保证 A 段最后一天的 10 日标签不会伸进 B 段。

#### 各段的用途（严格）

| 段 | 允许的操作 |
|---|---|
| **A 探索** | 全部测量（M4）、刀①②③（M5）、赌本结算（M6）、`discover` 的搜索（M7.3–7.5） |
| **B 确认** | 候选批次冻结后只读取一次；统一检验主效应、必要形状和旁证。此前任何搜索、重复切分和模型拟合都不许读它 |
| **H 封存** | 最终报告前读一次 |

#### 各段 n_eff 预估（h=5，industry_resid 口径，ρ̄≈0.10）

| 段 | 交易日 | 独立时间块 | n_eff | 可检出的最小效应 |
|---|---|---|---|---|
| A 探索 | 1440 | 288 | ≈ 2880 | 主效应 0.052 |
| B 确认 | 753 | 151 | ≈ 1510 | **交互 0.144** |
| H 封存 | 240 | 48 | ≈ 480 | 主效应 0.128 |

**这张表本身就是一个重要结论**：B 段能检出的最小交互效应是 0.144，而典型主效应只有 0.03–0.05。**所以全 panel 上的交互检验基本没有功效。** 这是跑 `power_budget`（M7.6）就能提前知道的事，不要做完了才发现。

#### 第三层：A 段内部的块级重切（稳定性检查，不是样本外）

在 A 段内做 5 次不同种子的块级训练/验证折：以 `SPLIT_BLOCK_DAYS(5)` 为单位整块分配，折间挖掉 `PURGE + EMBARGO`。这些折只用于训练/验证稳定性，不是正式 B 确认，也不能访问 B。

**这不是独立的样本外验证。** 5 次结果一致只说明"结论不依赖具体切法"，**不说明样本外有效**。这两件事经常被混淆，报告里必须写清楚。

#### 时间切分的代价（必须写进报告）

A 段（2016–2022）和 B 段（2022–2025）的市场状态确实不同。所以刀④不显著时，你**分不清是"发现错了"还是"市场变了"**。

处理办法是把这个歧义写进 `law.md` 而不是掩盖：按预先登记的市场状态指标和时间块报告 regime shift；`reopen` 写"需要在下一个市场周期重新检验"。B 不显著不能单独归因于发现错误。

**验收**：遍历所有 (A 块, B 块) 对，最小日期间隔 ≥ 15 个交易日；封存段目录的读取日志在项目结束时应恰好有 1 条记录。


---

### M1.6 `compute_n_eff(panel, factor, horizon, mask=None) -> float`

**目的**：算出真实的有效样本量。**这是全系统最重要的单个数字。**

#### 为什么名义行数没有意义

IC 就是一个样本相关系数。对 n 个**独立**观测，在真实相关为 0 的零假设下：

```
SE(ÎC) ≈ 1/√(n−1) ≈ 1/√n
```

（更一般是 `(1−ρ²)/√n`，但 ρ 只有 0.03 量级，`ρ²` 可忽略。）

所以"有效样本量"和"IC 的标准差"是同一件事的两种说法：

```
n_eff = 1 / σ_IC²        σ_IC = 1 / √n_eff
```

面板名义 1049 万行，但独立性在**两个方向**上被破坏：

1. **同一天的股票不独立** —— 同行业、同风格一起涨跌。当天你实际拥有的独立信息远不到 4300 个。
2. **相邻的天不独立** —— `h=5` 的标签覆盖未来 5 天，连续 5 天的 IC 几乎是同一个信息看了五遍。

#### ★ 主方法：块 bootstrap 反解

**这是唯一应该写进代码的定义。**

```
算法：
  1. 算出每日截面 IC 序列 {IC_1, ..., IC_T}
     （若给了 mask，只在 mask 内的样本上算）
  2. 块 bootstrap 估 mean(IC) 的标准误：
       - 块长 = BOOT_BLOCK_DAYS = max(10, 2×horizon)，**必须 ≥ horizon**
       - 有放回地重抽 T/块长 个块，拼成一个 bootstrap 样本
       - 算 mean(IC)
       - 重复 N_BOOT = 1000 次
       - σ_IC = 这 1000 个 mean(IC) 的经验标准差
  3. n_eff := 1 / σ_IC²
```

**为什么块长必须 ≥ horizon**：如果块长 < horizon，重叠的标签会被拆到不同的 bootstrap 块里，块间被当作独立，重叠效应被抹掉，**方差被系统性低估，显著性虚增**。

这样得到的 `n_eff` 是**从数据里量出来的**，自动包含了截面相关、标签重叠、行业聚类、时变波动——所有你想不全的因素。

#### 量级检查用的公式（只做 sanity check，不作为实现）

```
n_eff ≈ (T_days / horizon) × N_cross / (1 + (N_cross − 1)·ρ̄)
```

**这个公式有两处必须说清楚：**

**（a）ρ̄ 必须是「去均值残差」的两两相关，不是原始收益的相关。**

纯粹的市场共同项**根本不影响截面相关**——相关系数对平移不变，而共同项就是一个平移。你已经在标签里减掉了均值（`industry_resid` 是分组减均值），共同项在构造上就被移除了。

所以这里的 ρ̄ 来自**行业与风格效应的残余**，量级约 0.05–0.20，而不是原始收益那个 0.3–0.5。

**这两个数会让显著性门槛差一倍：**

| ρ̄（残差） | N_eff,cross | n_eff | σ_IC | t>2 的门槛 |
|---|---|---|---|---|
| 0.05 | ≈ 20 | ≈ 9800 | 0.010 | IC > 0.020 |
| 0.10 | ≈ 10 | ≈ 4900 | 0.014 | IC > 0.029 |
| 0.20 | ≈ 5 | ≈ 2450 | 0.020 | IC > 0.040 |

**（b）`N/(1+(N−1)ρ̄)` 是"相关变量之平均值"的方差膨胀公式，套到相关系数上是启发式近似，不是严格推导。**

所以它只用来核对量级：若 bootstrap 反解出的 n_eff 与公式差 10 倍以上，说明存在你没考虑到的依赖结构，去查。

#### 更好用的直觉：想聚类，不要想相关系数

> 4300 只股票聚成 31 个申万一级行业，行业内高度相关、行业间较弱。
> 那么**你实际拥有的独立截面观测大约是行业数量的量级，而不是股票数量。**

31 个行业中若干高度相关 → 有效截面数落在 5–20 之间，正好对应上表三行。

#### 三个必须分别算的维度

| 维度 | 为什么 |
|---|---|
| **每个 horizon 分别算** | h 越大，独立时间块越少 |
| **每个结构分别算**（用它自己的 `coverage` 作为 mask） | 一个只覆盖 15% 样本的结构，有效截面数也大约只有 15% |
| **分时间窗口算再加总** | 2018、2020 初、2024 初这些同涨同跌的时段 ρ̄ 会跳到 0.6，有效样本量骤降 |

第二条是 `gate_decidable`（M5.1）的直接依赖。第三条的副产品——**ρ̄ 的时序图本身就是一条度量层规律**，应该写进 `law.md`。

**验收**：
1. 蒙特卡洛：生成已知 ρ 与已知标签重叠度的合成面板，bootstrap 反解值与理论值之比落在 [0.7, 1.4]。
2. 用 `h=1`（无标签重叠）跑一次，`n_eff` 应显著大于 `h=10` 的结果，比例约 10 倍量级。
3. 把面板随机打乱（破坏所有相关结构）后重跑，`n_eff` 应接近名义行数。

**被调用**：`gate_decidable`（M5.1）、`measure_panel`（M4.1）、`power_budget`（M7.6）、`forest_propose` 的最小叶节点定义（M7.4）

---

### M1.7 `assert_no_lookahead(panel) -> None`

**目的**：检测未来信息泄漏。

**主检验**：验证每个变量的可得时间约束，而不是要求滞后后 IC 消失。截断到任意历史日期重新构建特征时，历史特征值必须不变；故意注入未来字段必须被拒绝。持续性变量滞后后仍有 IC 不构成前视证据。

**难点**："标签必须全变"这个判据不能用浮点相等实现。
**解法**：比较**分布**而非数值。

#### 本数据集特有的三个泄漏来源，必须逐个单测

**① 月末快照被当成日频真值**
用 7 月 31 日的行业/指数成分快照给 7 月 15 日打标签，就是用了未来信息。
必须用"该日期**之前**最近的快照"，或者用 `exact_changes` 的区间（行业）。

指数成分变化采用方案 A：相邻月末快照首次显示状态变化的日期，只登记为**月末快照观察事件**。事件窗口必须以该观察日为锚，并在结果中明确“观察事件”标签；不能解释为真实纳入/剔除生效日、公告日或基金实际调仓日。

**② `dividend` 表含 2026-08 的未来事件**
记录存在 ≠ 市场当时已知。必须用 `declaration_announcement_date` 而不是 `ex_dividend_date` 控制可得性。

**③ `adj_factor` 的 `ex_cum_factor` 是站在今天回看的**
在历史某日使用"最终累计因子"等于知道了未来所有分红。

**这一条有个微妙之处需要验证**：如果你只算**相对收益** `adj_close(t+h)/adj_close(t)`，那么两个时点的 `cum_factor` 中，`t` 之后到 `t+h` 之间的除权会正确反映，而 `t+h` 之后的除权在分子分母上同时出现、约掉了。所以**对收益率计算而言，用"从今天回看的复权序列"是等价的**。

但对于**用价格水平本身作为因子的情形**（比如 `close / ts_max(high, 252)` 距 52 周高点），就不等价——`ts_max` 会受未来复权因子影响。
**解法**：所有涉及价格水平的变量，必须用 point-in-time 的复权因子（截止到 t 日为止的累计因子）。在 `register_cell` 里检查这一点。

**验收**：故意注入一个用 `t+1` 收益构造的变量，本函数必须报警；对合法的持续性变量不得仅因滞后后仍有 IC 而误报。


---

## M2 变量注册（`cell.py`）

> **职责**：所有输入变量的唯一事实源。未经注册的变量不能进入任何计算。
> 注册期的检查**不接触未来收益，因此是免费的**，所以要尽可能严。

### M2.1 `register_cell(spec) -> Cell`

**目的**：注册一个输入变量并做五项强制检查。任一不过则不进池。

**背景**：大部分因子研究的失败不是因为逻辑错，而是因为变量本身有问题——量纲不对、和别的变量共线、隐含前视、边界情形被静默处理掉了。这些问题在注册期检查成本极低，在跑完之后再发现成本极高。

#### 五项检查

**① 量纲不变性**

```
测法：把 daily_bar 的所有价格列 ×10、成交量列 ×100，重新计算变量值。
判据：按注册时声明的 scale_exponent 核对。
      scale_exponent=0（尺度不变）→ 新旧值相关 > 0.999
      scale_exponent=1（线性）    → 新值 ≈ 旧值 × 10
```

为什么需要显式声明：有些变量确实该随尺度变（绝对价差），有些不该（换手率、收益率）。不声明就无法判断"变了"是 bug 还是设计。

**② 混淆物剥离**

```
测法一（线性）：与 spec.confounders 里每个变量的相关，|ρ| < 0.3
测法二（非线性）：按混淆变量分 5 层，本变量在各层的均值不得单调
```

为什么要第二个测法：低相关不代表没有混淆。一个 U 型关系的相关系数可以是 0。

**③ 边界行为**

| 情形 | 要求 |
|---|---|
| 比值类变量 | 必须取 log（否则分布严重偏斜，分位切分失效） |
| 截断变量（有上下限） | 必须带一个 flag 列标记是否触界 |
| 除零 | 必须返回 `NaN`，**绝不能静默返回 0** |
| `inf` | 必须抛异常或转 NaN，不能进入下游 |

`0` 和 `NaN` 的语义完全不同（见 M10.4），混淆会造成系统性错误。

**④ 前视**

单变量版的 `assert_no_lookahead`：把这一个变量右移一期，它与收益的相关应显著变化。

另外检查：若变量涉及价格水平（不只是收益率），必须使用 point-in-time 复权因子（见 M1.7 第 ③ 点）。

**⑤ 共线禁止**

与每个已注册变量做回归，`R² > 0.98` 直接拒绝。这防止同一个东西被注册两遍、然后在增量检验里互相抵消。

#### 额外硬要求

**变量的标准化、变换和切点规则按 `dimension` 类型冻结，不采用一刀切。** 有符号连续变量可用 `ts_z`/`xs_rank`，无符号幅度通常先 `log1p` 再按时序规则处理，有界和类别变量保留其边界语义。价格水平变量必须使用 point-in-time 复权；未声明规则的变量不得进入 IC。

**验收**：构造含 0 分母、含 `inf`、含全 NaN 列的小样本，函数必须保留 NaN 并计数；非法配置必须抛异常，不能静默返回 0。三个合理但相关的算子化可以并存，相关性只在增量检验阶段处理。

---

### M2.2 `dispatch_by_dimension(cell) -> {norm_fn, cut_rule, expected_shape, checks}`

**目的**：不同类型的变量需要不同的标准化与分层方式。这张表把"该怎么处理这个变量"从每次拍脑袋变成自动分派。

**背景**：这是"分层怎么推广到各种特征"这个问题的答案。分层在系统里承担三种不同职能，必须分开：

| 职能 | 用途 | 正确形式 | 层数由什么决定 |
|---|---|---|---|
| **测量** | 看效应的形状（单调/饱和/U型） | 硬分层 | 可分辨率（M2.3） |
| **条件** | 在 A 的某区域内估 B 的效应 | 不手工切，交给森林在冻结切点内搜 | 森林决定 |
| **生成** | 输出交易信号 | 连续 `logit_gate` | 不分层 |

**一句话**：看形状用分层，做条件用森林，做信号用连续。多数人的困惑来自把测量工具当生成工具用。

#### 分派表

| 变量类型 | 标准化 | 切分规则 | 预期形状 | 专属检查 |
|---|---|---|---|---|
| **有符号连续**<br>`ret_5d`, `gap_open` | `ts_z` / `xs_rank` | **切点必须包含 0** | 单调或反转 | 检查正负两侧是否对称——不对称本身是发现 |
| **无符号幅度**<br>`turnover`, `amihud`, `vol` | log 后分位 | 分位 | **预期饱和，不是单调** | 若呈现完美单调，多半是没饱和够，扩大值域重看 |
| **有界 [0,1]**<br>`dist_52w_high`, `auction_turnover_share` | 原值分位 | 端点单独成层 | 阈值型 | **端点堆积率 > 30% 时分位切分是假的**，必须改成"端点 + 中间分位" |
| **计数**<br>`num_trades` | `log1p` | 小计数各自成层 | 单调 | 计数=1、2、3 往往是独立机制，不该和大计数放同一条曲线 |
| **类别**<br>`board_type`, `industry` | 无 | 等值切分，**不排序** | 分组差异 | 层数由每类样本数决定，小类合并 |
| **二元状态**<br>`is_limit_up`, `is_st` | 无 | 直接作为条件 | — | **只做交互，不做主信号** |
| **分档时距** | 保持原档 | 保持 | 单调 | **必须用连续 `log(距离)` 做对照**，验证分档本身不是过拟合 |

**被调用**：`freeze_cuts`（M2.3）、`measure_panel`（M4.1）、`forest_propose`（M7.4）

---

### M2.3 `freeze_cuts(cells, explore_train) -> CutTable`

**目的**：把所有变量的分层切点算一次并**永久冻结**。

**背景**：如果切点可以在研究过程中调整，就等于多了几十个隐藏的自由参数，过拟合从这里进来。冻结是在**数据结构层面**强制"只调逻辑不调参"——不是靠自觉。

**难点 1 —— 切点必须建立在标准化之后的变量上，不是原始值上。**

日频面板跨越十年，原始值的分布会大幅漂移。在混合十年的池化分布上取分位，得到的切点在 2016 年和 2025 年含义完全不同（比如"换手率 > 3%"在两个时期的稀有程度差很多）。

**解法**：先按 M2.2 的分派表套上标准化函数（`ts_z` 或 `xs_rank`），**再在标准化后的尺度上冻结切点**。这样切点的语义是"距自身历史 1.5 个标准差"或"当日截面前 20%"，跨时间稳定。

**难点 2 —— 分位点本身有抽样误差，尾部尤其不稳。**

**解法**：
```
对每个切点做 200 次块 bootstrap，得到切点的置信区间。
若相邻两层的切点区间重叠 > 50%，强制合并这两层。
```
**层数由数据的可分辨率决定，不由 LAYER_N 指定。** 这条会让一些变量只有 3 层而不是 5 层，那是对的。

**算法**：
1. 对每个 cell，取 `dispatch_by_dimension` 给的标准化函数
2. 在探索段 A 的训练子集上应用标准化
3. 算分位数（或类别水平）
4. bootstrap 得置信区间，合并不可分辨的层
5. 写 `frozen/cuts.parquet`
6. **此后任何读取切点的代码路径不得再接触训练集标签**

**验收**：用不同 bootstrap 种子重跑两次，最终层数必须一致。

---

### M2.4 `vocabulary_registry(slot) -> list[Label]`

**目的**：返回某个标签槽位当前的受控词表，以及每个取值的使用次数。

**为什么重要**：标签的作用是构成统计模型的设计矩阵（M9）。只有当新研究**复用**已有的标签取值时，它才能与历史研究共享统计信息。如果每次都自由造词，每个取值只出现一次，模型学不到任何东西。

**词表的初始内容**：附录 F.1 给出了 10 个机制族的默认标签，那就是种子词表。

**被调用**：`llm_proposer`（M11.1）、`register_structure`（M3.4）、`build_design_matrix`（M9.1）

---

### M2.5 `merge_vocabulary(proposals) -> VocabEdit`

**目的**：合并语义相同但写法不同的标签取值。

**背景**：`"注意力有限"` 和 `"关注度约束"` 对人是同一个意思，对模型是两列。不归一化，设计矩阵会越来越稀疏。

**判据（客观，不靠口才）**：合并后整个结构库的**边际似然是否上升**，用 LOO（留一交叉验证）近似。

**难点**：边际似然在样本量小时对先验极敏感。
**解法**：检查 Pareto k 诊断；若 `k > 0.7` 的观测超过 20%，判据不可信，**拒绝合并**（保守方向）。

**硬约束**：**只修改标签，绝不修改断言。** 断言一旦冻结，任何操作都不能碰它——否则所有历史赌本失效。

**调用者**：`llm_inducer`（M11.5）提出方案，本函数裁决。


---

## M3 想法空间与档案（`cell.py`）

> **职责**：定义"有哪些想法值得研究"，并把每个想法冻结成不可篡改的档案。
> 这一层完全不接触未来收益，**因此是免费的，可以放开做**。

### M3.1 `build_map(rows, cols) -> Map`

**目的**：构建 M×F 的机制地图，即想法空间的全集。完整内容见**附录 F**。

**行 = 机制族**（为什么会有超额收益）：

| # | 族 | 一句话 |
|---|---|---|
| M1 | 注意力冲击 | 异常成交/涨跌吸引散户注意力 → 买入压力集中 → 短期反转 |
| M2 | 流动性提供 | 订单失衡需要有人承接 → 承接者要求补偿 → 反转是补偿的实现 |
| M3 | 处置效应与锚定 | 参考价造成不对称卖出压力 → 调整被拖慢 → 动量 |
| M4 | 信息缓慢扩散 | 信息先反映在部分标的，再传导到关联标的 |
| M5 | 交易约束（A股特有） | 涨跌停/停牌/T+1 使价格当日无法出清 → 调整延到次日 |
| M6 | 被动资金与再平衡 | 月末快照观察到的成分与权重变化，作为状态观察，不冒充公告/生效事件 |
| M7 | 参与者结构 | 大单/小单占比反映谁在交易 → 价格影响持续性不同 |
| M8 | 风险补偿与估值 | 估值差异是风险补偿，受宏观利率调节 |
| M9 | 不确定性解决 | 分红/送转/复牌使不确定性下降 → 折价消失 |
| M10 | 隔夜信息聚合 | 隔夜信息在集合竞价中被定价，委托失衡暴露定价压力 |

**列 = 表现形式**（在日频量价数据里长什么样）：

| # | 形式 | 典型算子 |
|---|---|---|
| F1 | 水平 | `xs_rank(x)` |
| F2 | 变化 | `x − lag(x,k)`、`diff(log(x))` |
| F3 | 时序相对 | `ts_z(x,60)`、`x/ts_mean(x,20)` |
| F4 | 截面相对 | `x − industry_mean(x)` |
| F5 | **背离** | `xs_rank(a) − xs_rank(b)`（两个本应同向的度量不一致） |
| F6 | 条件化 | `gate(z) × signal` |
| F7 | 事件化 | `CAR[-10,+20]` |

**F5 是本项目特有的设计**：文本面板里这一列是"文内相对"，日频量价里没有这个概念。改为**背离**——价涨量缩、竞价强而全天弱、大单流入但价格没涨。这是纯量价数据里信息量最高的一类构造，因为它天然是**两个变量的关系**而不是单一变量的水平，因此更难被简单的线性因子覆盖。

**规模**：10 × 7 = 70 格，可行约 52 格。

**为什么这样设计**：把"想法"从无限的表达式空间变成有限集合。于是三件事成为可能：
1. "想法空间有多大""探索了多少"是可以报出的数字
2. 未探索的空白格本身就是研究计划
3. 标签成为统计模型的设计矩阵（M9），跨研究的记忆才有载体

---

### M3.2 `mark_infeasible(map) -> Map`

**目的**：把机制上不成立的格子标灰并记录理由。

**灰格是产出，不是缺失**——它记录的是一个推理结论，而且这个结论本身可能是错的、可以被后来推翻。

灰格规则：

| 格子 | 灰的理由 |
|---|---|
| M4-F1 | 信息扩散的"水平"没有定义——扩散是关系，不是状态 |
| M5 的 F1–F5 | 涨跌停/停牌本身是状态，不是可排序的预测信号；只能做条件或事件 |
| M6-F1/F3/F5 | 指数权重是月末快照，日频水平无意义；只有变化和纳入剔除事件有 |
| M8-F2/F5 | 估值的日频变化几乎等于价格变化本身，构造上共线 |
| M9 的 F1–F5 | 不确定性解决本质就是事件 |
| M7-F7 | 参与者结构没有天然的离散事件 |

---

### M3.3 `compose_assertions(labels, z=None) -> list[Assertion]`

**目的**：从机制标签生成可证伪的断言。

**这是机制唯一的用处**——机制的价值在于它能推出可能为假的预测，不在于它读起来合理。

#### 映射规则（LLM 在此表内选择，不自由发挥）

| 标签取值 | 推出的断言 |
|---|---|
| `mispricing = underreaction` | 形状单调向上；峰值 horizon 偏长（5–20） |
| `mispricing = overreaction` | 形状反转；峰值 horizon 偏短（1–5） |
| `mispricing = temporary_impact` | 形状单调；峰值极短（1–3）；效应应随流动性变差而增强 |
| `mispricing = delayed` | 事件研究曲线应有明显峰值，而非单调 |
| `correction = inventory_unwind` | 峰值 horizon ∈ [1,5] |
| `correction = attention_decay` | 峰值 horizon ∈ [3,10] |
| `correction = slow_convergence` | 峰值 horizon ∈ [20,60] |
| `friction = inventory_risk` | 旁证：效应在 amihud 高、价差宽的股票上更强 |
| `friction = attention_bandwidth` | 旁证：效应在小市值、低关注度股票上更强 |
| `agent = retail` | 拥挤度断言：效应应随时间衰减（散户结构在变化） |
| `agent = index_fund` | 旁证：效应应集中在成分股，非成分股上不存在 |

#### 硬要求

```
至少 1 条形状断言（P_shape）
至少 2 条旁证断言（P_side），且其中至少 1 条不指向因子本身
```

**"不指向因子本身"是什么意思**：`P3 效应符号为负` 说的是因子和收益的关系；`P4 在流动性差的股票上效应更强` 说的是"如果我的机制对，在**别的地方**也应该看到某个现象"。只有后者才是机制的证据。

写不出两条旁证 = 这份档案只描述了因子与收益的关系，没有描述任何机制 → 打回。

#### 难点（本模块最核心的问题）：LLM 会写空洞断言

例如"效应显著为正"、"IC 大于 −1"。这种断言必然成立，没有证伪力，写多少条都等于零。

#### 解法：空洞性检验

```
算法：
  1. 把每条候选断言喂给 N_BLIND_BETTORS = 5 个独立的盲下注实例
     （只给标签和这条断言，不给任何数据）
  2. 各给一个先验概率 p_k，取平均 p̄ 和标准差 s
  3. 判定：
       p̄ > 0.80  → 太显然，拒绝（没有信息）
       p̄ < 0.20  → 自己都不信，拒绝（是在凑数）
       else      → 接受
       s > 0.25  → 额外标记为 high_information（实例之间分歧大 = 最有价值）
  4. 副产品：p̄ 就是 M6 计算 e 值需要的 prior_p
```

**这一步一举两得**：既过滤掉空洞断言，又产出了下注概率。

**验收**：人为写一条"IC 会大于 −1"，本检验必须拒绝它（p̄ 应接近 1.0）。

---

### M3.4 `register_structure(labels, assertions, exprs, coverage) -> Structure`

**目的**：注册一份档案，做四项检查。

| # | 检查 | 说明 |
|---|---|---|
| 1 | **标签必须先查词表** | 造新词必须显式标记 `is_new=True` 并说明理由。不禁止，但新词在统计模型里会被强烈压缩（`n_used=1`） |
| 2 | **标签取值必须能推出至少一个可观测条件** | ✗ `friction="注意力有限"`（推不出条件）<br>✓ `friction="关注带宽在同日事件数多时收紧"`（直接生成候选调节变量） |
| 3 | **槽位数固定，不许扩栏** | 每增加一个槽位，所有历史档案在那一栏都是缺失值，跨研究的记忆当场作废。要补充信息就加一条断言——**断言空间是开放的** |
| 4 | **断言通过 `compose_assertions` 的硬要求与空洞性检验** | |

---

### M3.5 `freeze_structure(s) -> hash`

**目的**：冻结档案，使其之后只能被检验、不能被修改。

**算法**：
```
1. 序列化 structure.yaml（不含 hash 字段本身）
2. hash = sha256(content + previous_structure_hash)
3. 写入 hash 与 frozen_at 时间戳
4. 追加到 chain.log
```

**为什么用哈希链而不是单个哈希**：单个哈希只能证明"这份文件从计算哈希那一刻起没变"，但如果有人同时改了文件和哈希就发现不了。链式结构使得**修改任何一份历史档案，都会让其后所有档案的校验失败**。成本几乎为零，但让篡改可检测。

**验收**：修改任一历史 `structure.yaml` 的一个字符，链校验必须报错。


---

## M4 测量层（`compute.py`）

> **职责**：把一个结构在数据上的表现完整刻画出来。
> **这一层不做任何判断**，只负责"看清楚它长什么样"。
> **它有四个下游消费者**：判别层（M5）、发现层（M7，输入就是 ψᵢ）、档案（附录 C）、组合层（M10）。

**一个重要的认识**：判别只有四把刀，而刀只输出 pass/fail，几乎不含信息。所以**测量层是系统里唯一的信息来源**。它的输出必须以数值落盘（`panel.parquet`），不只是出图——因为它是下游的输入，不是给人看的报告。

### M4.1 `measure_panel(structure, data, label_kind="industry_resid") -> Panel`

**在哪段数据上跑**：探索段 A（M1.5）。确认段 B 只有刀④能碰。

**子函数与产出**：

| 函数 | 产出 | 用途与备注 |
|---|---|---|
| `ic_tb_curve` | 各 horizon 的 IC 与分组收益：均值、5/95 分位、标准差、偏度、正 IC 比例、方向胜率 | 主指标。每个数字旁边必须标 n_eff |
| `monthly_ic_curve` | 月度 IC 均值曲线 + 5/95 分位带 | 看效应是否集中在少数几段时间 |
| `contribution_conc` | 因子值 × 未来行业残差收益作为绝对贡献，排序后 top 5/10/20% 的占比时间曲线 | 4 个 horizon × 3 条曲线。**高集中度 = 这不是结构，是几个事件** |
| `layered_ic` | ① 信号值域按 1% 宽度滑窗，逐窗算滑动平均 IC 与标准差<br>② 按值域分层，画各层的月度 IC 时序 | 看**剂量关系的形状**，是 P1 断言的判定依据 |
| `hac_t` | Newey-West t 值与置信区间 | **滞后阶必须 ≥ horizon**（标签重叠）。T < 200 时改用块 bootstrap 分位数，不用正态近似 |
| `hit_rate_curve` | 方向命中率，按天颗粒度，两条曲线（命中数 / 总数） | |
| `car_event` | 事件研究：窗口 [−10,+20]，分 10 层 | **事前窗口必须平坦**——这是最强的前视探测器 |
| `cost_sensitivity` | 在冻结参考持仓和费用规则下扫描成本情景 | 输出净收益、Sharpe、换手和 `cost_tol_bp`；IC/TB 单独报告 |
| `influence_decomposition` | **逐观测的 Ψᵢ** | 见 M4.2。发现层的唯一输入 |

**`car_event` 的平坦性判据**：事前 10 日累计收益的 `|t| < 1.5`，且逐日无单调趋势（Mann-Kendall `p > 0.1`）。

---

### M4.2 `influence_decomposition(f, r, dates) -> Psi`

**目的**：把 IC 这个汇总统计量分解到每一个观测上，得到"这个观测把 IC 往哪个方向拉了多少"。

**为什么需要它**：发现层（M7）要回答"这个因子在哪里失效"。

如果只用二值的"预测对/错"，会丢掉两类信息：
1. 丢掉了幅度——错得多和错得少是不同的
2. **会把"效应方向错了"和"那里噪声大"混为一谈**——而这两者的正确处理完全相反（M7.3）

影响函数是这个问题的标准工具。

#### 数学

Pearson 相关系数的影响函数（Devlin–Gnanadesikan–Kettenring）：

```
f̃ᵢ = (fᵢ − f̄) / σ_f
r̃ᵢ = (rᵢ − r̄) / σ_r

ψᵢ = f̃ᵢ · r̃ᵢ − (ρ/2) · (f̃ᵢ² + r̃ᵢ²)
```

两个性质：
```
mean(ψ) ≈ 0                 一阶条件
Var(ÎC) ≈ Var(ψ) / n        方差关系
```

直觉：第一项 `f̃ᵢr̃ᵢ` 是这个观测对协方差的贡献；第二项是对两个方差的贡献的修正（因为相关系数是标准化的，方差变大会稀释相关）。

#### 两层结构（必须写清楚，否则量纲会错）

```
第一层（日内截面）：
  对交易日 d，用当日的 f̄_d, σ_f,d, r̄_d, σ_r,d, ρ_d 计算
      ψ_{i,d} = f̃ᵢ·r̃ᵢ − (ρ_d/2)(f̃ᵢ² + r̃ᵢ²)
  满足 mean_i(ψ_{i,d}) ≈ 0，且 Var(ÎC_d) ≈ Var_i(ψ)/n_d

第二层（跨日聚合）：
  最终统计量是 mean_d(ÎC_d)，所以观测 i 对它的总影响是
      Ψᵢ = (ρ_{d(i)} + ψ_{i,d(i)}) / (n_{d(i)} × T)
  其中 n_d 是 d 日的截面样本数，T 是总天数；ρ_d 是该日截面 IC，ψ 是均值为零的一阶影响项
```

**发现层用的是 Ψᵢ，不是 ψ_{i,d}。**

用第一层的量会让不同日期的观测不可比——截面样本数每天不同（4000 vs 3000），同样大的 `ψ_{i,d}` 在小截面日对最终 IC 的影响更大。不除以 `n_d` 会**把"那天股票少"误当成"那天效应强"**。

#### 难点与解法

| 难点 | 解法 |
|---|---|
| 秩相关（Spearman）的影响函数复杂得多 | 对**秩变换后**的变量套用同一个 Pearson 公式，渐近正确 |
| Ψᵢ 在极端观测上非常大，直接喂给森林会被少数点主导 | 进入 M7 之前做 1%/99% 缩尾。但报告里同时给出**未缩尾**的贡献集中度——那里就是要看极端值 |
| 某日截面样本太少时 ρ_d 不稳 | `n_d < 100` 的日期整天剔除，并记录剔除比例 |

**验收**：把 Ψᵢ 按全样本求和，必须（数值上）等于 `mean_d(IC_d)`；逐日的 `Σ_i ψ_{i,d}` 仍应接近零。影响贡献和日均 IC 不能混为同一个中心化量。

---

### M4.3 `panel_signature(panel) -> vector`

**目的**：把面板压缩成一个同构的定长向量（曲线形状、集中度模式、衰减速度、月度稳定性等）。

**用途**：因为所有结构的面板结构相同，**面板本身就成为一种跨结构的相似性度量**，比信号相关更结构化——它能识别"这两个因子虽然相关不高，但衰减模式和集中度模式一模一样，可能是同一个东西"。

**被调用**：`relation_matrix`（M10.1）的第 4 层度量、`llm_inducer`（M11.5）找同类。

### M4.4 `regime_report(panel, state_spec) -> RegimeReport`

**目的**：识别价格走势和因子效应随时间的结构变化，并把它与结构本身的失败分开。状态指标、时间块长度和变化阈值必须事前登记；输出每个时间块的 IC、TB、波动、相关结构和状态标签。报告用于解释 B 段与 A 段差异，不得事后选择有利区间或改变正式检验样本。

---

## M5 判别层（`compute.py`）

> **职责**：回答"这个结果能不能信"。

### 设计要点：为什么只有四把刀

测量层产出二十几个数字，但它们是**同一次测量的不同切片**。高分辨率是好事，但它回答不了"这个效应是不是假的"——你把 IC 切成两百个数，伪影也会被切成两百个数。

所以判别只有四把刀，各自排除一种**不同性质**的错误：

| 刀 | 排除什么 | ✗ 之后的动作 | 用哪段数据 |
|---|---|---|---|
| ① 安慰剂 | 效应是**假的** | 效应**作废**，回数据层 | 探索段 A |
| ② 断言 | 效应真，但**原因不是你说的** | 效应**保留**，重写标签、开新 ID | 探索段 A |
| ③ 增量 | 真的、原因也对，但**早就有了** | 回退，父层即终点 | 探索段 A |
| ④ 正交矩 | 搜索出来的条件其实**不成立** | UNDECIDABLE 或 FAIL | **确认段 B** |

**刀①和刀②的动作相反**（作废 vs 保留）——这就是四把刀绝不能聚合成一个综合分数的根本原因。任何加权求和都会把这个方向信息毁掉。

### 四把刀什么时候用（时间线）

```
【冻结档案】
      ↓
【探索段 A】  measure_panel → 刀① → 刀② → 刀③
      ↓                      ← 每个格子都跑这三把
   结算赌本 → 三态判定
      ↓
【discover】  在 A 上搜出候选调节变量 z（只拿变量名）
      ↓
   【缝】写子结构的标签与断言并冻结（此时 B 未被读取）
      ↓
【确认段 B】  刀④ ICM
      ↓                      ← 只有搜索出来的子结构才跑这一把
   子结构的三态判定
```

**关键区别**：

| | 人/LLM 从地图直接写的格子 | `discover` 搜索出来的子结构 |
|---|---|---|
| 用哪份数据 | 只用探索段 A | 搜索在 A，检验在 B |
| 跑哪几把刀 | ①②③ | ①②③（在 A）+ **④（在 B）** |
| 为什么 | 没有搜索，无需为搜索付费 | 搜索制造假阳性，代价由"丢掉 A" 支付 |

所以**刀④不是第四道普通关卡，它是给搜索出来的东西专门加的一道**。

---

### M5.1 `gate_decidable(structure, data, horizon) -> bool`

**目的**：判定力闸门。有效样本量不足时直接判 UNDECIDABLE，四把刀都不出鞘，不消耗任何检验额度。

**背景**：区分"没有证据"和"有反面证据"。混淆这两者会让你去修一个本来没问题的逻辑——这是研究资源最常见的浪费方式。

**必须用这个结构自己的 n_eff，不是全 panel 的。**

```
n_eff = compute_n_eff(panel, factor, horizon, mask=structure.coverage)
```

一个只在 15% 样本上有意见的结构（例如只在小市值、或只在涨停次日），它的有效截面数也大约只有 15%。用全 panel 的 n_eff 会让闸门要么全放行、要么全拦截，失去意义。

**阈值怎么定（不拍脑袋）**：反解功效公式。在功效 0.8、显著性 0.05 下检出最小效应量 τ_min，需要：

```
n_eff ≥ (z_{0.975} + z_{0.8})² / τ_min² = 7.85 / τ_min²
```

反过来，给定 n_eff，**可检出的最小效应是 `τ_min = 2.8/√n_eff`**：

| 结构覆盖度 | n_eff（以 A 段全覆盖 2880 为基准） | 可检出的最小 IC |
|---|---|---|
| 100% | 2880 | 0.052 |
| 30% | 864 | 0.095 |
| 15% | 432 | 0.135 |

**这个计算会让你发现窄覆盖的结构本来就判不了。** 这不是坏消息，是诚实的先验体检。

它也解释了 M10.6 为什么强调"三个各覆盖 20% 的结构拼起来"优于"一个覆盖 100% 的弱因子"——覆盖度既决定信号强度，也决定**你有没有资格下结论**。

**闸门规则**：不写死通用 `n_eff` 下限。根据事前登记的最小有意义效应、该结构覆盖度、有效日期数和统计量自身的标准误计算可判定性；无法达到功效要求时返回 UNDECIDABLE，并保留重开条件。

---

### M5.2 `blade_placebo(structure, data) -> Result`

**目的**：**第一把刀，排除"效应是假的"。**

**为什么它永远排第一**：分样本（M7）对伪影**完全无效**——伪影在 A 段和 B 段同样存在。所以只有安慰剂能挡住它。刀①不过，后面全部不解读、不打印。

#### 三种安慰剂，各跑 N_PLACEBO = 500 次

**① 日内打乱股票标签**

在**每一个交易日内部**打乱 symbol 与因子值的配对。

**绝不能跨日打乱。** 跨日打乱会同时破坏时间结构，得到的零分布过于宽松——等于把"因子有预测力"和"数据有时间结构"混在一起检验，你会误以为通过了。

**② 随机时间平移**

整体平移 k 个交易日，`|k| > horizon` 且平移后仍在样本内。这检验的是"因子与收益的对应关系是否真的在这个时点"。

**③ 逐股票相位随机化**

对**每只股票**的因子时间序列做傅里叶变换，随机化相位后逆变换。

这保留了该股票因子的**自相关结构与边际分布**，只打乱它与收益的时序对应关系。比白噪声严格得多，因为很多假 IC 恰恰来自"因子有持续性 + 收益有持续性"这一巧合。

**必须逐股票做，不是对整个面板做一次**，否则会破坏截面结构。

#### 判据

真实统计量在安慰剂分布中的分位 > 0.99（单侧）。

#### 难点与解法

| 难点 | 解法 |
|---|---|
| 计算量大（500 次 × 每个格子） | 安慰剂只跑单个 horizon（h=5）、只用探索段的一半。**它是筛子不是精测** |
| 三种安慰剂结论不一致怎么办 | 取最严格的（分位最低的）。任一不过即判失败 |

#### 副产品

安慰剂分布同时用于估计每条断言的零假设概率 `p0`（M6.1）。这是必须的——`p0` 不能拍 0.5。

---

### M5.3 `blade_assertion(structure, panel) -> Result`

**目的**：**第二把刀，排除"效应真但原因不对"。**

**步骤**：逐条判定 P1..Pn → `{hold, violated, untested}`。每条断言冻结 `statistic`、`direction`、`tolerance`、`evaluation_horizons`、`scope` 和三态判定规则；断言必须同时记录其机制归因路径，不能只写“效应更强”。

#### 各类断言的判定方法

| 断言类型 | 判定方法 |
|---|---|
| `monotone_up/down`（形状） | 分层效应序列做 Mann-Kendall 趋势检验 + 检查各层置信区间 |
| `saturating` | 拟合单调约束样条 vs 线性，比较；或检查高层的边际增量是否显著小于低层 |
| `U`/`reversal` | 检查是否存在两层区间不重叠且方向相反 |
| `peak_horizon ∈ [a,b]` | IC 曲线的最大值位置；若曲线平坦（最大值与次大值区间重叠），判 untested |
| `effect_sign = ±1` | 先统一信号方向，再对方向统一后的效应做 HAC/块 bootstrap 检验 |
| `ic_difference(高组, 低组) > δ`（旁证） | 两组分别算 IC，用块 bootstrap 检验预先冻结的差值阈值 `δ` |

**形状判定的三态输出**：
```
单调    ：Mann-Kendall p < 0.1 且方向与断言一致  → hold
非单调  ：存在两层置信区间不重叠且方向相反      → violated
判不了  ：其余                                   → untested
```

#### 关键区分：`untested` 与 `violated` 必须分开

- `untested` = 数据不足以判定这一条 → **不参与赌本结算**
- `violated` = 数据说这一条是错的 → **是真证据，参与结算**

混在一起会让"数据缺失"变成"机制被推翻"，那是错误的信号。

**归因规则**：只有事前冻结的旁证、形状和方向共同支持同一机制路径时，才可记录“机制与证据一致”；单独的高 IC 或高 TB 只能说明预测关系，不能自动归因。

#### 与第一把刀的关系

两者的后续动作**相反**：

- 安慰剂不过 → **效应作废**，回数据层查前视/伪影，这一格重来
- 断言不过 → **效应保留**，但机制解释被证伪 → 重写标签、开新 ID，进 `law.md` 的 explanation 类

这个差别是四把刀不能聚合成一个分数的根本原因。

---

### M5.4 `blade_increment(structure, library) -> Result`

**目的**：**第三把刀，排除"这个发现早就被已有的覆盖了"。**

**步骤**：把新结构的信号对已有结构库做回归，取残差，看残差还有多少 IC。

**难点**：库大了之后必然显著变小（多重共线），最后什么都过不了。
**解法**：不用全库，只用 `relation_matrix`（M10.1）中**覆盖重叠 > 20%** 的子集做回归。覆盖不相交的结构不构成冗余。

**注意**：提案期已经用 `coverage_overlap` 做过一次免费初筛（不碰 y），这一把是花过钱之后的确认。

---

### M5.5 `blade_icm(structure, confirm_sample, h_func) -> (p_value, e_value)`

**目的**：**第四把刀，只对搜索出来的结构启用**——在确认段 B 上检验条件矩条件。

#### 要检验什么

你有因子 `f`、收益 `r`、候选调节变量 `z`（比如 amihud）。子结构声称：**f 的效应在 z 高的地方更强**。

零假设是"不，效应不随 z 变化"。

#### 数学推导

**第一步：正交化（partialling out）**

先把已知的东西剔掉，只留下待检验的部分。用交叉拟合估两个 nuisance 函数：

```
m(X) = E[r | X]        X = 控制变量（市值、行业、波动率等）
e(X) = E[f | X]
```

得到残差：
```
r̃ᵢ = rᵢ − m̂(Xᵢ)
f̃ᵢ = fᵢ − ê(Xᵢ)
```

**第二步：效应修饰模型**

```
r̃ᵢ = (τ₀ + τ₁·h(zᵢ))·f̃ᵢ + εᵢ
```

`h(z)` 是**门函数**，在探索段就已经定死，例如：
```
h(z) = 1{z > c} − 1{z ≤ c}          c 是冻结切点
```

零假设：`H₀ : τ₁ = 0`

**第三步：等价的矩条件**

```
E[ψ] = 0,   其中 ψ = (r̃ − τ₀·f̃)·f̃·h(z)
```

**为什么写成矩条件而不是直接做回归**：矩条件不需要假设误差同方差、不需要假设模型完全正确，只需要这一个期望为零。这是它在依赖数据上更稳健的原因。

**第四步：统计量**

```
T = (1/√n) Σᵢ ψ̂ᵢ
```

在 H₀ 下 T 渐近正态，但方差不好算——因为 ψᵢ 之间有依赖（同一天的股票相关、相邻天的标签重叠）。

**第五步：块级乘子 bootstrap**

重复 `B' = 1000` 次：
```
T*_b = (1/√n) Σᵢ ξ_{block(i)} · ψ̂ᵢ ,     ξ ~ N(0,1)
```

**关键在于 ξ 的下标是 `block(i)` 而不是 `i`：同一个块里的所有观测共用同一个乘子。**

**为什么必须这样**：
```
Var(T) = (1/n)[ Σᵢ Var(ψᵢ) + Σ_{i≠j} Cov(ψᵢ, ψⱼ) ]
```
第二项是正的（同日、同窗口的 ψ 正相关）。若 ξ 逐观测独立抽，bootstrap 分布里的 Cov 项被抹成零 → **方差被低估 → 分布太窄 → p 值偏小 → 系统性制造假阳性**。

块级共用乘子把块内的协方差结构保留了下来。

**块的定义**：连续 `max(BOOT_BLOCK_DAYS, h)` 个交易日为一块，块内所有股票所有观测共用一个 ξ。

**第六步**：
```
p = (1/B') Σ_b 1{|T*_b| ≥ |T|}
e = calibrate_p_to_e(p)          见 M6.4
```

#### 为什么这里可以用标准临界值

`h(z)` 这个门函数在探索段 A 上已经确定，所以在 B 段上这是一次**单点检验**，不是 sup 检验。**搜索的代价已经由"丢掉 A 段"支付过了**——这就是分样本设计的全部意义。

如果不分样本、在同一份数据上又搜又检验，就必须用 sup 型的临界值（把整个搜索空间算进去），那个临界值高得多，实际上什么都检不出来。

**验收**：在纯噪声数据上跑 200 次，p 值必须近似均匀分布（KS 检验 `p > 0.05`）。这是检验 bootstrap 实现是否正确的标准方法。

---

### M5.6 `run_blades(structure, data, library, confirm=None) -> Blades`

**算法**：短路求值。
```
gate_decidable → 不过则返回 UNDECIDABLE，后面全不跑
    ↓
blade_placebo  → 不过则返回，后面全部不解读、不打印
    ↓
blade_assertion
    ↓
blade_increment
    ↓
若 structure.lineage.origin 是 forest → 候选批次冻结后统一在 confirm 段运行 blade_icm；B 只读一次
```

**正式判定**：A 段用于测量、筛选和训练/验证折稳定性；B 段检验所有待确认候选。PASS 必须满足必要断言通过、数据质量无失败、且通过冻结候选批次的全库错误控制；任一关键旁证明确反向则不能被其他断言抵消。

### M5.7 `adjust_batch_errors(results, alpha=0.05) -> BatchDecision`

**目的**：对一次冻结候选批次的正式 B 段检验控制全库错误率。按候选结构的预先登记主检验 p 值执行 Holm step-down；断言和形状作为结构内部的必要条件，不在看到结果后重新分配权重。输出每个结构的调整后结论与批次审计信息。


---

## M6 证据层：e 值与赌本（`compute.py`）

> **职责**：用统一的、数学上正确的量度衡量"我们积累了多少证据"。
> 这一节从零开始讲，不假设读者知道 e 值。

### M6.0 从一个赌局讲起

有人拿出一枚硬币说"这是公平的"。这是**零假设 H₀**。

你有 1 元本金，可以按任意方式对每次抛掷下注。

**核心事实：如果硬币真的公平，没有任何下注策略能让你的期望财富增长。**

这是数学定理（鞅的性质），不是经验规律。你可以很聪明地下注，但期望财富永远是 1 元。

反过来说：

> **如果你的钱涨到了 20 倍，要么你运气极好，要么硬币不公平。**
> 而"运气极好"的概率是可以精确算的——**不超过 1/20 = 5%**。

这就是 e 值的全部内容。它把"证据"这个模糊概念变成了"赌本"这个可以累乘的量。

### M6.1 定义与保证

一个 **e 值**是任意满足下面两条的随机变量：

```
e ≥ 0
E_{H₀}[e] ≤ 1
```

**赌本**：`W_n = Π_{i≤n} eᵢ`，起始 `W₀ = 1`。首版只作为实验性记录，不作为正式 PASS/FAIL 判定。

**单次的保证**（Markov 不等式）：
```
P_{H₀}(e ≥ 1/α) ≤ α
```
所以 `e ≥ 20` 对应 `α = 0.05`。

**Ville 不等式**（这才是关键）：
```
P_{H₀}( ∃n : W_n ≥ 1/α ) ≤ α
```

注意是 **∃n —— 对所有时刻同时成立**。

### M6.2 为什么这个性质对我们特别重要

p 值只在**预先设定的时点看一次**才有效。

如果你边跑边看——跑一个格子、看结果、不显著就再加数据、显著就停——I 类错误会远超 5%。这叫 optional stopping 问题，是学术界"可复现性危机"的主要技术原因之一。

但**研究的真实工作方式就是反复看**：跑一个格子、看结果、决定要不要继续、加新的时间块、再看。

**只有在每一步满足可验证的条件期望约束时，e-process 才支持任意时点停止。** 本项目的块依赖、估计的 `p0` 和断言选择目前不自动满足这些条件，因此首版正式判定采用冻结候选批次的 p 值与 Holm 全库错误控制；赌本保留用于研究和展示。

三个直接后果：

**① α 预算不用拍。** 原来那种"总额 0.20、每次检验扣 0.010、读 holdout 扣 0.050"的记账是编的，数字没有出处。现在余额就是赌本，规则是数学定理。

**② 可以随时看。** 不需要事先声明"我要做多少次检验"。

**③ 相关的检验能合并。** 见 M6.5。

### M6.3 用断言下注

#### `estimate_p0(assertion, placebo_runs) -> float`

**目的**：估计零假设下这条断言碰巧成立的概率。

**绝不要拍 0.5。**

**算法**：复用经过交换性验证的安慰剂样本估计零假设分布；`p0` 的估计不直接进入正式错误控制。盲下注先用 Brier 分数、对数损失和可靠性曲线评估，待概率误差与未测试选择规则冻结后再决定是否启用 e 值结算。

例：断言是"剂量曲线单调向下"。若 500 个随机因子里有 175 个看起来也单调向下，则 `p0 = 0.35`。

**难点**：`p0` 本身有估计误差，用点估计会高估证据。
**解法**：取安慰剂分布的**置信上界**（保守方向，宁可低估证据）。500 次抽样下，`p0 = 0.35` 的 95% 上界约 0.39，用 0.39。

**验收**：对一个明显无信息的断言（"IC 不恰好等于 0"），`p0` 必须接近 1，其 e 值必须接近 1。

#### `shrink_bet(q_raw, lam) -> float`

**目的**：防止 LLM 过度自信导致赌本爆仓。

**问题**：若 LLM 给 `q = 0.95` 而断言实际失败：
```
e = (1 − 0.95) / (1 − 0.35) = 0.05 / 0.65 = 0.077
```
**一次就打掉 92% 的赌本。** 之后即使后面全对也很难爬回来。

**解法**：分数 Kelly。
```
q' = 0.5 + λ · (q − 0.5)
```
`λ = 0.5` 时，`q = 0.95` 被收缩到 `0.725`，最坏情况的 e 值从 0.077 变成 0.42。牺牲一半的增长率，换取抗爆仓。

**λ 怎么定（不是可调参数）**：用历史盲下注的校准曲线反解。
```
λ = 已实现命中率 对 声称概率 的回归斜率
```
如果 LLM 说 0.9 的事情实际只有 0.7 成立、说 0.6 的实际 0.55 成立，那条回归线的斜率就是它的可信度。**它由数据决定。**

冷启动时（没有历史）用 `λ = 0.5` 作为保守默认。

#### `bet_on_assertion(assertion, q_raw, p0, outcome) -> e`

```
q = shrink_bet(calibrate(q_raw))

e = q / p0                断言成立（hold）
e = (1 − q) / (1 − p0)    断言不成立（violated）
（untested 不参与结算）
```

#### 验证它确实是 e 值

在 H₀ 下，这条断言以概率 `p0` 成立。所以：

```
E_{H₀}[e] = p0 · (q/p0) + (1−p0) · ((1−q)/(1−p0))
          = q + (1−q)
          = 1
```

**恰好等于 1，对任何 q 都成立。**

这一点值得停下来体会：**下注比例 q 是完全自由的，但公平性是结构性保证的。**

LLM 给的概率再离谱，也**无法凭空制造证据**——它只能让自己赚得快或赔得快。这正是我们需要的性质：我们想让 LLM 表达信念（因为那是可解释性的来源），但不想让它的信念污染证据。

### M6.4 `calibrate_p_to_e(p) -> e`

**目的**：实验性地把 `blade_icm` 输出的 p 值转成 e 值；不得把该输出当作首版正式判定。

用标准校准族：
```
e = p^(−1/2) − 1
```

**警告：绝不自己发明校准函数。** 校准函数必须满足"对任意在 H₀ 下均匀分布的 p，`E[e] ≤ 1`"，随便写一个会破坏这个性质，整套保证失效。

### M6.5 `merge_e(e_list, weights, mode) -> e`

**合并规则，写死在代码里，不许有第三种：**

| 场景 | 操作 | 理由 |
|---|---|---|
| 跨时间块、跨独立实验 | **相乘** | 独立赌局的财富自然连乘 |
| 同一份数据上的多条断言 | **加权平均** | e 值的加权平均仍是 e 值，**即使各 e 之间任意相关** |

**第二条是 e 值相对 p 值最实用的优势。**

你的 P1–P5 必然高度相关（都基于同一个面板、同一个因子）。p 值在这种情况下要么用 Bonferroni（保守到几乎检不出任何东西），要么用 BH（需要独立性或特定相关结构的假设）。**e 值直接平均，没有任何附加条件。**

**代价：权重必须事先固定。**

看到结果之后再调权重（比如给表现好的断言加权）会破坏 `E[e] ≤ 1`。实现上：权重写进 `structure.yaml` 的冻结部分，默认等权；若要非等权，必须在冻结时就写好并说明理由。

**验收**：纯噪声数据跑 1000 次完整流程，W 超过 20 的比例必须 ≤ 5%。**这是整个证据层最重要的一个测试。**

### M6.6 完整数值例子

用 S-0001（M2-F3 短期反转）走一遍。

安慰剂跑完得到各条断言的 `p0`，盲下注给出 `q`（已收缩），跑数后得到结果：

| 断言 | 内容 | p0 | q | 结果 | e |
|---|---|---|---|---|---|
| P1 | 剂量单调向下 | 0.35 | 0.62 | hold | 0.62/0.35 = **1.77** |
| P2 | 峰值在 1–5 天 | 0.30 | 0.70 | hold | 0.70/0.30 = **2.33** |
| P3 | 效应符号为负 | 0.50 | 0.75 | hold | 0.75/0.50 = **1.50** |
| P4 | amihud 高处更强 | 0.40 | 0.60 | **violated** | 0.40/0.60 = **0.67** |
| P5 | 竞价价差宽处更强 | 0.42 | 0.55 | untested | — 不参与 |

同一份数据上的多条断言用加权平均，等权（P5 不参与，剩四条各 0.25）：

```
e_avg = (1.77 + 2.33 + 1.50 + 0.67) / 4 = 1.57
W = 1 × 1.57 = 1.57
```

**W = 1.57，远未达到 20 的门槛 → 判 UNDECIDABLE。**

意思是："这个格子看起来有点东西，但证据远不足以下结论。继续观察，**不需要任何多重检验修正**。"

明年数据积累了，在新的时间块上再结算一次，`W` 乘上新的 e 值。这就是"随时可看"的实际形态。

**注意 P4 violated 的双重含义**：它既拉低了赌本（e = 0.67 < 1），又是一个**有信息的失败**——旁证不成立意味着"效应可能是真的，但不是流动性提供这个机制"。所以它同时触发 M7.8 的"跨行换机制"算子。

### M6.7 `wealth_process` / `portfolio_wealth`

```
wealth_process(structure) -> W
    判定接口：
      W ≥ 20     → 可判 PASS
      W ≤ 0.05   → 判 FAIL
      介于两者   → UNDECIDABLE，继续观察不需要任何修正

portfolio_wealth(library) -> Trajectory
    全库总赌本轨迹（各结构的 W 按时间排列）
```

**全库赌本轨迹**直接回答"我们做了多少次检验、还剩多少证据额度"，而且它**数学上正确，不是记账约定**。这是路演可报的三个数字之一。

### M6.8 增长率分解（不是函数，是这一层的理论依据）

设真实的成立概率为 `P`，你按信念 `q` 下注：

```
E_P[log e] = P·log(q/p0) + (1−P)·log((1−q)/(1−p0))
```

加减 `P log P + (1−P) log(1−P)` 整理：

```
E_P[log e] = D_KL(P ‖ p0)  −  D_KL(P ‖ q)
             ╰──────────╯     ╰──────────╯
             数据里客观有       你的机制故事
             多少证据           错了多少
```

**第一项与你无关**，是这个数据集本身的性质——市场里到底有没有这个效应。
**第二项是你的信念离真相有多远**，`q = P` 时为零。

于是：**赌本涨得慢有两个可能的原因，而它们在数学上是分开的。**
- 第一项小 → 数据里没东西
- 第二项大 → 你的故事讲错了

这也给了"可解释性"一个严格定义：

> **一条机制陈述可解释，当且仅当按它下注能赚钱。**

这不是比喻，就是上式的第二项。这是本系统对可解释性的操作定义，也是它区别于"让 LLM 写一段解释"的根本处。


---

## M7 发现层（`compute.py`）

> **职责**：回答"下一步该研究什么"。
>
> **设计要点**：不预先枚举失败模式再写路由表（那只是把设计者的想象力写死），而是
> **方向由数据产生、机制由人写、正当性由另一半数据裁决**。
>
> **理论出处**：条件矩检验（Newey–Tauchen–White）；ICM（Bierens）；未知断点稳定性
> （Andrews 1993；Bai–Perron）；分样本推断（Chernozhukov 等 Generic ML）；
> 切片发现（SliceLine）；影响诊断（Cook；Belsley–Kuh–Welsch）。

### M7.1 `split_explore_confirm(data, mode, seed) -> (explore, confirm)`

具体日期见 M1.5。这里只讲两种模式的取舍。

| 模式 | 做法 | 优点 | 风险 |
|---|---|---|---|
| **`time`（主用）** | 前期探索 / 后期确认 | 确认样本天然是样本外，顺带就是序贯记分 | 两期市场状态可能不同，不显著时分不清是发现错了还是状态变了 |
| `cross` | 按**行业整块**分配（不是按代码奇偶） | 两半共享同一时间段，市场状态一致 | 同日收益高度相关，两半不独立，方差必须块聚类 |

**绝不能按行随机切**：同日样本会跨半，标签重叠会跨半，分样本就完全失效了。

**为什么不用 50/50**：探索侧只需要能对候选**排序**，确认侧才需要**统计功效**。所以 `EXPLORE_FRAC = 0.65`。但若 `power_budget` 显示确认侧功效不足，应降到 0.5 甚至 0.35。

---

### M7.2 `orthogonalize(sample, X_cols) -> residuals`

**目的**：去掉主效应与控制变量的影响，留下待检验的残差。

**算法**（交叉拟合 / cross-fitting）：
```
1. 把 sample 按块级 purged K 折切分（K=5）
2. 对每一折 k：
     在其余 K−1 折上训练 m̂(X) = E[r|X] 和 ê(X) = E[f|X]
     在第 k 折上预测，得到残差 r̃ = r − m̂(X), f̃ = f − ê(X)
3. 拼回完整的残差序列
```

**关键约束 1**：**探索段和确认段各自做交叉拟合，绝不共用在全样本上训练的 nuisance 模型。** 共用会把确认样本的信息泄漏回探索侧，分样本就白切了。

**关键约束 2**：折的划分必须是**块级且带 purge**，不是随机 K 折。随机 K 折会让同日样本跨折，nuisance 模型学到了它不该知道的东西。

**难点**：nuisance 模型用什么？
**解法**：用简单模型（岭回归或浅层 GBDT），因为这里只需要去掉主要的可解释部分，过拟合 nuisance 会把真信号也吸收掉。控制变量 X 建议只放：log 市值、行业哑变量、20 日波动率、log 换手率。

**验收**：把确认样本的标签整体打乱，重跑探索侧的搜索，候选排序必须不变。若变了，说明存在信息泄漏。

---

### M7.3 `variance_vs_mean_screen(psi, candidates) -> (mean_shift, variance_only)`

**目的**：**在搜索之前先分流。这是本模块最容易被忽略、但最重要的一步。**

#### 背景

把一个因子拆成一笔笔观测、找出它在哪里失效、再把失效点聚合起来找规律——这个直觉是对的，在计量经济学里叫**设定检验**：模型的残差如果在某个方向上不是零均值，模型就是错的，而那个方向就是缺失的变量。

但"失效点聚集"有**三种完全不同的成因**，处理方式完全相反：

| 成因 | 数学形式 | 含义 | 正确动作 |
|---|---|---|---|
| **均值误设** | `E[Ψ\|Z] ≠ 0` | 在 Z 上系统性偏一边 | **改逻辑**：Z 是缺失的调节变量 |
| **方差异质** | `E[Ψ\|Z] = 0` 但 `Var[Ψ\|Z]` 大 | 方向没错，只是那里噪声大 | **改仓位**：`w ∝ 1/σ`，不开新格子 |
| **标签污染** | 关系来自度量本身 | 那里的收益算得就不对 | **改数据层**，这一格作废 |

#### 第二种是绝大多数人踩的坑

失效点**必然**扎堆在高波动区，因为那里方差本来就大。

在那里加一个条件门，你不是发现了市场状态，**只是剔掉了噪声大的样本**——样本内 IC 必然上升，样本外必然崩溃。

**而且分样本挡不住它**：探索段和确认段的方差结构是一样的，高波动区在两边都同样"表现更好"。所以这一步**必须在森林之前**，不能指望后面的检验兜住。

#### 算法

```
1. 均值检验：
     把 Ψ 对候选变量 Z 做 5 分位分箱，各箱算 mean(Ψ)
     F 检验（块聚类方差）：各箱均值是否全为零
2. 方差检验：
     先去掉均值效应（Ψ' = Ψ − 分箱均值）
     对 Ψ'² 做 Breusch-Pagan / White 检验，看它是否随 Z 变化
3. 分流：
     均值显著            → 进入 mean_shift，走搜索流程
     仅方差显著          → 进入 variance_only，走仓位缩放
     都显著              → 两边都走
     都不显著            → 这个候选没信息，丢弃
```

#### 为什么必须用 Ψ 而不是"预测对错"

Ψ 已经是一阶展开后的**可加量**，均值与方差检验不会互相污染。

如果用二值的"预测对/错"，"方向错得多"和"方向对但幅度小"会被编码成同一个值，均值和方差就纠缠在一起了。

**验收**：合成数据注入纯方差异质（均值恰为零、方差随 Z 变化），第 1 步必须不显著、第 2 步必须显著。反之注入纯均值偏移，第 1 步显著、第 2 步不显著。

#### 三种成因之后的完整处理

**① 均值误设 → 走完整的 `discover` 流程**

候选进森林 → 缝里写子结构的标签和断言 → 在 B 段跑刀④。三种结果：
- 过 → 新格子进库
- 不过 → UNDECIDABLE，写 reopen 条件
- 补不出机制 → 丢弃，且**不产生"我们发现了一个 regime"这种说法**

**② 方差异质 → 不开新格子，做两件事**

**(a) 写一条边界层规律：**
```yaml
kind: 边界层
claim: "该效应在高波动区方向不变但噪声显著更大"
scope_predicate: realized_vol_pct > 70
confirmed: false        # 除非在 B 段验过
evidence: [S-0001.variance_screen]
reopen: "若换用波动率调整后的标签，此结论需重查"
```

**(b) 在权重上做仓位缩放：**

「仓位缩放」的意思是：**方向和覆盖都不变，只把噪声大的地方的持仓规模调小。**

理由是**夏普比而不是收益率**。如果某个区域效应量 τ 一样、但收益波动 σ 大一倍，那里每承担一单位风险拿到的收益只有一半。等权持有 = 在那里过度承担风险而没有额外补偿。

```
w_i = gate(z_i) × sign × strength(f_i) × clip(σ̄ / σ̂_i , 0.3, 3.0)
      ╰────────╯   ╰──╯   ╰──────────╯   ╰──────────────────────╯
      覆盖，不变   方向   信号强度        ★ 新增的缩放因子
```

- `σ̂_i` 用该股票的 20 日已实现波动率（`realized_vol`，注意它在变量表里的 `causal_role` 是 **moderator 而不是 signal**，就是为了这个用途）
- `σ̄` 是截面中位数，起归一化作用
- **必须 clip**，否则极低波动的股票会拿到荒谬的权重

**这是截面版的波动率目标化，不是一个新因子**，所以：不消耗证据额度、不进设计矩阵、不需要走四把刀。

**③ 标签污染 → 这一格整个作废**

典型例子：`Z` = 是否触及涨跌停，而涨跌停股票的 `r_h` 因为买不进本来就算得不对。

处理：回数据层修（补上 `not_limit_locked` 过滤），修完重跑。

写进**变量注册表的备注**，**不进 `law.md`**——这是关于你自己代码的发现，不是关于市场的知识。混进去会稀释账本。

---

### M7.4 `forest_propose(explore, psi, cut_table, candidates) -> list[(z_name, freq)]`

**目的**：在探索段上找出条件预测关系的异质性，即**收益对信号的条件斜率**在哪些变量上不同；它是候选搜索器，不自动提供因果识别。

#### 算法

```
1. R-Learner 正交化（M7.2），目标变量是收益对信号的条件斜率残差
2. 因果森林：
     - honest split：把样本按时间块对半，一半用来决定分裂点，
       另一半用来估计叶内效应。**绝不能用同一批样本既选分裂又估效应**
     - max_depth = 2
     - 每个候选变量只在 QUANTILE_CUTS = 5 个分位点上搜索分裂
     - n_trees = 500
     - min_leaf 用 n_eff 定义（见下）
3. 森林计票：统计每个变量被选为分裂变量的频率
```

#### 三处关键设计

**① honest split 是必须的，不是可选优化**

如果用同一批样本既决定"在哪里分裂"又估计"分裂后两边的效应差"，那个效应差会被系统性高估——你是挑了最大差异的地方再去测那个差异。honest split 把两件事分开，估计才是无偏的。

**实现细节**：对半必须**按时间块**，不是按样本随机。随机对半会让同日样本跨半。

**② 每个变量只在 5 个分位点上搜索**

这是防过拟合的关键一步。如果允许在任意切点搜索，一个连续变量提供的有效自由度是 O(n)；限制在 5 个冻结的分位点上，自由度降到 4。

**切点必须来自 `freeze_cuts`（M2.3）**，不能在探索段现算——现算等于又引入了一批自由参数。

**③ min_leaf 用 n_eff 定义，不用行数**

```
叶内至少 N_BLOCK 个独立时间块（建议 20）
且至少 N_STOCK 只不同股票（建议 100）
```

用行数会让森林分裂到"某一个月的某几只股票"这种叶子，那里的 5000 行其实只有 1 个独立观测。

#### 另外三条硬约束

**输出解释**：森林报告条件斜率差和入选频率；逐观测影响贡献 `Ψ` 只用于探索诊断和均值/方差分流，不与条件斜率估计混作同一统计量。

**候选变量空间必须先由 `friction` 标签收窄到 3–5 个。** 三五个从机制推出的候选，与四十个随便扔进去的列，**统计功效差一个数量级**（因为后面 `repeat_splits` 的多重比较代价随候选数增长）。逻辑先行在这里是**提高统计功效的手段**，不是哲学立场。

**输出只有变量名和入选频率，不含效应大小和方向。** 原因见 M11.1——这是整个发现流程的意义所在。

---

### M7.5 `repeat_splits(data, candidates, B) -> (p_median, selection_freq)`

**目的**：单次切分不稳定——切法一变，候选就可能换。

**算法**：
```
做 B = 20 次不同的探索/确认切分（不同的时间切点或不同的行业分配）
每次：forest_propose → 得到最强候选 → blade_icm → 得到 p 值
p 值取 B 次的中位数 × 2       （保守修正，有理论保证）
selection_freq = 每个候选在 B 次中被选中的比例
```

**副产品比 p 值更有用**：**入选频率**。

某个变量只在 3/20 次被提名，那它就是切分噪声，不管那一次的 p 值多好看。这个频率写进档案血缘（`lineage.origin`），也写进最终的结构卡（M10.8）——**它比 p 值更能说明稳健性**。

**难点**：计算量大（20 次 × 森林）。
**解法**：B 次共用同一份交叉拟合的**折结构**（但不共用拟合值）；森林树数从 2000 降到 500（这里只要排序不要精度）。

---

### M7.6 `power_budget(n_eff, explore_frac, tau_main) -> Report`

**目的**：**先算账再动手。建议这是全项目第一个跑的函数。**

#### 关键修正：交互检验比主效应难得多，不止"样本减半"这一层

交互检验问的是"**两个子组的效应是否不同**"，这是一个**差值**。设两组各占确认样本的一半：

```
σ(每组)  = 1/√(n_eff'/2)
σ(差值)  = √(σ₁² + σ₂²) = √(2/(n_eff'/2)) = 2/√n_eff'

80% 功效需要：τ_int ≥ 2.8 × 2/√n_eff' = 5.6/√n_eff'
```

对比主效应只需要 `τ_main ≥ 2.8/√n_eff`。

**所以交互检验的门槛大约是主效应的 `2/√(1−explore_frac)` 倍：**
- `explore_frac = 0.65` → 3.4 倍
- `explore_frac = 0.50` → 2.8 倍

#### 本数据集的账（industry_resid 口径，ρ̄≈0.10，A 段 n_eff≈2880）

| explore_frac | 确认侧 n_eff' | 可检出的最小交互 τ_int | 对比 τ_main≈0.05 |
|---|---|---|---|
| 0.65 | 1510 | **0.144** | 2.9× |
| 0.50 | 2210 | **0.119** | 2.4× |
| 0.35 | 2870 | **0.105** | 2.1× |

**结论很硬：在全 panel 上，交互效应几乎不可能在确认样本上显著。**

这不是方法问题，是数据问题，**必须写进报告而不是掩盖**。

#### 四条缓解，按有效性排序

**1. 优先做 F7 事件格。**
事件样本的 ρ̄ 远低于全 panel——不同事件在不同日期发生，横截面相关大幅下降，n_eff 可能高一个量级。

**M5 交易约束、M6 被动资金、M9 不确定性这三行几乎全是事件格，它们是功效上的退路。**

**2. 候选先由 `friction` 收窄到 3–5 个。**
这不改变单次检验的门槛，但大幅减少需要的检验次数，从而让每一次都能用完整的证据额度。

**3. `explore_frac` 降到 0.5 甚至 0.35。** 探索侧只需要能排序。

**4. 接受 UNDECIDABLE 是常态**，写 reopen 条件等数据积累。

#### 如果算出来 τ_int 远大于 τ_main（大概率会）

**不要降低阈值硬找方向。** 正确的反应是把研究重心从"找调节变量"移到"找主效应"和"做事件研究"，并在报告里明确说明**这个数据量下发现层能做到什么、不能做到什么**。

这本身就是一个诚实且有价值的结论，而且是绝大多数同类工作不会报告的。

---

### M7.7 `discover(structure, panel, data, library) -> list[Structure]`

**目的**：本模块的主流程。

```
1. explore, confirm = split_explore_confirm(data, mode="time", seed)

2. Psi = winsorize(panel.psi, 1%, 99%)

3. mean_shift, var_only = variance_vs_mean_screen(Psi, candidates)
     若只有方差异质 → 写边界层规律 + 仓位缩放，**不开新格子**，结束

4. cands = forest_propose(explore, Psi, cut_table, mean_shift)
   p_med, freq = repeat_splits(...)
     不显著 → **UNDECIDABLE，没有新方向，不开新格子**
              （这是最常见的正确结果，不是失败）
     显著   → 取最强的 z 作为提案（**只有变量名**）

5. ★★★ 【缝】在这里写机制 ★★★
   此时确认样本的任何结果尚未被读取。
     labels     = llm_proposer(z_name, vocab, law_book, map)     # M11.1
     assertions = compose_assertions(labels, z)                   # M3.3
                  必须包含"z 在高区应该是什么方向"这条断言
     child      = register_structure(...)                         # M3.4
     freeze_structure(child)                                      # M3.5
     bets       = llm_blind_bettor(child.labels, child.assertions) # M11.3

   补不出完整标签与断言 → **丢弃，且不产生"我们发现了一个市场状态"这种说法**

6. 候选批次全部冻结后，统一解锁 confirm，并只读取一次；在其上同时检验三样：
     ① blade_icm(child, confirm, h_func)      主效应
     ② 形状断言是否符合
     ③ **旁证断言是否出现**    ← 最关键的一条

   只过 ① 得到的是"这里效应更强"。
   三条都过得到的才是"这里效应更强**且原因是我说的那个**"。
   —— 这一步把「找到一个变量」变成了「检验一个机制」。

7. 记录实验性赌本；用冻结批次的 Holm 调整结果和必要断言执行正式三态判定
```

#### 难点：如何在工程上**保证**第 5 步没有偷看确认样本

**解法（三重）**：
1. 确认段存放在**独立目录**，读取必须经 `audit_read(path, reason)` 包装并写日志
2. `discover` 内部在第 5 步之前对该目录持"未读"锁，第 6 步才解锁
3. 审计日志与哈希链一起进档案，`blades.json` 里必须有这条记录

**验收**：故意在第 5 步读一次确认样本，审计必须报错并中止。

---

### M7.8 `evolve_operators(structure, assertion_matrix) -> list[Structure]`

**目的**：除"加条件"之外的另外三个演化方向。**这三个不需要搜索，读断言判定矩阵即可。**

| 算子 | 触发条件 | 动作 | 成本 |
|---|---|---|---|
| **横向：换表现形式** | 三个表达式符号不一致，或覆盖度太低 | 同一机制换地图上的另一列（F1↔F3↔F5…） | **最便宜且不增加复杂度，优先做** |
| **跨行：换机制** | 断言不成立（尤其旁证）但效应仍在 | 保留表达式、重写标签，开新 ID | 中 |
| **交叉：借条件** | 另一行已验证了某个条件 | 把 A 行验证过的条件移植到 B 行 | 提案期免费 |

#### 交叉算子值得单独说

**它是遗传算法里 crossover 的对应物，也是这套系统里唯一的"重组"操作。**

例：M2 流动性族已经验证了"效应只在小市值上成立"。那么问：**M1 注意力族是不是也只在小市值上成立？**

这个移植**自带可证伪预测**（应同向）：
- 移植成功 → 发现的不是 M2 的性质，而是"小市值这个环境"的性质。**这是一条更值钱的条件层规律**
- 移植失败 → 它确实是 M2 特有的

**两种结果都有信息，而且提案期完全免费**（不碰 y）。

**难点**：移植需要判断"这两个机制是否可能共享同一个条件"，这需要语义理解。
**解法**：由 `llm_proposer` 提出候选移植（它能看到 law.md 和地图），但移植后的子结构必须走完整套刀。

**规则**：横向走到尽头，再走纵向（加条件）。因为横向不增加复杂度，纵向增加。

---

### M7.9 `should_stop(structure, discovery) -> bool`

**两条终止条件，满足其一即停**：

**① 排除不了**：有效样本量不足以区分剩余候选 → UNDECIDABLE，写 reopen 条件。

**② 排除了也没用**：若区分两种解释**不会改变实际下单行为**，就不该花证据额度区分它。

**第 2 条的可操作判据**：
```
在两种解释下分别生成目标持仓向量 w_A, w_B
若 corr(w_A, w_B) > 0.9 且 |turnover_A − turnover_B| / turnover_A < 0.1
则判定为"不影响下单"，停止
```

例："这个效应到底是注意力还是流动性"——如果两种解释下你的持仓、方向、周期完全一样，那么区分它们是纯学术兴趣，不该花预算。

**第 2 条大约能砍掉一半的待办。**


---

## M8 一致性检查（`compute.py`）

> **职责**：回答"这些结论彼此相容吗"。
> **一个算法，六个用处。** 这是全项目性价比最高的模块，而且**它不依赖任何统计模型**——
> 因此在层次模型（M9，最高风险）失败时，它能承接一部分职能。

### M8.1 `balance_check(nodes, edge_fn, sign_fn, weight_fn) -> BalanceResult`

#### 背景：一个统一的问题形式

很多问题的形式都是"**同一个论断在多个语境下是否一致**"：

- 两个结构在覆盖交叠的区域里方向一致吗？
- 同一个机制的三个表达式符号一致吗？
- 一个结构在不同时间段符号一致吗？
- 共享同一个标签的几个结构方向一致吗？
- law.md 里的两条规律互相矛盾吗？

把论断的实例连成图，边的符号表示两个实例是否一致，问题就变成了图论问题。

#### 定理（Harary 符号图平衡）

```
符号图平衡  ⟺  不存在含奇数条负边的圈
            ⟺  节点可二分，使正边在组内、负边跨组
```

直觉：如果 A 与 B 一致、B 与 C 一致，那么 A 与 C 也应该一致。如果实测 A 与 C 相反，这三条边构成一个含 1 条负边（奇数）的三角形——**这就是矛盾**。

#### 接口

```
输入：
  nodes            论断的实例列表
  edge_fn(a,b)     两个实例是否可比较 → bool
  sign_fn(a,b)     是否一致 → +1 | −1 | None
                   （None = 证据不足，该边被剔除）
  weight_fn(a,b)   边的置信度 → float

输出：
  balanced           bool
  bipartition        {node: ±1}   平衡时的二分，即两个相反的分组
  cycles             list[Cycle]  受挫时，每个含奇数负边的圈及其节点交集
  frustration_index  float        要删掉多少加权边才能变平衡
```

#### 算法

```
1. 建图，丢弃 sign_fn 返回 None 的边
2. BFS 二染色（或并查集带奇偶标记）：
     给每个节点分配 σ ∈ {+1, −1}
     正边要求两端同色，负边要求两端异色
3. 遇到冲突即得到一个受挫圈，记录其边集与节点集
4. frustration_index：删边使图平衡的最小加权代价
复杂度：O(V + E)
```

#### 难点 1：符号估计有噪声，弱边会造出假的受挫圈

**解法**：`sign_fn` 只在 bootstrap 置信区间**不跨零**时返回 ±1，否则返回 `None`。

**宁可少边也不要假边**——一条假的负边会污染它所在的**所有**圈，让整张图看起来到处是矛盾。

**解释边界**：符号图是一个有价值的数学一致性结构，用于定位冲突、比较关系和生成待解释候选；图平衡不等于三个表达式同号，也不等于机制已被证明。`+,+,-` 的三角形仍可平衡，表达式同号必须另行检验。任何冲突都不能通过翻转已冻结的信号方向解决。

#### 难点 2：`frustration_index` 一般是 NP 难的

**解法**：本项目规模只有几十个节点，用整数规划精确求解即可（几秒）。超过 200 节点时退化为贪心近似，并在报告中标注"近似"。

**验收**：
1. 已知平衡的三节点系统加一条人为反号边 → 必须检出唯一受挫圈
2. 全正边图 → `balanced = True`，`frustration_index = 0`
3. 三角形两条负边 → 平衡（负边数为偶）；三条负边 → 受挫

---

### M8.2 六个调用点

| 函数 | 节点 | 边的符号含义 | 受挫意味着 |
|---|---|---|---|
| `combination_balance` | 已通过的结构 | 覆盖交叠区内方向是否一致 | 缺一个调节变量；**圈上结构的覆盖交集就是它起作用的区域** |
| `convergent_validity_balance` | 同一机制的三个表达式 | 符号是否一致 | 机制没被正确表达 → 走横向演化 |
| `regime_balance` | (结构, 时间块) | 该块内符号是否一致 | 存在市场状态结构 |
| `vocabulary_balance` | 共享同一标签取值的结构 | 符号是否一致 | 这个标签是多义的，该拆 |
| `lawbook_balance` | `law.md` 的条目 | 蕴含为正、矛盾为负 | 账本内部自相矛盾 |
| `cross_market_balance` | 同一机制在不同市场的实例 | 符号是否一致 | 机制不普适（v2 议题，本项目不做） |

#### 四点补充说明

**`convergent_validity_balance`**：系统要求"同一机制必须写出三个不同表达式且符号一致"，这条规则在这里显示为**三角形上的平衡性**——是同一个数学事实的特例。三个表达式两两比较，三条边，若含奇数负边则受挫。

**`regime_balance` 把发现顺序倒了过来。**

常规做法是先挑一个状态变量（比如波动率）再切分。这里是**先用二分找出哪些时间块属于同一组，再回头找什么变量能分开这两组**——即先发现状态的**存在性**，后命名它。

输出的 `bipartition` 直接作为发现层的目标：找一个变量使它对这个二分的解释力最强（一个简单的二分类问题）。

这给发现层提供了一个**与因果森林完全独立的入口**：森林找横截面调节变量，它找时间维分组。

**`vocabulary_balance` 降低架构的单点风险。**

层次模型（M9）是全项目风险最高的部件。而"某标签下的结构彼此符号矛盾，说明该标签该拆"这个判断，用符号图 O(V+E) 就能做，**完全不需要贝叶斯**。

**`lawbook_balance` 是通常没人做的一件事。**

`law.md` 会越写越长，而没有任何机制检查 L-0007 是否和 L-0023 矛盾。把受挫指数按时间画成曲线，就是"**这套知识体系的内部一致性随研究推进的变化**"——这是一个别人完全没有的报告指标。

边的符号由 `llm_inducer` 初判（哪两条规律的 scope 有交集、结论是否相容），人工确认后写入。

---

## M9 记忆层：层次模型（`compute.py`）

> **职责**：让"跑过的研究"影响"接下来研究什么"。
> **⚠ 这是全项目风险最高的模块，请先读 M9.3 的风险说明再决定实现到哪一步。**

### M9.1 数据长什么样

层次模型的输入是一张表，每行是"某个结构在某个时间块上的效应量"。

**时间块建议用季度**（A 段 1440 天 ≈ 24 个季度）。

| 结构 | 季度 | τ̂ | σ̂ | family | form |
|---|---|---|---|---|---|
| S-0001 | 2016Q3 | 0.041 | 0.019 | M2 | F3 |
| S-0001 | 2016Q4 | 0.028 | 0.021 | M2 | F3 |
| S-0001 | … | … | … | M2 | F3 |
| S-0002 | 2016Q3 | 0.015 | 0.023 | M2 | F1 |
| S-0003 | 2016Q3 | −0.031 | 0.020 | M1 | F3 |

- `τ̂[s,t]` = 结构 s 在第 t 季度的效应量 = **那个季度内每日截面 IC 的平均**
- `σ̂[s,t]` = 它的标准误，用块 bootstrap 在那个季度内算

20 个结构 × 24 个季度 ≈ **480 行**。这就是模型的输入。

### M9.2 `build_design_matrix(library) -> Matrix`

**目的**：把每份档案的标签摊成设计矩阵的一行。

**⚠ 关键设计：四个机制槽位折叠成一个 `family` 因子。**

理由见 M9.6：`agent` 与 `friction` 在机制上捆绑（散户总配注意力带宽，做市商总配存货风险），**在原理上不可分离**。展成四列会得到一个永远无法识别的模型。

所以设计矩阵只有两个因子：
```
family : M1..M10        （10 个水平）
form   : F1..F7         （7 个水平）
```

五个槽位仍然写在档案里——它们是给人和 LLM 读的语义，也是 `vocabulary_balance`（M8.2）的输入——**但进入统计模型的只有折叠后的 family 和 form**。

### M9.3 `fit_hierarchy(library, stage) -> Posterior`

**统计边界**：层次模型只用于跨结构记忆、先验预测和下一步探索，不自动授予结构 PASS，也不替代冻结批次的多重检验。相同股票日期产生的结构效应估计相关时，必须在后验诊断中报告；同一表达式换标签不计为新的独立证据。不可识别或诊断失败时返回 `UNIDENTIFIABLE`，不得解读点估计。

#### 模型

```
观测层：  τ̂[s,t] ~ N( τ[s,t], σ̂²[s,t] )
结构层：  τ[s,t] = μ + a_family(s) + a_form(s) + a_int(s) + b_t + u_s
```

**第一行是测量误差模型**：我们观测到的 τ̂ 是真值加噪声，**噪声大小 σ̂ 是已知的**（bootstrap 给的）。这是标准的元分析设定——把"这个估计有多准"作为已知信息喂进去，而不是让模型自己猜。

**第二行逐项**：

| 项 | 含义 | 几个 | 类型 |
|---|---|---|---|
| `μ` | 全局平均效应 | 1 | 固定效应 |
| `a_family` | 这个机制族的共同效应 | ≤10 | 随机效应，`~ N(0, σ_fam²)` |
| `a_form` | 这个表现形式的共同效应 | ≤7 | 随机效应，`~ N(0, σ_form²)` |
| `a_int` | 特定 family×form 组合的额外效应 | ≤70 | 随机效应，仅阶段 C 解锁 |
| `b_t` | 第 t 季度的市场共同效应 | 24 | 随机效应，`~ N(0, σ_b²)` |
| `u_s` | 这个结构特有的、上面都解释不了的部分 | 20 | 随机效应，`~ N(0, σ_u²)` |

#### "随机效应"到底是什么意思

不是"随机数"，是**这一组系数被假定来自同一个分布**。

对比一下：

**当作固定效应**：M6 族只有 1 个结构、IC = 0.08，你就直接估 `a_M6 = 0.08`——**完全相信一个观测**。

**当作随机效应**：模型会问"在 `σ_fam` 这个尺度下，0.08 有多可能只是噪声？"如果其他族的效应都在 0.02–0.04，`σ_fam` 就估得很小，于是 `a_M6` 会被**强烈拉向 0**，比如收缩到 0.03。

而 M2 族有 3 个结构、都在 0.02–0.04，一致性高、样本多，收缩就轻。

**这就是"部分池化"：观测少的被拉向整体均值，观测多的保留自己的信息，收缩强度由数据决定而不是人设。**

**它顺带解决了多重检验**：你测的第 9 个 M2 族结构会自动被压缩得更狠，因为前 8 个已经把 `σ_fam` 钉得很小了。**检验次数是通过后验进入判定的，不需要单独的计数器。**

#### ⚠ 风险（本项目最高）

即使折叠成两因子，20 个结构撑起 10 个族 + 7 个形式 + 24 个时间效应，仍然**接近欠识别**。直接拟合会得到一堆发散转移和贴着先验的后验，而你会**误以为学到了东西**。

#### 解法 A：分阶段解锁

| 库规模 | 拟合什么 | 能回答什么问题 |
|---|---|---|
| **< 10 个结构** | 只拟合 `μ + b_t + u_s` | **只有一个问题**：有没有共同的时间效应 |
| **10–30** | 加入 `a_family` 与 `a_form` | 族效应与形式效应 |
| **> 30** | 加入 `a_family × a_form` 交互 | 特定组合的额外效应 |

**不要试图分别估四个机制槽位——它们在原理上不可分离。**

#### 解法 B：先验

各标准差用弱信息先验：
```
σ_fam, σ_form, σ_b, σ_u  ~  HalfNormal(0, 0.02)
```
在 IC 尺度上 0.02 已经是很大的效应，所以这个先验是弱信息的但不是平的。平先验在小样本下会导致后验发散。

#### 解法 C：必做的诚实检查

**每次拟合后必须报告先验-后验重叠度**（prior-posterior overlap）。

```
若某个参数的后验与先验重叠 > 90%：
    在报告里写"该项数据未提供信息"
    **不要解读它的点估计**
```

**这一条能防止整个项目建立在幻觉之上。** 它也是一个诚实的展示点——敢报"我们的模型在这一项上什么也没学到"，比给出一个漂亮但无意义的点估计可信得多。

**验收**：用已知标签效应的合成数据跑一遍，恢复率必须 > 70% 才能信任真实数据的结果。

#### 失败的结构怎么入模（必须分类型）

| 判定 | 处理 | 理由 |
|---|---|---|
| PASS / UNDECIDABLE | 正常入模，各带自己的 σ̂ | UNDECIDABLE 的 σ̂ 大、贡献自然小——**这正是它该有的样子，不需要特殊处理** |
| **刀①（安慰剂）不过** | **完全不入模** | 它是关于代码的发现，不是关于市场的。入模会污染所有槽位 |
| **刀②（断言）不过** | 效应真、**标签假**：该行从设计矩阵**撤下**，以新 ID + 新标签重新入模 | 把被证伪的机制标签留在矩阵里，等于用错误的标签估计词表效应，**越迭代越偏** |
| 刀③（增量）不过 | 正常入模 | 它对 `a_form` 是很强的信息：这个表现形式已被覆盖 |

**一刀切会让模型越迭代越偏。** 这四行必须分别实现。

#### 实现建议

480 行、几十个参数，用 NumPyro 或 Stan 都很快（几十秒）。用 NUTS，检查发散转移数（应为 0）和 R̂（应 < 1.01）。

---

### M9.4 `shared_rivals(posterior) -> list[Finding]`

**目的**：读出**跨结构共享**的解释——单个结构的检验永远看不到这一层。

| 后验现象 | 读法 | 动作 |
|---|---|---|
| `b_t` 在某段时间显著偏离 | 全库在那段时间一起变化 | 这是一条**条件层规律**，不是十个因子各自失效。写进 law.md 的 W4 写入点 |
| `u_s ≈ 0` 而 `a_family` 显著 | 你在重复发现同一件事 | 该换 family 了，当前这一行已经挖完 |
| 某个 family 的后验被强烈收缩 | 那一族的结构彼此不一致 | 检查 `vocabulary_balance`，可能该拆 |

---

### M9.5 `prior_predict(coord, labels, posterior) -> (μ, σ)`

**目的**：给**还没研究过**的格子合成先验预测分布。

```
μ = μ̂ + â_family + â_form  （后验均值）
σ = 后验预测分布的标准差（含参数不确定性 + σ_u）
```

**用途**：让**整张地图在跑数之前就能上色**——不是绿/红/灰，而是"预期效应 + 不确定度"的深浅。

这是"跑过的东西影响未来"最直观的形态，也是路演上很好看的一张图。

---

### M9.6 `design_diagnostics(posterior) -> list[(factor_i, factor_j)]`

**目的**：找出在数学上**不可分离**的因子对（后验系数相关 > 0.7）。

**例子**：如果你跑过的格子恰好全是 `(family=M2, form=F3)` 和 `(family=M1, form=F1)`，那么 family 效应和 form 效应完全共线，无法分离。

**意义**：模型会直接告诉你——**问题不在某个因子里，在你选格子的方式里。**

这是"系统性误差在哪里"这个问题第一次有了可计算的答案，而且它指向**研究者本身**，不是数据。

**动作**：若输出非空，下一轮强制走 `factorial_seed`（M9.7）。

---

### M9.7 `factorial_seed(map, posterior, vocab) -> coord`

**目的**：冷启动阶段（前 `N_SEED_GRIDS = 4` 轮）的格子选择。

**原理**：后验为空时，目标**不是"挑最可能成功的"，而是"让设计矩阵可辨识"**。这是实验设计问题，目标函数是 D-最优性：

```
枚举候选格子 → 对每个候选，算把它加入设计矩阵后 det(XᵀX) 的增量 → 取最大
```

#### ⚠ 必须承认的结构性限制：槽位是耦合的

`agent` 与 `friction` **不能自由组合**。机制上讲，散户几乎总是配"注意力带宽"，做市商几乎总是配"存货风险"。你写不出 `(agent=retail, friction=inventory_risk)` 这样一个既合法又有意义的机制。

**后果：`a_agent` 与 `a_friction` 在原理上就无法分离，没有任何实验设计能解决它。** 这不是设计得不好，是这两个标签描述的本来就是同一件事的两个侧面。

因此实际可以独立变化的只有两个维度：
```
维度 A：family（M1..M10）—— agent/friction/mispricing/correction 捆绑变化
维度 B：form（F1..F7）   —— 可以自由变化
```

`design_diagnostics` 的第一个输出就应该是这条。**看到它不要去改实验设计，去承认它。**

#### 冷启动的正确设计：2×2 析因

| # | 格子 | family | form | 表达式起点 | 变的是 |
|---|---|---|---|---|---|
| **G1** | M2-F3 | 流动性提供 | F3 时序相对 | `neg(ts_z(ret_5d, 60))` | 基准 |
| **G2** | M2-F1 | 流动性提供 | F1 水平 | `amihud = mean(abs(ret)/turnover, 20)` | **form** |
| **G3** | M1-F3 | 注意力冲击 | F3 时序相对 | `ts_z(turnover_today, 60)` | **family** |
| **G4** | M1-F1 | 注意力冲击 | F1 水平 | `xs_rank(turnover_today)` | 两者（交互格） |

四个观测恰好估出：**截距、family 主效应、form 主效应、family×form 交互——一个不多一个不少。**

**为什么挑 M1 与 M2 这两族做基准**：
1. 两者都是**全 panel 覆盖**（不是窄覆盖事件格），闸门过得去
2. 两者机制上相邻但条件不同（注意力 → 反转；流动性 → 也是反转，但在流动性差的地方更强），所以 family 效应**有机会真的被估出来**
3. 两者都不依赖事件定义，实现最简单，适合把整条流水线先跑通

**⚠ 反例——不要这么做**：随便挑 M10-F1（竞价失衡）、M7-F1（平均单笔）、M3-F1（距前高）、M6-F7（指数纳入）这四个"最看好的"。它们的 family 互不相同、form 也不齐整，**四个观测什么参数也估不出来**，而且 M6-F7 是窄覆盖事件格，闸门大概率过不去。

#### 难点

纯 D-最优会挑出"统计上最优但机制上没意思"的组合。
**解法**：候选集先由 `llm_proposer` 过滤（只保留能写出合法标签与断言的），再在剩下的里做 D-最优。**约束在前，优化在后。**

**验收**：跑完 G1–G4 后，`design_diagnostics` 对 (family, form) 这一对的后验相关应低于 0.3。若仍然高，说明四个格子的覆盖区域重叠过大，需要换 family。

---

### M9.8 `next_probe(map, posterior, library) -> coord`

冷启动之后的常规选择。综合五个来源，输出**一个坐标，不是一个分数**：

```
1. shared_rivals      → 有共享效应待解释的区域优先
2. map_blanks         → 未探索的空白格
3. prior_predict      → 在空白格之间按预期效应/不确定度排序
4. coverage_overlap   → 与已有结构覆盖重叠高的降级
5. design_diagnostics → 若有不可分离的因子对，强制回到 factorial_seed
```


---

## M10 组合层（`compute.py`）

> **职责**：把多个已通过检验的结构变成一个可交易的因子。
>
> **⚠ 边界先划死：组合层不许创造新逻辑，只能做三件事——路由、加权、执行对齐。**
>
> 任何逻辑上的新组合（两个结构取交、取并、加条件）**必须回到研究层立一个新格子，走完整套刀**。
> 否则研究层所有的纪律都会从这个后门漏光——你在组合层取个交集，就造出了一个从未通过任何
> 检验、而且样本量比两个成分都小的新结构。

### M10.1 `relation_matrix(library) -> Relations`

**目的**：刻画结构之间的关系。**两个维度交叉，不是只看覆盖重叠。**

| 覆盖关系 | 标签关系 | 诊断 | 动作 |
|---|---|---|---|
| 不相交 | 不同机制 | 真正独立 | **并联，最理想** |
| 不相交 | 同机制 | 同一机制在两个区域的两种表现 | 其实是一个结构的两半 → **回研究层合并成一个带 gate 的结构** |
| 嵌套 | 同机制 | 子是父的特例 | 增量刀本该杀掉；没杀掉说明是真正的条件化 |
| 重叠·同号 | 同机制 | 重复发现 | 取更简单的那个 |
| 重叠·同号 | **不同机制** | **两条独立因果链指向同一笔交易** | **加信心，不加权重** |
| 重叠·异号 | 任意 | 有未识别的调节变量 | 空仓 + **回地图开新格** |

#### 关键区分（有档案才做得到）

```
信号相关性  →  决定分散化（权重）
机制独立性  →  决定信心（信念）
```

两个高度相关但机制无关的结构：**权重上算一个，信心上算两个。**

传统组合把这两件事都压进协方差矩阵，于是丢掉了后者。你有标签，所以能把它们分开——**这是有档案的直接红利**。

**经验规则**：共享标签 → 加信心；共享原始变量 → 减权重。

#### 六层度量，按成本阶梯

**能在免费层解决就不往下走**：

| 层 | 度量 | 接触收益？ | 时机 |
|---|---|---|---|
| 1 | 标签重叠（共享几个槽位） | 否 | 提案期 |
| 2 | 覆盖区域重叠（Jaccard） | 否 | 提案期 |
| 3 | 血缘关系（父子/兄弟/移植对） | 否 | 提案期 |
| 4 | `panel_signature` 相似度 | 已付费 | 判定后 |
| 5 | 权重序列相关 | 已付费 | 判定后 |
| 6 | 增量回归 R² | 已付费 | 判定后 |

前三层完全免费，可以在提案期就砍掉一大批"换了个窗口的同一个东西"。

---

### M10.2 `combination_balance(structures)` / `resolve_frustration(cycle, law_book)`

`combination_balance` 是 `balance_check`（M8.1）的一个调用点：
```
nodes   = 已通过（PASS）的结构
edge_fn = 两者的覆盖区域是否有交叠
sign_fn = 在交叠区域内，两者的信号方向是否一致（+1/−1/None）
```

**平衡** → 存在一致的全局符号分配，可以拼成一个因子。而且 `bipartition` 本身就给出了**两个相反的市场状态分组**。

**受挫** → 拼不起来。而**受挫圈精确定位了缺失的调节变量所在的区域**：圈上那几个结构的**覆盖交集**，就是那个未识别变量起作用的地方。

**这把"组合冲突"从一个麻烦变成了一台定位器。** 它不只说有矛盾，还说去哪儿找矛盾的原因——这是 M7 之外**第二个免费的提案来源**（不碰 y）。

#### `resolve_frustration` 的处理优先级

```
1. 若有 confirmed=true 的条件层规律能路由该区域
   → 按规律分区，各归各的状态
2. 否则在交叠区**空仓**
   → 这是诚实的默认值。不知道该往哪边就不下注
3. 无论走 1 还是 2，**都必须记一条发现候选回研究层**
   → 这是组合层唯一允许向研究层输出的东西
```

---

### M10.3 `conviction(structure) -> float`

**目的**：给结构打一个"可信度"，用于组合权重。

**⚠ 关键设计：不用历史收益。**

在 n_eff 只有 10³ 的量级上，三个结构的历史夏普差异**统计上不可区分**。按历史收益给权重 = 在组合层又调了一次参，而且是**最没有约束的一次**（组合层没有四把刀）。

**信息来源全部取自档案元数据**：

| 来源 | 测什么 | 为什么它不是收益 |
|---|---|---|
| 断言命中数（hold / 总数） | 机制的正确性 | 测的是预测对不对，不是赚了多少 |
| 对数赌本 `log W` | 证据强度 | 事前预测，不可重跑 |
| 盲下注命中率 | 可解释性 | 测的是陈述的可预测性 |
| 森林入选频率 | 发现的稳健性 | 测的是不依赖切分 |
| 覆盖度 | 适用范围 | 结构性质 |

这些量测的都是"**这个结构有多可信**"，不是"它历史上赚了多少"，而且它们建立在**不同的信息**上，合起来比单看收益稳健得多。

**用法保守**：**默认等权，只用 conviction 做降级，不做升级。**

小样本上等权几乎总是不劣于优化权重，这是组合优化里最稳的结论之一。conviction 低于阈值的结构降到半仓或剔除，但不给高 conviction 的结构加仓。

---

### M10.4 `to_continuous_weight(structure, cut_table) -> WeightFn`

```
w_i = logit_gate(z_i; c, k) × sign × strength(f_i) × [σ̄/σ̂_i 若有方差异质]
```

- `c` 用冻结切点（M2.3）
- `k`（门的陡峭程度）由分层曲线的形状决定，**不搜索**
- 方差缩放见 M7.3

#### ⚠ 覆盖区域之外必须是 NaN，不能是 0

这是本模块最重要的一条。

```
0   的语义是「我认为这只股票该持平」
NaN 的语义是「我对这只股票没有意见」
```

**两者在聚合时完全不同**：三个结构里两个是 NaN、一个是 +1，等权聚合应该给 **+1**（唯一有意见的那个），而不是 **+1/3**。

**混淆这两者会系统性地稀释稀疏结构的信号——这是"逻辑很漂亮但做不成因子"最常见的技术死因。**

实现上：聚合时用 `nanmean` 而不是 `mean`，并且记录每个位置有几个结构有意见（用于 M10.6 的容量计算）。

#### 区域外取 NaN，不取反

除非断言明确说了另一侧有反向机制。无脑取反等于凭空多了一个**未经检验的假设**。

---

### M10.5 `to_position_path(structure) -> Path`

#### ⚠ 不同 horizon 的结构不能在分数层相加

一个有效期 1–3 天的结构和一个 5–20 天的结构，把标准化分数加起来是**没有意义的**——它们说的是不同时间尺度上的事。

**正确做法**：每个结构输出的是**目标持仓路径**——按自己的衰减速度展开成未来若干天的目标仓位，然后在**仓位层**求和。

```
对结构 s，在 t 日产生信号后：
    target_position(t + k) = w_s(t) × decay_s(k),   k = 0, 1, ..., L_s
其中 decay_s 由该结构的 peak_horizon 断言决定，L_s 是有效期上限
```

**衰减速度从哪来**：
1. 档案的 `peak_horizon` 断言（P2）
2. `law.md` 的**传导层规律**

**这就是传导层规律进入组合的具体入口。**

---

### M10.6 `assemble_portfolio(structures, laws) -> Factor`

**算法**：
```
1. relation_matrix → combination_balance → 找受挫圈
2. 受挫圈 → resolve_frustration（路由 / 空仓 / 回研究层）
3. 平衡部分 → 按 bipartition 分配符号
4. 每个结构 → to_continuous_weight → to_position_path
5. 在**仓位层**求和（nansum），应用边界层规律的约束
6. 重新计算组合的成本容忍度（见下）
```

#### ⚠ 组合可能提高容量，而不是降低

两个结构如果在部分时点方向相反，合并后的换手会**净减少**（对冲掉了）。

所以**组合的成本容忍度不是成分的加权平均，必须重新算一遍**。

在只有十几个基点容忍度的结构上，这可能是决定性的：**单独上不了线，合起来能上。**

#### 核心视角：因子 = 地图上的一条覆盖路径

陈述层的产物天生是**稀疏且有条件**的（某个结构可能只覆盖 15% 的样本），而因子需要稠密。

传统做法把稀疏结构稀释成全样本因子，效应立刻被 85% 的噪声摊平——**这是"逻辑很漂亮但做不成因子"的主要死因**（另一个是 M10.4 的 NaN/0 混淆）。

正确做法反过来：

> **三个各覆盖 20% 的强结构拼起来，远好于一个覆盖 100% 的弱因子。**
> 覆盖不到的样本就是空仓，这是诚实的。

于是"把地图填满"不只是研究上的完整性，**它直接就是提高覆盖度的路径**——两件事在这个架构里是同一件事。

---

### M10.7 `compile_routing(law_book) -> RoutingRules`

**目的**：把 `confirmed = true` 的条件层规律编译成 mask 函数。

```
输入：law.md 中 kind=条件层 且 confirmed=true 的条目
输出：{law_id: callable(panel) -> bool_mask}
```

**`routing.yaml` 是编译产物，不手写。** 手写就意味着在组合层引入了未经检验的判断。

---

### M10.8 `emit_structure_card(structure, panel, blades) -> Card`

**目的**：PASS 时生成，是**研究层与交易层之间的唯一接口**。

| 研究层提供 | 交易层负责 |
|---|---|
| 条件区域（在哪里适用） | 入场触发（价位、微观确认） |
| 方向 | 出场规则 |
| 有效 horizon | 仓位与杠杆 |
| 失效监控量（什么指标变了就该停） | 多结构冲突仲裁 |
| 成本容忍度（bp） | 拆单与滑点控制 |
| 森林入选频率（稳健性） | 容量测算 |
| 覆盖度 | |

**研究层不给价位，交易层不改逻辑。** 这条边界是防止交易层的调参回流污染研究层的唯一屏障。

### M10.9 `execute_long_only(signal, bars, execution_config) -> BacktestResult`

**目的**：把已冻结的结构转换为可审计的股票多头＋现金回测结果；研究指标与交易指标同时输出。

**唯一时点协议**：T 日收盘后生成信号，T+1 开盘尝试成交。T 日竞价字段只能作为 T 日已经可得的输入，不能改变成交时点。

**硬错误规则**：买入订单无法成交、到期卖出无法成交、持仓在策略要求退出时无法退出，均抛出带股票、日期、原因和订单信息的异常并终止本次回测。不能静默保留、删除或修改该交易记录；这代表策略与执行假设不兼容。

**输出**：

- 交易层：累计收益、年化收益、Sharpe、最大回撤、换手、成交失败记录数（成功回测必须为 0）、费用和滑点情景。
- 研究层：各 `h ∈ HORIZONS` 的 IC、行业残差 IC、TB、多空分组收益（仅研究指标，不代表可做空）。

首版采用经典、可复现的执行配置：等资金预算、固定持有期、固定最大持股数、单股权重上限、现金最低比例、固定调仓日和预先登记的成交量约束。重复信号的叠加上限、冲突时空仓规则和交易平滑参数必须在回测前冻结；不从峰值区间自动推导衰减函数，也不使用未确认的方差规律调仓。

费用、滑点、持仓上限、现金比例和交易平滑规则全部由 `execution_config` 注入并冻结；主流程不内置隐式参数。

---

## M11 LLM 岗位（`test.py`）

> **职责**：LLM 承担五个岗位，**按能看到什么信息划分，不按能力划分**。
>
> **总原则：看到收益数据之前岗位无限；看到之后只剩一个岗位，且那个岗位不许写因果句。**

### 岗位与可见信息总表

| 岗位 | 能看到 | 绝对看不到 |
|---|---|---|
| 提案器 | 候选变量的**名字**、词表、law.md、地图、血缘 | **任何效应读数** |
| 算子化器 | 标签 + 可用字段列表 | 收益 |
| 盲下注器 | **只有标签和断言** | 一切数据，连候选变量是什么都不给 |
| 对账员 | 全部数据 | —（但禁止写因果句） |
| 归纳器 | law.md + 后验系数 | 原始收益 |

---

### M11.1 `llm_proposer(z_name | coord, vocab, law_book, map) -> labels`

**职责**：给定一个格子坐标（或一个候选变量名），写出五个标签。

**能看到**：候选变量的**名字**、受控词表、`law.md`、地图当前状态、血缘。

**绝对看不到**：z 的效应大小和方向、确认样本的任何结果、任何 IC 读数。

`law_book` 不能原样传入。主流程先生成 `build_deidentified_research_context`：只保留机制词表、地图状态、A 段已冻结的研究约束和不含方向/效应大小的摘要；B/H 结果、收益数值和后验系数禁止进入提案上下文。由 A 段产生的数据启发必须标记为 `data_informed_A`，不伪装成事前假设。

#### 为什么这条边界是整个发现流程的意义所在

如果告诉它"z 在高区效应更强"，它写出来的断言必然是"效应在高区更强"——**这条断言只是复述了观测，没有任何证伪力。**

只给变量名，它必须**从机制推出方向**，断言才有可能是错的。**而能不能错，是它有没有信息的唯一标准。**

#### 工程实现

上下文由 `build_proposer_context()` 从**白名单字段**拼装，而不是从 panel 对象里挑字段排除。

**黑名单必然漏。** 白名单的实现是：明确列出允许放进 prompt 的字段名，其余一律不进。

---

### M11.2 `llm_operationalizer(labels, field_list) -> list[expression]`

**职责**：写三个表达式（同一机制的三种写法），逐个通过 `register_cell` 的五项检查。

"LLM 写代码、改代码"归这个岗位。

**难点**：LLM 写的表达式经常量纲不一致或隐含前视。

**解法：不让它写自由 Python，让它在受限 DSL 里组合。**

#### 受限 DSL 的算子集

| 类别 | 算子 | 语义与约束 |
|---|---|---|
| 时序 | `lag(x, k)` | 必须 `k ≥ 1`，`k=0` 编译报错 |
| | `ts_mean(x, w)` / `ts_std(x, w)` / `ts_max` / `ts_min` | 窗口只含到 t 为止 |
| | `ts_z(x, w)` = `(x − ts_mean)/ts_std` | 输出无量纲 |
| | `ts_rank(x, w)` | 输出 [0,1] |
| | `diff(x, k)` = `x − lag(x,k)` | |
| 截面 | `xs_rank(x)` | 当日截面排名，输出 [0,1] |
| | `xs_z(x)` | 当日截面标准分 |
| | `industry_mean(x)` | 当日同行业均值，**用 PIT 行业** |
| 算术 | `add/sub/mul/div/log/abs/sign/neg` | `div` 遇零返回 NaN；`log` 要求正值 |
| 条件 | `gate(z, cut_id)` | cut_id 必须是**冻结切点表里的 id** |
| | `where(cond, a, b)` | |

**DSL 的每个算子自带量纲规则与时滞语义，编译期就能查出违规：**
- `lag(x, 0)` → 编译报错（前视）
- `div(a, b)` 其中 b 可能为零 → 强制插入 NaN 处理
- `gate(z, c)` 其中 c 不在冻结表里 → 编译报错（防止现算切点）
- 量纲：`ts_z` 的输出标记为无量纲，与有量纲的量相加会报错

---

### M11.3 `llm_blind_bettor(labels, assertions) -> Bets`

**职责**：对每条断言给一个**概率**（不是"会/不会"）。

**能看到**：**只有标签和断言**。连候选变量是什么都不需要给。

**落盘时机**：**必须先于任何测量。** `bet.yaml` 的时间戳必须早于 `panel.parquet` 的修改时间，这一条要在代码里断言。

#### 双重用途

1. 这些概率是**可解释性的度量**——一条机制陈述如果让没看过数据的读者猜不准，那条陈述就是装饰
2. 它们**直接就是 M6 计算 e 值时的下注比例**

两个用途合并成同一个数，这是本系统的一处设计统一（M6.8）。

#### 难点：LLM 的概率严重不校准（过度自信）

**解法**：
1. `N_BLIND_BETTORS = 5` 个独立实例取平均
2. 用历史命中率做 Platt 校准
3. 分数 Kelly 收缩（M6.3）

**校准曲线本身作为报告的一张图**（"我们的 LLM 有多可信"）——横轴声称概率，纵轴实际命中率，理想是 45 度线。

---

### M11.4 `llm_reconciler(panel, blades, structure) -> (aligned, divergent)`

**职责**：逐条判定断言并对账。

**能看到**：全部数据。

**⚠ 硬约束：禁止写因果句。**

只输出"相符"与"相悖"两份，**不合并、不取平均、不给综合评价**。

**句式固定**：
```
「P1 形状：低区 τ=−0.048(t=−2.9)，中区 τ=−0.031(t=−2.1)，高区 τ=−0.009(t=−0.6)，
  呈单调向下但高区不显著，预测为 monotone_down → hold（边缘）」
```

**为什么这条约束**：防火墙不是防它撒谎，是**防它诚实地做一件不该做的事**——看过收益之后写出的因果句，形式上和事前写的一模一样，但性质完全不同（纪律一）。

---

### M11.5 `llm_inducer(law_book, posterior) -> list[MapEdit | VocabEdit]`

**职责**：
1. 提出"这几条规律其实是同一条"
2. 提出"这一行机制族该拆"
3. 提出词表合并方案
4. 给 `lawbook_balance`（M8.2）判定条目之间是蕴含还是矛盾

**能看到**：`law.md` + 后验系数。**看不到原始收益。**

**输出是对地图/词表的修改提案，必须走完整套检验才算数。**

**为什么这个岗位有价值**：地图的行是人拍的，`family` 的划分也是人拍的。**LLM 读账本重新分行，是人机分工里真正互补的地方**——它能看到人看不到的类比，但它的提案必须交给客观判据（M2.5 的边际似然）裁决，不能自己算数。

---

## M12 规律账本（`test.py`）

### M12.1 `write_law(kind, claim, scope_predicate, evidence, reopen) -> Law`

#### 四类规律

| 类型 | 记什么 | 本项目的例子 |
|---|---|---|
| **度量层** | 变量本身是什么 | "换手率的截面分布在 2020 年后显著右移，同一个 z 分数在两个时期含义不同" |
| **传导层** | 信息怎么进入价格 | "隔夜跳空的效应在 T+1 开盘一次性释放；盘中形成的动量分散在 T+1 到 T+3" |
| **条件层** | 何时成立、何时反号 | "短期反转只在小市值成立；沪深300成分股内符号相反" |
| **边界层** | 成本、容量、拥挤、方差异质 | "该效应集中在 T+1，固定成本吃掉约六成，容忍度 14bp" |

#### 五个字段

```yaml
kind:            条件层
claim:           "短期反转只在小市值成立，沪深300成分股内符号相反"
scope_predicate: market_cap_pct < 30          # ★ 必须可求值
scope_negative:  index_member IN ['000300.XSHG']
confirmed:       true
evidence:        [S-0001.P4, S-0003.panel.layered_ic]
reopen:          "若 2026 年后小市值流动性结构变化，需重查"
```

#### `scope_predicate` 必须是可求值表达式

否则组合层（M10.7）用不了它。散文式的 scope 只能进报告，不能进路由。

#### `confirmed` 是一道必须设的闸门

| 值 | 含义 | 允许 |
|---|---|---|
| `false` | 只是观察到的 | **只能进报告，只能指导研究** |
| `true` | 经确认样本裁决（某条断言在 B 段 hold，或走过完整的 discover 流程） | **才能进组合层的路由** |

**未确认的规律可以指导研究，不能指导仓位。**

不设这条，你的组合会慢慢变成账本里所有观察的叠加，**而账本里有一半是还没验的猜测**。

#### 重要区分：关于代码的发现不进账本

前视 bug、变量不忠实、复权错误——这些写进**变量注册表的备注**，不进 `law.md`。

**否则账本会被自己的 bug 稀释。** `law.md` 记的是关于市场的知识，那是可以复用、可以跨项目带走的资产；关于代码的发现随着代码修好就失效了。

---

### M12.2 四个写入点

| 写入点 | 时机 | 类型 | 谁写 |
|---|---|---|---|
| **W1** | 变量注册 / 覆盖度检查完成 | 度量层 | 程序自动 + LLM 润色 |
| **W2** | 闸门判 UNDECIDABLE | 条件层（带 reopen） | 程序自动 |
| **W3** | 判定与逐条断言完成 | 传导/条件/边界层 | `llm_reconciler` |
| **W4** | **层次模型重新拟合后** | 条件层（跨结构共享的） | `llm_inducer` |

**W4 最容易漏也最值钱**：`b_t` 显著这类发现**不属于任何单个结构**，如果没有专门的写入点，它永远不会被写下来。

---

### M12.3 `reconcile_law_posterior(law_book, posterior) -> list[Gap]`

**每轮必跑。**

后验是**数**、`law.md` 是**词**，两者会漂移。这条规则把它们钉在一起：

```
后验里每一个显著效应       ⟹ law.md 必须有对应条目
law.md 里每条条件层断言    ⟹ 后验必须有对应的项
```

**举例**：
- `b_t` 在 2020Q1 显著为负，但没人写下"2020Q1 全库效应一起变弱" → **这个发现下次就丢了**，必须补进账本
- `law.md` 写了"只在小市值成立"，但模型里根本没有市值这一维 → **这条 law 无法参与未来的预测**，要么补一个协变量，要么标记为"不可建模"

不一致就必须补一边。**这是让"词"和"数"共同构成记忆的唯一办法。**


---

## M13 主循环（`test.py`）

### M13.0 `run_parallel_tasks(tasks, executor_config) -> list[TaskResult]`

**目的**：在不改变研究顺序和审计边界的前提下并行执行相互独立的 CPU 任务，例如不同安慰剂种子、A 段训练/验证折和不同结构的纯测量。

**规则**：任务必须是显式输入、无共享可变状态、带固定随机种子；结果按任务 ID 排序后再合并。CPU 密集任务使用受控进程池，I/O 使用异步任务；禁止让进程池直接读取 B/H 目录，B/H 读取由主进程的审计锁串行完成。GPU 任务通过独立组件注入，默认不依赖 CUDA；4090 接入后只替换森林或批量张量计算器，不改变统计协议。

**失败处理**：任一子任务异常时取消同批未完成任务，保留已完成结果和错误信息，批次标记为 `invalid`，不得静默跳过。

```
FUNC main():

  validation = validate_data_sources(data_root)  # M1.0a；fail 立即终止
  require validation.has_no_failures()

  # ══════════ 阶段 0：先算账（半天，建议全项目第一件事）══════════
  panel_min = build_panel_minimal(data_root)      # 只要 daily_bar + 标签
  for h in HORIZONS:
      n_eff = compute_n_eff(panel_min, ref_factor, h)     # M1.6
      report(h, n_eff, tau_min = 2.8/sqrt(n_eff))
  power_budget(n_eff_h5, EXPLORE_FRAC, tau_main=0.05)     # M7.6

  # ★ 如果这一步显示 τ_int >> τ_main（大概率），
  #   就在动手之前调整计划：EXPLORE_FRAC 降到 0.5，
  #   研究重心转向主效应与 F7 事件格。

  # ══════════ 一次性初始化 ══════════
  panel      = build_panel(data_root, universe_spec)      # M1.1，默认池排除 ST
  assert_no_lookahead(panel)                              # M1.7
  H, A, B    = split_data(panel)                          # M1.5
  splits_A   = [block_split(A, seed) for seed in 1..5]    # A 段内的稳定性重切
  cells      = [register_cell(spec) for spec in specs]    # M2.1
  cut_table  = freeze_cuts(cells, splits_A[0].train)      # M2.3
  map        = mark_infeasible(build_map(...))            # M3.1-2，免费
  posterior  = None
  library    = []

  # ══════════ 研究循环 ══════════
  WHILE 还想研究:

    # ── (a) 选一个格子 ──
    IF len(library) < N_SEED_GRIDS:
        coord = factorial_seed(map, posterior, vocab)     # M9.7 冷启动 2×2
    ELSE:
        coord = next_probe(map, posterior, library)       # M9.8

    # ── (b) 写档案并冻结 ──
    vocab   = {s: vocabulary_registry(s) for s in SLOTS}  # M2.4
    labels  = llm_proposer(coord, vocab, law_book, map)   # M11.1
    asserts = compose_assertions(labels)                  # M3.3（含空洞性检验）
    exprs   = llm_operationalizer(labels, field_list)     # M11.2
              → 每个都过 register_cell                    # M2.1
    S       = register_structure(labels, asserts, exprs, coverage)   # M3.4
    freeze_structure(S)                                   # M3.5 哈希链
    bets    = llm_blind_bettor(S.labels, S.assertions)    # M11.3
              → 落盘 bet.yaml，时间戳必须早于任何测量

    # ── (b2) 免费的去重初筛 ──
    IF coverage_overlap(S, library) > 阈值:               # M10.1 第 1–3 层
        降级并 continue                                    # 不花任何额度

    # ── (c) 判定力闸门 ──
    IF NOT gate_decidable(S, A, horizon=5):               # M5.1
        write_law(W2, kind=条件层, reopen="需 n_eff ≥ ...")
        library.append(S, verdict=UNDECIDABLE)            # 仍入模，σ 大、贡献小
        GOTO (f)

    # ── (d) 测量 → A 段前三把刀；B 段留到候选批次冻结后统一读取 ──
    P = measure_panel(S, A, label_kind="industry_resid")  # M4.1，产出 Ψᵢ
    Bl = run_blades(S, A, library, confirm=None)          # M5.6，刀①②③

    W = 1.0
    e_list, w_list = [], []
    FOR a IN S.assertions:
        IF Bl.assertion_verdicts[a.id] == "untested": continue
        p0 = estimate_p0(a, Bl.placebo_runs)              # M6.3
        e  = bet_on_assertion(a, bets[a.id], p0, Bl.assertion_verdicts[a.id])
        e_list.append(e); w_list.append(a.weight)
    W = merge_e(e_list, w_list, mode="parallel")          # M6.5，仅实验性记录

    verdict = provisional_decide(P, Bl)                    # 仅 A 段临时状态，不得 PASS
    llm_reconciler(P, Bl, S)                              # M11.4 逐条对账
    write_law(W3, ...)                                    # M12.1-2

    # ── (e) 按失败类型决定怎么入模 ──                     # M9.3
    IF Bl.placebo == FAIL:
        记入 cell.py registry note
        **不进 law.md、不进设计矩阵**
    ELIF 任一旁证断言 == violated:
        旧 labels 撤下设计矩阵
        触发"跨行换机制"算子，以新 ID + 新标签重新入库
    ELSE:
        library.append(S, P, Bl, verdict)

    # ── (f) 更新记忆 ──
    posterior = fit_hierarchy(library, stage=auto)        # M9.3 按规模解锁
    报告先验-后验重叠度；重叠 >90% 的项**不解读**
    reconcile_law_posterior(law_book, posterior)          # M12.3
    map.recolor(prior_predict(c, ·, posterior)
                for c in map_blanks(map))                 # M9.5 空白格上色
    IF 轮次 % 5 == 0:
        merge_vocabulary(llm_inducer(law_book, posterior))  # M2.5 / M11.5
    IF design_diagnostics(posterior) 非空:                # M9.6
        下一轮强制走 factorial_seed

    # ── (g) 发现与演化 ──
    IF NOT should_stop(S, ...):                           # M7.9
        children_a = discover(S, P, A, B, library)        # M7.7；B 只在候选批次冻结后读取
        children_b = evolve_operators(S, assertion_matrix(library))  # M7.8
        map.enqueue(children_a + children_b)

    # ── (h) 一致性检查与组合（每 5 轮，或有新 PASS 时）──
    bal = combination_balance([s for s in library if s.verdict == PASS])  # M10.2
    FOR c IN bal.cycles:
        map.enqueue(resolve_frustration(c, law_book))     # 免费的新提案
    FOR each 已跑格子:
        convergent_validity_balance(s)                    # M8.2 三表达式一致性
    regime_balance(library, time_blocks)                  # M8.2 状态发现
    lawbook_balance(law_book)                             # M8.2 账本自洽
  portfolio = assemble_portfolio(PASS 结构, law_book)   # M10.6
  backtest_result = execute_long_only(portfolio, bars, execution_config)  # M10.9

    # ── (i) PASS 的复验 ──
    IF verdict == PASS:
        在 splits_A[1..4] 上复验（这是稳定性，不是样本外）
        IF 一致: emit_structure_card(S, P, Bl)            # M10.8

  # ══════════ 收尾：封存段只读一次 ══════════
  batch_results = confirm_frozen_batch_once(B, candidate_batch)  # B 只读一次
  batch_decisions = adjust_batch_errors(batch_results, alpha=0.05) # Holm
  FOR S IN [已 PASS 且已发结构卡的结构]:
      audit_read(H, reason="final confirmation")
      final_check(S, H)
  写最终报告
```

---

# 附录 A · 调用关系

```
main
 │
 ├─【阶段 0】
 │   └─ compute_n_eff ── power_budget
 │
 ├─【初始化】
 │   ├─ build_panel ─┬─ build_universe_pit
 │   │               ├─ attach_industry_pit
 │   │               ├─ build_labels
 │   │               └─ assert_no_lookahead
 │   ├─ split_data（H / A / B 三段）
 │   ├─ register_cell ── dispatch_by_dimension
 │   ├─ freeze_cuts ── dispatch_by_dimension
 │   └─ build_map ── mark_infeasible
 │
 ├─【每轮 · 选格子】
 │   ├─ factorial_seed   ← design_diagnostics ← fit_hierarchy      （冷启动）
 │   └─ next_probe       ← shared_rivals / prior_predict /
 │                          map_blanks / coverage_overlap / design_diagnostics
 │
 ├─【每轮 · 写档案】
 │   ├─ vocabulary_registry
 │   ├─ llm_proposer            （只见变量名，不见效应）
 │   ├─ compose_assertions ── llm_blind_bettor ×5   （空洞性检验，产出 prior_p）
 │   ├─ llm_operationalizer ── register_cell        （受限 DSL，编译期检查）
 │   ├─ register_structure ── vocabulary_registry
 │   ├─ freeze_structure        （哈希链）
 │   └─ llm_blind_bettor        （落盘，时间戳先于测量）
 │
 ├─【每轮 · 测量与判别】
 │   ├─ measure_panel ─┬─ ic_tb_curve / monthly_ic_curve / contribution_conc
 │   │                 ├─ layered_ic / hac_t / hit_rate_curve
 │   │                 ├─ car_event / cost_sensitivity
 │   │                 └─ influence_decomposition → Ψᵢ
 │   ├─ run_blades ─┬─ gate_decidable  ── compute_n_eff(mask=coverage)
 │   │              ├─ blade_placebo   （永远优先；产出 p0 的原料）
 │   │              ├─ blade_assertion
 │   │              ├─ blade_increment ── relation_matrix
 │   │              └─ blade_icm       ── orthogonalize   （仅 forest 来源）
 │   └─ 赌本 ─┬─ estimate_p0 ← blade_placebo
 │            ├─ shrink_bet
 │            ├─ bet_on_assertion
 │            └─ merge_e ── wealth_process
 │
 ├─【每轮 · 发现】
 │   └─ discover ─┬─ split_explore_confirm
 │                ├─ orthogonalize          （两半各自 cross-fit）
 │                ├─ variance_vs_mean_screen ← Ψᵢ，★必须在森林之前
 │                ├─ forest_propose          ← fit_hierarchy 的残差；只出变量名
 │                ├─ repeat_splits           （B=20，p 中位数×2，出入选频率）
 │                ├─【缝】llm_proposer → compose_assertions → freeze_structure
 │                │        ★ 此前 confirm 目录持"未读"锁
 │                └─ blade_icm               （解锁 confirm）
 │
 ├─【每轮 · 记忆】
 │   ├─ fit_hierarchy ── build_design_matrix ── vocabulary_registry
 │   ├─ shared_rivals / prior_predict / design_diagnostics
 │   └─ reconcile_law_posterior
 │
 └─【每轮 · 一致性与组合】
     ├─ balance_check ─┬─ combination_balance ── resolve_frustration
     │                 ├─ convergent_validity_balance
     │                 ├─ regime_balance
     │                 ├─ vocabulary_balance
     │                 └─ lawbook_balance
     └─ assemble_portfolio ─┬─ relation_matrix
                            ├─ conviction
                            ├─ to_continuous_weight
                            ├─ to_position_path
                            └─ compile_routing ← law_book
```

---

# 附录 B · 谁接触未来收益

这个划分必须**在代码层面可判定**，因为它决定了三件事：什么是免费的、什么消耗证据额度、防火墙划在哪里。

| 类别 | 函数 | 说明 |
|---|---|---|
| **直接接触 y** | `build_labels`、`measure_panel` 及全部子函数、四把刀、`fit_hierarchy`、`forest_propose`、`bet_on_assertion`、`estimate_p0` | 消耗证据额度 |
| **间接携带 y 的信息** | `prior_predict`、`design_diagnostics`、`factorial_seed`、`shared_rivals`、`panel_signature`、`Ψᵢ`、`relation_matrix` 的第 4–6 层、`balance_check` 的 `sign_fn` | 本身不是收益，但由收益派生。**可以自由调用，但绝不能被 `llm_proposer` / `llm_operationalizer` / `llm_blind_bettor` 读到** |
| **完全不接触 y（免费）** | 地图枚举、标签与断言的书写、空洞性检验、词表操作、算子化、`coverage_overlap`、构造效度检验、`power_budget`、`relation_matrix` 的第 1–3 层、`balance_check` 的图算法本身 | 可以放开做 |

### 五条最容易被打破的纪律

1. **`discover` 第 5 步（缝）必须在读取确认样本之前完成**——否则就是在确认样本上事后编故事，分样本的全部意义作废。
2. **`llm_proposer` 只拿变量名，不拿效应大小和方向**——否则断言只是复述观测，没有证伪力。
3. **`variance_vs_mean_screen` 必须在 `forest_propose` 之前**——因为分样本挡不住方差异质。
4. **`blade_placebo` 必须在其余刀之前**——因为分样本对伪影完全无效。
5. **`bet.yaml` 的时间戳必须早于 `panel.parquet`**——否则盲下注不是盲的。

---

# 附录 C · 目录结构

```
research/S-0007/
  structure.yaml    标签 + 断言 P1..Pn（含 prior_p / p0 / weight）
                    + 三个表达式 + coverage + 血缘 + 哈希链
  bet.yaml          盲下注概率，★ 时间戳必须早于 panel.parquet
  panel.parquet     测量层全部数值，含逐观测 Ψᵢ（是下游输入，不只是给人看）
  panel.ipynb       全部图
  blades.json       四把刀结果 + 各条 e 值 + 森林入选频率
                    + 确认段读取审计日志
  verdict.json      三态判定 + 逐条断言判定 + 赌本 W + 未排除的候选
  card.yaml         仅 PASS 时生成

law.md              规律账本（四类，含 scope_predicate 与 confirmed）
map.yaml            地图状态：绿/红/灰/空白 + prior_predict 上色
vocab.yaml          受控词表 + 复用计数 n_used
posterior.json      层次模型后验 + 先验-后验重叠度 + 拟合阶段
wealth.json         全库赌本轨迹
relations.json      结构关系矩阵（六层度量）
frustration.json    受挫圈清单 + 每个圈定位到的覆盖交集区域
chain.log           档案哈希链
audit.log           封存段与确认段的读取日志
frozen/cuts.parquet 冻结切点，只写一次
```

---

# 附录 D · 风险登记表

| # | 风险 | 严重度 | 症状 | 应对 |
|---|---|---|---|---|
| 1 | **复权方向搞反** | 极高 | 除权日出现巨大假收益 | 用高送转股票单测，复权后除权日收益应 ≈ 0（M1.4） |
| 2 | **收益用收盘到收盘（忽略 T+1）** | 极高 | 全系统收益系统性虚高 | 收益起点必须是 T+1 开盘价（M1.4） |
| 3 | **层次模型欠识别** | 极高 | 后验贴着先验、发散转移多 | 分阶段解锁；报告先验-后验重叠度；重叠 >90% 的项**不解读**（M9.3） |
| 4 | **确认侧功效不足** | 极高 | 交互项永远不显著 | 先跑 `power_budget`；接受 UNDECIDABLE 是常态；优先 F7 事件格（M7.6） |
| 5 | **一字涨停样本混入** | 高 | 高 IC 因子实际买不进 | T+1 执行检查；无法成交直接报错（M1.2/M10.9） |
| 6 | **月末快照当成日频真值** | 高 | 行业/指数成分用了未来信息 | 行业用 exact_changes 区间；成分用"该日之前最近快照"（M1.3/M1.7） |
| 7 | **LLM 写空洞断言** | 高 | 所有断言都 hold | 空洞性检验（VACUITY_BAND）；断言先验概率纳入 e 值（M3.3） |
| 8 | **信息防火墙被绕过** | 高 | 盲下注命中率异常高 | 白名单构造上下文；确认段审计锁；哈希链（附录 B） |
| 9 | **方差异质被误当市场状态** | 高 | 加条件后样本内大涨、样本外崩 | `variance_vs_mean_screen` 强制前置（M7.3） |
| 10 | **NaN 被当成 0** | 高 | 稀疏结构的信号被系统性稀释 | 聚合用 nanmean；覆盖外一律 NaN（M10.4） |
| 11 | **ρ̄ 时变导致 n_eff 失真** | 中 | 危机期显著性虚高 | 分窗口算 n_eff 再加总；报告 ρ̄ 时序图（M1.6） |
| 12 | **符号图假受挫圈** | 中 | 到处都是冲突 | `sign_fn` 只在置信区间不跨零时返回 ±1（M8.1） |
| 13 | **计算量爆炸** | 中 | 单格子跑几小时 | 安慰剂只跑单 horizon 半样本；森林 500 棵；重复切分共用折结构 |
| 14 | **组合层偷偷造新逻辑** | 中 | 组合表现远好于成分 | 边界写死：只能路由/加权/对齐（M10 开头） |
| 15 | **LLM 过度自信导致赌本爆仓** | 中 | 一次失败打掉九成赌本 | 分数 Kelly，λ 由历史校准曲线反解（M6.3） |
| 16 | **p₀ 拍成 0.5** | 中 | 证据被系统性高估 | 从安慰剂分布估计，取置信上界（M6.3） |
| 17 | **森林用非 honest split** | 中 | 效应差被系统性高估 | 按时间块对半，分裂与估计分离（M7.4） |
| 18 | **e 值校准函数选错** | 低 | I 类错误失控 | 用标准校准族；纯噪声数据上验 ≤5%（M6.4/M6.5） |

---

# 附录 E · 建议的实现顺序

| 阶段 | 内容 | 为什么先做这个 | 预估 |
|---|---|---|---|
| **0** | `compute_n_eff` + `power_budget` | **如果确认侧没有功效，M7 整条流程要重新设计。早知道省两天** | 半天 |
| **1** | M1 数据层全部。**先做复权验证与 T+1 口径** | 地基。这两处是 A 股回测虚高最常见的来源 | 2 天 |
| **2** | M2 变量注册 + `freeze_cuts` + 受限 DSL | 变量的唯一事实源；DSL 是 M11.2 的前提 | 1.5 天 |
| **3** | M4 测量层，**特别是 `influence_decomposition`** | Ψᵢ 是 M7 的唯一输入 | 2 天 |
| **4** | M3 档案与冻结 + M5 前三把刀 | 至此能跑通一个完整格子 | 2 天 |
| **5** | M6 赌本 + M11.3 盲下注 | 可解释性的量化，路演可报的数字 | 1 天 |
| **6** | M8 `balance_check` | **不依赖任何统计模型，性价比最高，最适合当演示主视觉** | 1 天 |
| **7** | M7 发现层 | 依赖 Ψᵢ 和阶段 0 的结论 | 3 天 |
| **8** | M9 层次模型 | 风险最高，放最后。**做不成就在报告里说明，那也是一个诚实的结果** | 2 天 |
| **9** | M10 组合层 | 需要至少 3 个 PASS 才有意义 | 2 天 |

**如果时间只够做一半**：阶段 0–6 就是一个完整可展示的系统。M7 发现层和 M9 层次模型可以在报告里作为"已设计、待验证"的部分说明。


---

# 附录 F · A 股日频量价机制地图

> 这是 `build_map`（M3.1）的具体内容。10 行 × 7 列 = 70 格，可行约 52 格。
> **地图在跑任何数据之前就画完，完全不接触未来收益，因此枚举是免费的。**

## F.1 机制族（行）及其默认标签

`llm_proposer` 在这些默认值的基础上细化。

| # | 机制族 | 机制陈述 | agent | friction | mispricing | correction |
|---|---|---|---|---|---|---|
| **M1** | 注意力冲击 | 异常成交/涨跌吸引散户注意力 → 买入压力集中 → 短期反转 | `retail` | `attention_bandwidth` | `overreaction` | `attention_decay` |
| **M2** | 流动性提供 | 短期订单失衡需要有人承接 → 承接者要求补偿 → 反转是补偿的实现 | `liquidity_provider` | `inventory_risk` | `temporary_impact` | `inventory_unwind` |
| **M3** | 处置效应与锚定 | 参考价（前高、成本区）造成不对称卖出压力 → 调整被拖慢 → 动量 | `retail` | `disposition_effect` | `underreaction` | `gradual_realization` |
| **M4** | 信息缓慢扩散 | 信息先反映在部分标的，再传导到关联标的 | `limited_attention_inst` | `processing_capacity` | `underreaction` | `peer_diffusion` |
| **M5** | 交易约束（A股特有） | 涨跌停/停牌/T+1 使价格当日无法出清 → 未完成的调整延到次日 | `all` | `hard_constraint` | `unfinished_adjustment` | `next_session` |
| **M6** | 被动资金与再平衡 | 指数成分与权重变化产生机械买卖 → 事件前后价格压力 | `index_fund` | `mandate_constraint` | `temporary_impact` | `post_event_reversal` |
| **M7** | 参与者结构 | 大单/小单占比反映谁在交易 → 价格影响持续性不同 | `mixed` | `order_splitting` | `information_asymmetry` | `information_incorporation` |
| **M8** | 风险补偿与估值 | 估值差异是风险补偿，受宏观利率环境调节 | `long_horizon_inst` | `risk_aversion` | `risk_premium` | `slow_convergence` |
| **M9** | 不确定性解决 | 分红/送转/复牌等事件使不确定性下降 → 折价消失 | `all` | `uncertainty_aversion` | `ambiguity_discount` | `event_resolution` |
| **M10** | 隔夜信息聚合 | 隔夜信息在集合竞价中被定价，委托失衡暴露定价压力 | `mixed` | `overnight_closure` | `overnight_repricing` | `intraday_absorption` |

## F.2 表现形式（列）

| # | 形式 | 含义 | 典型算子 |
|---|---|---|---|
| F1 | 水平 | 变量本身的截面位置 | `xs_rank(x)` |
| F2 | 变化 | 相对自身上一期的差分 | `diff(x, k)`、`diff(log(x), k)` |
| F3 | 时序相对 | 相对自身历史分布的位置 | `ts_z(x, 60)`、`div(x, ts_mean(x, 20))` |
| F4 | 截面相对 | 相对同行业/同规模组的位置 | `sub(x, industry_mean(x))` |
| **F5** | **背离** | 两个本应同向的度量出现不一致 | `sub(xs_rank(a), xs_rank(b))` |
| F6 | 条件化 | 在某个状态区域内才启用 | `mul(gate(z, cut_id), signal)` |
| F7 | 事件化 | 围绕一个离散事件的窗口研究 | `CAR[-10, +20]` |

**F5 是本项目特有的设计。** 文本面板里这一列是"文内相对"，日频量价里没有这个概念。改为背离——价涨量缩、竞价强而全天弱、大单流入但价格没涨。这是纯量价数据里信息量最高的一类构造，因为它天然是**两个变量的关系**而非单一变量的水平，因此更难被简单的线性因子覆盖。

## F.3 完整地图

|  | F1 水平 | F2 变化 | F3 时序相对 | F4 截面相对 | F5 背离 | F6 条件化 | F7 事件化 |
|---|---|---|---|---|---|---|---|
| **M1 注意力** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| **M2 流动性** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| **M3 处置锚定** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| **M4 信息扩散** | ▨ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| **M5 交易约束** | ▨ | ▨ | ▨ | ▨ | ▨ | ✓ | ✓ |
| **M6 被动资金** | ▨ | ✓ | ▨ | ✓ | ▨ | ✓ | ✓ |
| **M7 参与者结构** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ▨ |
| **M8 风险补偿** | ✓ | ▨ | ✓ | ✓ | ▨ | ✓ | ▨ |
| **M9 不确定性** | ▨ | ▨ | ▨ | ▨ | ▨ | ✓ | ✓ |
| **M10 隔夜聚合** | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

**灰格理由**：
- M4-F1：信息扩散的"水平"没有定义——扩散是关系，不是状态
- M5、M9 的 F1–F5：本身是状态或事件，不是可排序的预测信号
- M6-F1/F3/F5：指数权重是月末快照，日频水平无意义
- M8-F2/F5：日频估值变化与价格变化构造上共线
- M7-F7：参与者结构没有天然的离散事件

**首版 horizon 约束**：地图仍保留机制完整性，但任何默认峰值 horizon 超过 10 个交易日的格子标记为 `deferred_horizon`，不进入首版可交易结构和 PASS 统计；待扩展 horizon、purge 和标签隔离后再解锁。

**52 个可行格 = 想法空间的全集。** 这个数字应该出现在报告的第一页。

## F.4 优先格子的候选表达式

**每格必须写 3 个表达式（收敛效度），下面给的是起点不是全部。**

### M1 注意力冲击

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M1-F3 | `ts_z(turnover_today, 60)` | 单调，高换手 → 负收益 | 3–10 |
| M1-F3 | `log(div(volume, ts_mean(volume, 20)))` | 同上 | 3–10 |
| M1-F2 | `sub(turnover_today, turnover_week)`（换手加速） | 饱和 | 2–5 |
| M1-F5 | `sub(xs_rank(abs(ret_1d)), xs_rank(ts_z(turnover,20)))`（大涨但没量） | 非单调 | 3–10 |
| M1-F6 | `mul(gate(market_cap, low), attention_shock)` | 小市值更强 | 3–10 |
| M1-F7 | 换手突增（>3σ）事件的 CAR[-10,+20] | 事前平坦、事后反转 | — |

### M2 流动性提供

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M2-F3 | `neg(ts_z(ret_5d, 60))`（短期反转） | 单调 | 1–5 |
| M2-F1 | Amihud：`ts_mean(div(abs(ret_1d), total_turnover), 20)` | 单调 | 5–20 |
| M2-F5 | `ret_1d` 与竞价委托失衡方向相反 | 非单调 | 1–3 |
| M2-F6 | `mul(gate(amihud, high), reversal)` —— 流动性差处补偿更高 | 高区更强 | 1–5 |
| M2-F7 | 极端单日收益（>3σ）的 CAR | 事后反转 | — |

### M3 处置效应与锚定

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M3-F1 | `div(close, ts_max(high, 252))`（距 52 周高点） | 单调向上 | 20–60 |
| M3-F1 | CGO 近似：`div(sub(close, ts_wmean(close, turnover, 252)), close)` | 单调 | 20–60 |
| M3-F3 | `ts_rank(close, 252)` | 单调 | 20–60 |
| M3-F5 | 距前高很近但换手在萎缩（接近解套区但没人卖） | 非单调 | 10–20 |
| M3-F6 | `mul(gate(dist_52w_high, near), momentum)` | 阈值型 | 20–60 |

### M4 信息缓慢扩散

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M4-F4 | 同行业昨日等权收益（行业动量溢出） | 单调 | 1–5 |
| M4-F4 | 同行业**大市值股票**昨日收益 → 预测小市值（领先滞后） | 单调 | 1–5 |
| M4-F5 | `ret_i − β·ret_industry` 的滚动均值（滞后程度） | 单调 | 5–10 |
| M4-F6 | `mul(gate(turnover, low), industry_spillover)` —— 关注度低吸收更慢 | 低区更强 | 1–5 |

### M5 交易约束（A 股特有，外部竞品不会做）

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M5-F7 | **炸板事件**：盘中触及 `limit_up` 但收盘 < `limit_up` 的 CAR | 事后负 | — |
| M5-F7 | 停牌复牌事件的 CAR | 复牌后调整 | — |
| M5-F6 | `mul(gate(is_limit_up_yesterday), any_signal)` | 约束区反向 | 1–3 |
| M5-F6 | `mul(gate(is_st), any_signal)` —— ST 股涨跌幅限制 5% | 约束区衰减更慢 | 3–10 |

### M6 被动资金与再平衡

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M6-F7 | **月末成分状态观察事件**（比对相邻两次月末成分快照）的 CAR[-10,+10] | 只能解释为观察日附近的条件关系，不代表真实调仓生效 | — |
| M6-F2 | 指数权重的月度变化（相邻快照差分） | 单调 | 5–20 |
| M6-F4 | 同时属于多个宽基指数 vs 只属于一个 | 阈值 | 20–60 |
| M6-F6 | `mul(gate(is_index_member), reversal)` | 分组差异 | 1–5 |

### M7 参与者结构（本数据集独有资源）

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M7-F1 | **`avg_trade_size = div(total_turnover, num_trades)`** | 单调 | 5–20 |
| M7-F3 | `ts_z(avg_trade_size, 60)`（大单占比的异常变化） | 单调 | 3–10 |
| M7-F2 | `diff(log(avg_trade_size), 5)` | 单调 | 3–10 |
| M7-F5 | **大单流入但价格没涨**（`ts_z(avg_trade_size)` 高而 `ret_5d` 低） | 非单调 | 5–20 |
| M7-F6 | `mul(gate(avg_trade_size, high), reversal)` —— 机构主导的反转更弱 | 高区衰减 | 1–5 |

> `num_trades` 是本数据集里最被低估的一列。绝大多数公开因子库没有成交笔数，
> 因此 `total_turnover / num_trades` 这一族在文献覆盖上相对稀薄。

### M10 隔夜信息聚合（本数据集最稀缺的资源）

| 格 | 候选表达式 | 形状先验 | peak h |
|---|---|---|---|
| M10-F1 | **竞价委托失衡** `(Σb_v − Σa_v)/(Σb_v + Σa_v)` | 单调 | 1–3 |
| M10-F1 | **竞价盘口价差** `(a1−b1)/mid` —— 也是成本模型的输入 | 单调（宽价差 → 高补偿） | 1–5 |
| M10-F1 | **竞价成交额占比** `auction_turnover / ts_mean(total_turnover, 20)` | 饱和 | 1–3 |
| M10-F2 | 竞价失衡相对昨日的变化 | 单调 | 1–2 |
| M10-F5 | **竞价强但全天弱**：`gap_open` 高而 `ret_intraday` 低 | 非单调 | 1–5 |
| M10-F5 | 竞价失衡方向与开盘缺口方向相反 | 非单调 | 1–3 |
| M10-F6 | `mul(gate(auction_volume_share, high), gap_open)` | 高区更强 | 1–3 |
| M10-F7 | 大幅跳空（>3σ）的 CAR | 事后延续或反转 | — |

## F.5 冷启动的 2×2 析因设计

见 M9.7。四个格子：**M2-F3 / M2-F1 / M1-F3 / M1-F1**。

## F.6 变量注册清单

按 M2.2 的量纲分派表分类。**每个变量进入 IC 计算前必须至少套一层 `ts_rank` 或 `ts_z`。**

| 变量 | 来源 | 量纲类型 | 标准化 | causal_role | scale_exp |
|---|---|---|---|---|---|
| `ret_1d`, `ret_5d`, `ret_20d` | adj_close | 有符号连续 | `ts_z`/`xs_rank`，切点含 0 | signal | 0 |
| `turnover_today` | turnover.today | 无符号幅度 | log 后分位 | signal / moderator | 0 |
| `turnover_accel` | today − week | 有符号连续 | `ts_z` | signal | 0 |
| `avg_trade_size` | total_turnover / num_trades | 无符号幅度 | log 后 `ts_z` | signal / moderator | 1 |
| `amihud` | `ts_mean(abs(ret)/total_turnover, 20)` | 无符号幅度 | log 后分位 | **moderator** | −1 |
| `realized_vol` | `ts_std(ret_1d, 20)` | 无符号幅度 | log 后分位 | **moderator（不是 signal）** | 0 |
| `auction_imbalance` | `(Σb_v−Σa_v)/(Σb_v+Σa_v)` | 有界 [−1,1] | 原值分位，切点含 0 | signal | 0 |
| `auction_spread` | `(a1−b1)/mid` | 无符号幅度 | log 后分位 | signal / 成本输入 | 0 |
| `auction_turnover_share` | 竞价成交额 / ADV | 有界 [0,1] | 分位，端点单独成层 | signal | 0 |
| `gap_open` | `open/prev_close − 1` | 有符号连续 | `ts_z`，切点含 0 | signal | 0 |
| `ret_intraday` | `close/open − 1` | 有符号连续 | `ts_z`，切点含 0 | signal | 0 |
| `dist_52w_high` | `close / ts_max(high, 252)` | 有界 [0,1] | 分位，**注意端点堆积** | signal | 0 |
| `market_cap` | valuation.market_cap | 无符号幅度 | log 后分位 | **moderator** | 1 |
| `pb_ratio_lf`, `pe_ratio_ttm` | valuation | 有符号连续（可为负） | 分位，负值单独成层 | signal | 0 |
| `is_limit_up`, `is_limit_down` | close vs limit_up/down | 二元 | 无 | **selector（只做交互）** | — |
| `is_st`, `is_suspended` | st_flag / suspension | 二元 | 无 | selector | — |
| `industry_ret_1d` | 同行业等权（PIT 行业） | 有符号连续 | `ts_z` | signal | 0 |
| `index_weight_chg` | index_weights 相邻快照差 | 有符号连续 | 分位 | signal | 0 |
| `is_index_member` | index_components | 二元 | 无 | selector / moderator | — |
| `yield_10y`, `yield_slope` | yield_curve | 有符号连续 | `ts_z` | **confounder / regime** | 0 |
| `days_since_listing` | date − listed_date | 计数 | `log1p` | selector | — |

## F.7 本数据集独有的三个优势（报告里单独讲）

1. **`num_trades`（成交笔数）**：能构造平均单笔成交额，这是参与者结构的直接代理。多数公开因子库没有这一列，因此 M7 整行的文献覆盖相对稀薄。
2. **`open_auction` 五档盘口**：日频数据里罕见地包含真实的买卖盘口与委托量。它同时支撑 M10 整行、以及成本模型里的**真实价差估计**（而不是拍一个固定假设）。
3. **`industry_sws_2021_exact_changes`**：行业归属的精确有效期，使 PIT 行业中性化可以做对。多数团队用月末快照，会有最多一个月的错配。

## F.8 明确不做的（写进报告的边界声明）

- 分钟频与日内数据：没有
- 文本、研报、公告：没有
- 融资融券、龙虎榜、北向资金：本数据集不含
- 财务报表基本面：只有估值比率，没有原始科目，因此不做基本面因子
- **跨市场迁移（岛屿模型）**：需要第二个市场的数据，列为 v2 议题

---

# 附录 G · 术语速查

| 术语 | 一句话 | 详见 |
|---|---|---|
| IC | 因子值与未来收益的截面相关系数 | 0.1 |
| n_eff | 有效样本量 = 1/σ_IC²，本项目约 2500–5000 | M1.6 |
| Ψᵢ | 逐观测的影响函数分解，"这个观测把 IC 往哪拉了多少" | M4.2 |
| 标签 labels | 五个受控词表字段，构成统计模型的设计矩阵 | 0.2 / M2.4 |
| 断言 P1..Pn | 编号的可判真假命题，冻结后不可改 | 0.2 / M3.3 |
| 结构 structure | 一份完整档案，系统操作的基本单位 | 1.3 |
| 机制地图 | 10×7 的想法空间，52 格可行 | 附录 F |
| 四把刀 | 安慰剂 / 断言 / 增量 / 正交矩 | M5 |
| e 值 | 满足 `E_{H₀}[e] ≤ 1` 的量，可累乘成赌本 | M6 |
| 赌本 W | `Π eᵢ`，首版仅作实验性证据轨迹，不直接判 PASS/FAIL | M6.7 |
| 三态判定 | PASS / FAIL / UNDECIDABLE | 0.2 |
| 探索段 A | 2016-07 ~ 2022-06，用于测量与搜索 | M1.5 |
| 确认段 B | 2022-07 ~ 2025-07，**只有刀④能碰** | M1.5 |
| 封存段 H | 2025-08 ~ 2026-07，全项目只读一次 | M1.5 |
| 缝 | 搜索出候选之后、读确认样本之前，写机制的那一步 | M7.7 |
| 受挫圈 | 符号图里含奇数负边的圈，定位缺失的调节变量 | M8.1 |
| family | 四个机制槽位折叠成的单一因子（不可分离） | M9.2 / M9.7 |
| 仓位缩放 | 方向不变、覆盖不变，只把噪声大处的仓位调小 | M7.3 |
| 覆盖路径 | 因子 = 若干稀疏结构的拼接，不是一个稠密因子 | M10.6 |
